# IGCSE 0606 附加数学综合笔记第二版本

本笔记严格按照 **剑桥 IGCSE 附加数学（0606）2028–2030 考纲** 编写。附加数学课程位于 IGCSE 普通数学之上，旨在拓展代数、函数、几何、三角与微积分的深度，为 AS/A Level Further Math, AP calculus BC, IB AA HL 数学打下坚实基础。

每一章的编排遵循"概念定义 → 公式推导 → 典型例题"的结构。例题选自历年真题风格，难度对标考试要求。
// 里面部分题型是有真题。引用源https://www.cambridgeinternational.org/programmes-and-qualifications/cambridge-igcse-mathematics-additional-0606/past-papers/

> **说明**：考卷的公式列表中会给出部分公式（如等差/等比数列的求和公式、二项式展开公式），但本笔记仍完整推导，以帮助理解而非机械记忆。考试中，**理解何时使用哪个公式**比背公式更重要。
---
> 本笔记基于剑桥 IGCSE 附加数学（0606）2025-2027 考纲编写，按照认知顺序排列，涵盖所有主题。包含推导过程、典型例题和常见易错点。适合自学。
- 这份笔记由 LG-leo 整理和维护。如果你觉得这份笔记对你有帮助，欢迎在 GitHub 上关注我或给我一个 ⭐，这能帮助我持续产出更多免费的学习资源。
- 我的其他课程笔记：https://github.com/LG-leo?tab=repositories

- This note is maintained by LG-leo. If you find it helpful, feel free to follow me or leave a ⭐ on GitHub. It helps me keep producing more free study resources. Check out my other notes: https://github.com/LG-leo?tab=repositories
---
---

## 目录

- [第 1 章：数列、排列、组合与二项式定理](#第-1-章数列排列组合与二项式定理)
- [第 2 章：向量与变化率](#第-2-章向量与变化率)
- [第 3 章：二次函数（含多项式因式）](#第-3-章二次函数含多项式因式)
- [第 4 章：函数（线性、三次、指数、对数）](#第-4-章函数线性三次指数对数)
- [第 5 章：微分（导数）](#第-5-章微分导数)
- [第 6 章：方程与不等式（图形法）](#第-6-章方程与不等式图形法)
- [第 7 章：积分（不定积分与定积分）](#第-7-章积分不定积分与定积分)
- [第 8 章：三角学（含弧度法）](#第-8-章三角学含弧度法)
- [第 9 章：几何（直线与圆）](#第-9-章几何直线与圆)
- [第 10 章：综合应用](#第-10-章综合应用)

---

# 第 1 章：数列、排列、组合与二项式定理

本章将三个看似不同但内核相通的主题放在一起。**数列**训练你发现规律和求和的能力；**排列与组合**训练你系统化计数的逻辑；**二项式定理**则是排列组合在代数展开中的直接应用。

---

## 考纲对照表（Syllabus Mapping）

| 考纲编号 | 英文原文（Syllabus 2028–2030） | 中文说明 | 覆盖章节 |
|:-------:|------|---------|:-------:|
| **11.1** | *Recognise the difference between permutations and combinations and know when each should be used.* | 识别排列与组合的区别，知道何时使用哪个 | §1.2.4 |
| **11.2** | *Know and use the notation $`n!`$ and the expressions for permutations and combinations of $`n`$ items taken $`r`$ at a time. Includes $`0! = 1`$.* | 掌握 $`n!`$、$`^nP_r`$、$`^nC_r`$ 的记号与表达式 | §1.2.2, §1.2.3 |
| **11.3** | *Solve problems on arrangement and selection using permutations or combinations. Problems will be either in an everyday context or based on an algebraic problem. Problems involving: repetition of objects; objects arranged in a circle; both permutations and combinations, are **not** included.* | 用排列或组合解决排列与选择问题（日常生活或代数背景）。不考重复元素、圆排列、排列组合混合。 | §1.2.5 |
| **12.1** | *Use the binomial theorem for expansion of $`(a+b)^n`$ for positive integer $`n`$. Includes simplification of coefficients.* | 使用二项式定理展开 $`(a+b)^n`$（$`n`$ 为正整数），包含系数的化简 | §1.3.1 |
| **12.2** | *Use the general term $`C(n,r)a^{n-r}b^r`$, $`0 ≤ r ≤ n`$. For example: Find the term independent of $`x`$ in the expansion of $`(x^2 + 1/x)^{10}`$. Knowledge of the greatest term and properties of the coefficients is **not** required.* | 使用通项公式求特定项（如常数项）。不考最大项和系数性质。 | §1.3.2, §1.3.3 |
| **12.3** | *Recognise arithmetic and geometric progressions and understand the difference between them.* | 识别等差数列与等比数列，理解它们之间的区别 | §1.1.4 |
| **12.4** | *Use the formulas for the $`n`$th term and for the sum of the first $`n`$ terms to solve problems involving arithmetic or geometric progressions. Problems may be in context.* | 使用第 $`n`$ 项和前 $`n`$ 项和公式解决等差/等比数列问题（可能含实际情境） | §1.1.2, §1.1.3 |
| **12.5** | *Use the condition for the convergence of a geometric progression, and the formula for the sum to infinity of a convergent geometric progression. Includes explaining why a particular geometric progression has or does not have a sum to infinity.* | 使用等比数列的收敛条件及无穷和公式，**包含解释为什么某个等比级数有/无无穷和** | §1.1.3 |

---

## 1.1 数列（等差与等比）

数列是按顺序排列的一列数。附加数学中重点考察两种最基本的数列：**等差数列**（相邻两项的差固定）和**等比数列**（相邻两项的比固定）。

### 1.1.1 求和符号 $`Σ`$

在深入讨论数列之前，我们需要掌握求和符号 $`Σ`$（希腊字母 Sigma）。它表示将一系列数相加：

$$`Σ_{k=1}^{n} a_k = a_1 + a_2 + a_3 + ⋯ + a_n`$$

求和运算满足三条基本法则：

**法则 1——常数因子提取**：

$$`Σ_{k=1}^{n} (c · a_k) = c · Σ_{k=1}^{n} a_k`$$

**法则 2——和的拆分**：

$$`Σ_{k=1}^{n} (a_k ± b_k) = Σ_{k=1}^{n} a_k ± Σ_{k=1}^{n} b_k`$$

**法则 3——常数的和**：

$$`Σ_{k=1}^{n} c = n c`$$

**示例**：计算 $`Σ_{k=1}^{5} (3k - 2)`$。

$$`Σ_{k=1}^{5} (3k - 2) = 3Σ_{k=1}^{5} k - Σ_{k=1}^{5} 2 = 3 × (1+2+3+4+5) - 5 × 2 = 3 × 15 - 10 = 45 - 10 = 35`$$

---

### 1.1.2 等差数列（Arithmetic Progression）

#### 定义

一个数列如果从第二项起，每一项与它的前一项的差等于**同一个常数**，则称为**等差数列**。这个常数叫做**公差**，记作 $`d`$：

$$`a_{n+1} - a_n = d`（对所有 $`n ≥ 1`$ 成立）$$

**示例**：$`5, 9, 13, 17, 21`$ 是等差数列，$`d=4`$，$`a=5`$。

#### 第 $`n`$ 项公式（推导）

设首项 $`a_1 = a`$，则：

$$`a_2 = a + d`$$
$$`a_3 = a_2 + d = a + 2d`$$
$$`a_4 = a_3 + d = a + 3d`$$

规律：第 $`n`$ 项是在首项的基础上加了 $`(n-1)`$ 次公差。

$$`a_n = a + (n-1)d`$$

**验证**：$`n=1`$ 时 $`a_1 = a`$；$`n=2`$ 时 $`a_2 = a + d`$。正确。

#### 前 $`n`$ 项和公式（推导——倒序相加法）

$$`S_n = a + (a+d) + (a+2d) + ⋯ + [a+(n-1)d]`$$
$$`S_n = [a+(n-1)d] + [a+(n-2)d] + ⋯ + a`$$

两式相加，每一对的和均为 $`2a + (n-1)d`$，共 $`n`$ 对：

$$`2S_n = n[2a + (n-1)d]`$$

$$`S_n = n/2 [2a + (n-1)d]`$$

若末项 $`l = a_n = a + (n-1)d`$，则：

$$`S_n = n/2 (a + l)`$$

#### 例题

**例题 1**：首项 $`7`$，公差 $`3`$，求第 $`15`$ 项。

$$`a_{15} = 7 + 14 × 3 = 7 + 42 = 49`$$

---

**例题 2**：首项 $`2`$，公差 $`5`$，前 $`n`$ 项和 $`156`$，求 $`n`$。

$$`n/2 [4 + (n-1) × 5] = 156 ⇒ n/2 (5n - 1) = 156`$$

$$`5n^2 - n - 312 = 0 ⇒ n = (1 ± √(1 + 6240))/10 = (1 ± 79)/10`$$

$`n`$ 为正整数，$`n = 80/10 = 8`$。

---

**例题 3**（实际情境）：第一年存 $`500`$ 元，之后每年多存 $`50`$ 元。$`10`$ 年共存多少钱？

$$`S_{10} = 10/2 [2 × 500 + 9 × 50] = 5 × (1000 + 450) = 7250`$$

答：$`7250`$ 元。

---

### 1.1.3 等比数列（Geometric Progression）

#### 定义

一个数列如果从第二项起，每一项与它的前一项的比值等于**同一个常数**，则称为**等比数列**。这个常数叫做**公比**，记作 $`r`$：

$$`a_{n+1}/a_n = r`（对所有 $`n ≥ 1`$ 成立）$$

**示例**：$`2, 6, 18, 54, 162`$ 是等比数列，$`r=3`$，$`a=2`$。

#### 第 $`n`$ 项公式

$$`a_2 = a r`$$
$$`a_3 = a_2 · r = a r^2`$$
$$`a_4 = a_3 · r = a r^3`$$

$$`a_n = a r^{n-1}`$$

#### 前 $`n`$ 项和公式（推导——错位相减法）

$$`S_n = a + a r + a r^2 + ⋯ + a r^{n-1}`$$

$$`r S_n = a r + a r^2 + a r^3 + ⋯ + a r^{n}`$$

相减：

$$`S_n - r S_n = a - a r^{n}`$$

$$`S_n(1 - r) = a(1 - r^{n})`$$

若 $`r = 1`$：$`S_n = n a`$。

若 $`r ≠ 1`$：

$$`S_n = a · (1 - r^{n})/(1 - r)`$$ 或等价地 $$`S_n = a · (r^{n} - 1)/(r - 1)`$$

#### 无穷等比级数的收敛条件与和（**考纲 12.5 重点**）

$$`S_∞ = \lim_{n → ∞} S_n = \lim_{n → ∞} a · (1 - r^{n})/(1 - r)`$$

$`r^{n}`$ 在 $`n → ∞`$ 时的行为决定了级数是否收敛：

| $`r`$ 的取值范围 | $`n → ∞`$ 时 $`r^{n}`$ 的行为 | 级数是否收敛 | 无穷和 |
|:-------------:|----------------------------------|:-----------:|:------:|
| $`|r| < 1`$ | $`r^{n} → 0`$ | ✅ **收敛** | $`S_∞ = a/(1-r)`$ |
| $`|r| > 1`$ | $`|r^{n}| → ∞`$ | ❌ **发散** | 无有限和 |
| $`r = 1`$ | $`r^{n} = 1`$（恒定） | ❌ **发散** | $`S_n = n a → ∞`$ |
| $`r = -1`$ | 在 $`1`$ 和 $`-1`$ 之间振荡 | ❌ **发散** | 和不确定 |

因此，**等比级数收敛的充要条件是 $`|r| < 1`$**。

考试中如果被要求"解释为什么某个等比级数有/没有无穷和"，你需要说明：当 $`|r| < 1`$ 时，$`n → ∞`$ 使 $`r^{n} → 0`$，因此 $`S_n → a/(1-r)`$；当 $`|r| ≥ 1`$ 时，$`r^{n}`$ 不趋近于 $`0`$，$`S_n`$ 不趋近于有限值。

#### 例题

**例题 1**：首项 $`3`$，公比 $`2`$，求第 $`6`$ 项。

$$`a_6 = 3 × 2^5 = 3 × 32 = 96`$$

---

**例题 2**（收敛判断 + 无穷和）：$`12 + 6 + 3 + 3/2 + ⋯`$

首项 $`a = 12`$，公比 $`r = 6/12 = 1/2`$。

因为 $`|r| = 1/2 < 1`$，当 $`n → ∞`$ 时 $`(1/2)^{n} → 0`$，所以级数收敛。

$$`S_∞ = 12/(1 - 1/2) = 12/(1/2) = 24`$$

---

**例题 3**（已知 $`S_∞`$ 求 $`r`$）：无穷等比级数收敛，和为 $`8`$，首项为 $`4`$，求公比 $`r`$。

$$`8 = 4/(1 - r) ⇒ 8(1 - r) = 4 ⇒ 1 - r = 1/2 ⇒ r = 1/2`$$

验证：$`|r| = 1/2 < 1`$，满足收敛条件。

---

### 1.1.4 对照：等差数列 vs 等比数列（**考纲 12.3 重点**）

| 特征 | 等差数列 (AP) | 等比数列 (GP) |
|:----|:-------------|:-------------|
| **定义性质** | $`a_{n+1} - a_n = d`$（差恒定） | $`a_{n+1}/a_n = r`$（比恒定） |
| **检查方法** | 相邻项相减，看是否恒等 | 相邻项相除，看是否恒等 |
| **第 $`n`$ 项** | $`a_n = a + (n-1)d`$ | $`a_n = a r^{n-1}`$ |
| **前 $`n`$ 项和** | $`S_n = n/2[2a + (n-1)d]`$ | $`S_n = a(1-r^n)/(1-r)`$（$`r ≠ 1`$） |
| **增长模式** | **线性增长**（每次加固定值） | **指数增长/衰减**（每次乘固定值） |
| **无穷和** | 不存在（除 $`d=0`$ 常数数列） | $`|r| < 1`$ 时收敛于 $`a/(1-r)`$ |

**判断练习**：

1. $`5, 8, 11, 14, 17, …`$ → 差恒为 $`3`$ → **AP** ✓
2. $`5, 10, 20, 40, 80, …`$ → 比恒为 $`2`$ → **GP** ✓
3. $`1, 4, 9, 16, 25, …`$ → 差为 $`3,5,7,9`$（不恒定），比为 $`4, 2.25, 1.78,…`$（不恒定）→ **两者都不是** ✗
4. $`100, 50, 25, 12.5, …`$ → 比恒为 $`1/2`$ → **GP**（收敛）✓

---

## 1.2 排列与组合

排列与组合回答同一个基本问题："有多少种不同的方式？"二者的核心区别在于 **顺序是否重要**。

### 1.2.1 乘法原理

如果一项任务可以分解为 $`k`$ 个依次进行的步骤，第 $`i`$ 步有 $`m_i`$ 种方法，则总方法数为：

$$`m_1 × m_2 × ⋯ × m_k`$$

**示例**：A→B 有 $`3`$ 条路，B→C 有 $`4`$ 条路，A→B→C 共有 $`3 × 4 = 12`$ 条路线。

### 1.2.2 排列——顺序重要（Permutations）

**定义**：从 $`n`$ 个不同元素中取出 $`r`$ 个（$`r ≤ n`$），按一定顺序排成一列。顺序不同算不同排列。记作 $`^nP_r`$ 或 $`P(n,r)`$。

**推导**：

$$`^nP_r = n(n-1)(n-2)⋯(n-r+1)`$$

用阶乘表示：

$$`^nP_r = n!/(n-r)!`$$

**特殊值**：$`^nP_n = n!`$，$`^nP_0 = 1`$，$`0! = 1`$。

### 1.2.3 组合——顺序不重要（Combinations）

**定义**：从 $`n`$ 个不同元素中取出 $`r`$ 个（$`r ≤ n`$），不考虑顺序。记作 $`^nC_r`$、$`C(n,r)`$ 或 $`C(n,r)`$。

**推导**：排列数除以内部排列数：

$$`^nC_r = C(n,r) = (^nP_r)/r! = n!/(r!(n-r)!)`$$

**性质**：

- 对称性：$`C(n,r) = C(n,n-r)`$
- 边界值：$`C(n,0) = C(n,n) = 1`$，$`C(n,1) = C(n,n-1) = n`$

### 1.2.4 如何选择？（**考纲 11.1 重点**）

问自己：**交换任意两个已选元素，结果是否不同？**

| 情境 | 顺序重要？ | 使用 | 示例 |
|:----|:---------:|:----|:-----|
| 排队、名次、密码、排名 | ✅ 是 | **排列** $`^nP_r`$ | 6 人选 3 人站一排 |
| 委员会、选科、抽奖、组队 | ❌ 否 | **组合** $`^nC_r`$ | 6 人选 3 人组委员会 |

### 1.2.5 应用题例题

**例题 1**（排列——日常生活）：6 本不同的书选 4 本排上书架（从左到右），多少种摆法？

$$`^6P_4 = 6!/(6-4)! = 6!/2! = 6 × 5 × 4 × 3 = 360`$$

答：$`360`$ 种。

---

**例题 2**（组合——日常生活）：7 男 5 女中选 4 人组委员会，要求至少 2 名女生，多少种选法？

- $`2`$ 女 $`2`$ 男：$`C(5,2) × C(7,2) = 10 × 21 = 210`$
- $`3`$ 女 $`1`$ 男：$`C(5,3) × C(7,1) = 10 × 7 = 70`$
- $`4`$ 女 $`0`$ 男：$`C(5,4) × C(7,0) = 5 × 1 = 5`$

总方法数：$`210 + 70 + 5 = 285`$。

> **补集法验证**：总数 $`C(12,4)=495`$，减去"至多 1 名女生"（$`0`$ 女 $`4`$ 男 $`= C(5,0)C(7,4)=35`$，$`1`$ 女 $`3`$ 男 $`= C(5,1)C(7,3)=5×35=175`$，共 $`210`$），$`495 - 210 = 285`$ ✓

---

**例题 3**（代数背景——组合恒等式）：证明 $`C(n,r) + C(n,r-1) = C(n+1,r)`$，并用此计算 $`C(8,3) + C(8,2)`$。

**证明**：

$$`C(n,r) + C(n,r-1)`$$
$$`= n!/(r!(n-r)!) + n!/((r-1)!(n-r+1)!)`$$
$$`= n!/(r!(n-r+1)!) [(n-r+1) + r]`$$
$$`= n! · (n+1)/(r!(n-r+1)!) = (n+1)!/(r!(n+1-r)!) = C(n+1,r)`$$

**应用**：

$$`C(8,3) + C(8,2) = C(9,3) = (9 × 8 × 7)/(3 × 2 × 1) = 84`$$

### 1.2.6 ⚠️ 考试不考的内容

根据考纲，以下类型**不会出现**：

| 不考内容 | 英文原文 |
|---------|---------|
| ❌ 重复元素的排列 | *repetition of objects* |
| ❌ 圆排列 | *objects arranged in a circle* |
| ❌ 排列与组合在同一题中混合使用 | *both permutations and combinations* |

考试中每道题要么是纯排列，要么是纯组合。

---

## 1.3 二项式定理（Binomial Theorem）

### 1.3.1 二项式展开（**考纲 12.1**）

$`(a+b)^n`$ 可看作 $`n`$ 个 $`(a+b)`$ 相乘。展开时，每一项是分别从每个因子选 $`a`$ 或 $`b`$ 的乘积。要得到 $`a^{n-r}b^{r}`$，需从 $`r`$ 个因子中选 $`b`$，从 $`n-r`$ 个因子中选 $`a`$。选择方式数为 $`C(n,r)`$。

对 $`r = 0, 1, …, n`$ 求和：

$$`(a+b)^n = Σ_{r=0}^{n} C(n,r) a^{n-r} b^{r}`$$

**示例**：展开 $`(x+3)^4`$。

$$`(x+3)^4 = C(4,0)x^4(3)^0 + C(4,1)x^3(3)^1 + C(4,2)x^2(3)^2 + C(4,3)x^1(3)^3 + C(4,4)x^0(3)^4`$$
$$`= 1 · x^4 + 4 · 3x^3 + 6 · 9x^2 + 4 · 27x + 1 · 81`$$
$$`= x^4 + 12x^3 + 54x^2 + 108x + 81`$$

注意：每一项的系数都经过了**化简**（如 $`4 × 3 = 12`$，$`6 × 9 = 54`$），这是考纲明确要求的。

### 1.3.2 通项公式与特定项（**考纲 12.2**）

展开式中的第 $`(r+1)`$ 项（从 $`r=0`$ 开始计数）：

$$`T_{r+1} = C(n,r) a^{n-r} b^{r}`$$

常见题型：

| 题型 | 方法 |
|:----|:----|
| 求 $`x^k`$ 的系数 | 设通项中 $`x`$ 的指数 $`= k`$，解出 $`r`$ |
| 求常数项（不含 $`x`$） | 设 $`x`$ 的指数 $`= 0`$，解出 $`r`$ |
| 求 $`x^p y^q`$ 的系数 | 分别设 $`x`$ 和 $`y`$ 的指数匹配 |

> **⚠️ 重要区分**：
> - **二项式系数** = $`C(n,r)`$（只与 $`n,r`$ 有关）
> - **完整系数** = $`C(n,r)`$ × $`a`$ 和 $`b`$ 中的常数因子及符号
>
> 例如 $`(2x-3)^5`$ 中 $`r=2`$：二项式系数 $`C(5,2)=10`$，完整系数 $`= 10 · (2)^3 · (-3)^2 = 720`$

### 1.3.3 例题

**例题 1**（求指定幂次的系数）：求 $`(2x + 1/x)^7`$ 展开式中 $`x`$ 的系数。

**解**：

$$`T_{r+1} = C(7,r) (2x)^{7-r} (1/x)^{r}`$$
$$`= C(7,r) 2^{7-r} x^{7-r} · x^{-r}`$$
$$`= C(7,r) 2^{7-r} x^{7-2r}`$$

令 $`7-2r = 1`$，得 $`r = 3`$。

$$`T_4 = C(7,3) 2^{4} x = 35 × 16 × x = 560x`$$

所以 $`x`$ 的系数为 $`560`$。

---

**例题 2**（求常数项——考纲典型）：求 $`(x^2 - 2/x)^6`$ 展开式中的常数项。

**解**：

$$`T_{r+1} = C(6,r) (x^2)^{6-r} (-2/x)^{r}`$$
$$`= C(6,r) (-2)^r x^{12-2r} · x^{-r}`$$
$$`= C(6,r) (-2)^r x^{12-3r}`$$

令 $`12 - 3r = 0`$，得 $`r = 4`$。

$$`T_5 = C(6,4) (-2)^4 = 15 × 16 = 240`$$

所以常数项为 $`240`$。

---

**例题 3**（两个二项式乘积的系数）：求 $`(1+x)^5 (2-x)^4`$ 展开式中 $`x^2`$ 的系数。

**解**：

$`(1+x)^5`$ 通项：$`C(5,r) x^r`$
$`(2-x)^4`$ 通项：$`C(4,k) 2^{4-k} (-1)^k x^k`$

需要 $`r + k = 2`$：

| $`r`$ | $`k`$ | 计算 | 贡献 |
|:--:|:--:|:----|:----:|
| $`0`$ | $`2`$ | $`C(5,0) · C(4,2) 2^{2} (-1)^2 = 1 × 6 × 4 × 1`$ | $`24`$ |
| $`1`$ | $`1`$ | $`C(5,1) · C(4,1) 2^{3} (-1)^1 = 5 × 4 × 8 × (-1)`$ | $`-160`$ |
| $`2`$ | $`0`$ | $`C(5,2) · C(4,0) 2^{4} (-1)^0 = 10 × 1 × 16 × 1`$ | $`160`$ |

总和：$`24 + (-160) + 160 = 24`$。

答：$`x^2`$ 的系数为 $`24`$。

---

### 1.3.4 二项式系数的性质（杨辉三角）

**对称性**：

$$`C(n,r) = C(n,n-r)`$$

**杨辉法则**（递推关系）：

$$`C(n,r) + C(n,r-1) = C(n+1,r)`$$

**杨辉三角**：

```
n=0:        1
n=1:       1 1
n=2:      1 2 1
n=3:     1 3 3 1
n=4:    1 4 6 4 1
n=5:   1 5 10 10 5 1
```

> **考试提示**：$`n ≤ 5`$ 时可用杨辉三角直接写系数，$`n ≥ 6`$ 时用组合数公式 $`C(n,r)`$。

> **不考内容**（考纲明确说明）：最大项（greatest term）和系数的性质（properties of the coefficients）不考。

---

## 本章公式速查表

| 主题 | 公式 | 条件 |
|:----|:----|:----|
| AP 第 $`n`$ 项 | $`a_n = a + (n-1)d`$ | — |
| AP 前 $`n`$ 项和 | $`S_n = n/2[2a + (n-1)d]`$ | — |
| GP 第 $`n`$ 项 | $`a_n = a r^{n-1}`$ | — |
| GP 前 $`n`$ 项和 | $`S_n = a(1-r^n)/(1-r)`$ | $`r ≠ 1`$ |
| GP 无穷和 | $`S_∞ = a/(1-r)`$ | $`|r| < 1`$ |
| 排列数 | $`^nP_r = n!/(n-r)!`$ | 顺序重要 |
| 组合数 | $`C(n,r) = n!/(r!(n-r)!)`$ | 顺序不重要 |
| 二项式定理 | $`(a+b)^n = Σ_{r=0}^{n} C(n,r) a^{n-r} b^{r}`$ | $`n ∈ ℤ^+`$ |
| 通项 | $`T_{r+1} = C(n,r) a^{n-r} b^{r}`$ | 用于求特定项 |

---
---

# 第 2 章：向量与变化率

## 考纲对照

本章对应剑桥 IGCSE 附加数学（0606）2028–2030 考纲的以下内容：

| 考纲编号 | 内容 | 说明 |
|---------|------|------|
| **13.1** | 理解并使用向量记号 | 列向量、$`𝐢`$-$`𝐣`$ 形式、$`\overrightarrow{AB}`$、$`p`$ 等形式 |
| **13.2** | 位置向量与单位向量 | 求单位向量 $`𝐚̂ = 𝐚 / \|𝐚\|`$ |
| **13.3** | 向量的模、加法、减法与数乘 | 含向量相等、向量几何问题（给定图形） |
| **13.4** | 速度的合成与分解 | 求合向量、利用速度向量求位置、相撞问题 |
| **14.1** | 导函数的思想（变化率入门） | 极限的直观理解，不要求从第一原理求导 |

---

## 引言

在现实世界中，许多量不仅有大小，还有方向。从飞机航行时的风速修正，到两艘船在海上是否可能相撞，再到抛体运动的轨迹分析——这些问题的数学语言都是**向量**。

本章将从零开始构建二维向量的完整知识体系。我们先学习向量的基本表示方法和运算规则（含垂直向量的判定），然后把这些工具应用于几何问题和运动学问题（速度合成、相撞检测）。最后，我们将视角从"静态的向量"转向"动态的变化率"——当位置向量随时间变化时，它的变化率就是速度，而速度的变化率就是加速度。反过来，已知加速度，我们可通过积分求出速度和位置。这个思想将直接通向第 5 章的微分学与第 7 章的积分学。

---

## 2.1 二维向量基础

### 2.1.1 什么是向量？

向量（vector）是一个既有**大小**又有**方向**的量。与它相对的是标量（scalar），标量只有大小没有方向。例如：

- **向量**：位移、速度、力
- **标量**：距离、速率、质量、温度

在二维平面中，向量有多种等价的表示方式。

#### 表示法 1：列向量

$`𝐯 = (x, y)`$

其中 $`x`$ 是水平分量，$`y`$ 是垂直分量。这种表示法在解方程组时非常方便，因为我们可以直接对分量进行操作。

#### 表示法 2：$`𝐢`$-$`𝐣`$ 形式

定义两个基本单位向量：

$`𝐢 = (1, 0), \quad 𝐣 = (0, 1)`$

$`𝐢`$ 指向正 $`x`$ 轴方向（向右），$`𝐣`$ 指向正 $`y`$ 轴方向（向上）。那么任意向量可以写为：

$`𝐯 = x𝐢 + y𝐣`$

**两种表示法的等价性**：

$`x𝐢 + y𝐣 = (x, y)`$

例如，向量 $`3𝐢 - 2𝐣`$ 与列向量 $`(3, -2)`$ 是同一个向量。

#### 表示法 3：有向线段

从点 $`A`$ 到点 $`B`$ 的向量记作 $`\overrightarrow{AB}`$。它等于终点位置减去起点位置：

$`\overrightarrow{AB} = 𝐫_B - 𝐫_A`$

> **为什么是"终点减起点"？** 假设你想从家 $`A`$ 走到学校 $`B`$。你的位移（从 $`A`$ 到 $`B`$）就是你最终到达的位置（学校的位置）减去你出发时的位置（家的位置）。如果 $`A`$ 在 $`(1,1)`$，$`B`$ 在 $`(4,5)`$，那么你需要向右走 $`3`$ 个单位、向上走 $`4`$ 个单位，即 $`\overrightarrow{AB} = (3,4)`$。

---

### 2.1.2 位置向量

设 $`O`$ 为原点。任意点 $`P(x, y)`$ 的**位置向量**是：

$`𝐫 = \overrightarrow{OP} = (x, y) = x𝐢 + y𝐣`$

位置向量告诉我们点 $`P`$ 相对于原点的位置。这里的关键区分是：**点** $`P(x, y)`$ 是空间中的一个位置，而**向量** $`𝐫 = (x, y)`$ 是从原点到该点的有向线段——它包含了位移的信息。

> **点和向量的区别**：点是一个位置，像地图上的一个坐标。向量是一个位移，像"向右 3 步，向上 2 步"。同一个向量可以从任何起点出发，但同一个点只能在一个位置。

---

### 2.1.3 向量的模（大小）

向量 $`𝐯 = x𝐢 + y𝐣`$ 的模（或称长度、大小）由勾股定理给出：

$`|𝐯| = √(x² + y²)`$

模一定是非负实数。当且仅当向量是零向量 $`𝟎 = (0, 0)`$ 时，模为零。

> **为什么是 $`√(x² + y²)`$？** 把向量 $`𝐯`$ 看作从原点 $`(0,0)`$ 到点 $`(x,y)`$ 的线段。这条线段就是直角三角形的斜边，两条直角边的长度分别是 $`|x|`$ 和 $`|y|`$。根据勾股定理，斜边长度 $`= √(x² + y²)`$。

---

### 2.1.4 单位向量

**单位向量**是模为 1 的向量。给定任意非零向量 $`𝐯`$，我们可以构造与它同方向的单位向量 $`𝐯̂`$：

$`𝐯̂ = 𝐯 / |𝐯|`$

也就是说，将原向量除以它自己的模。这个操作称为**归一化**。

> **为什么这样定义？** 设 $`𝐯̂ = 𝐯 / |𝐯|`$，那么
> $`|𝐯̂| = |𝐯| / |𝐯| = 1`$
> 方向保持不变，因为我们在用正标量除以原向量。
>
> **类比**：就像把一段绳子切成单位长度的小段。如果你有一根 5 米长的绳子，把它切成 5 等份，每份就是 1 米，方向与原来相同。

---

### 2.1.5 向量的加法与减法

两个向量的加法，就是将它们的对应分量分别相加：

$`(x₁, y₁) + (x₂, y₂) = (x₁ + x₂, y₁ + y₂)`$

减法同理：

$`(x₁, y₁) - (x₂, y₂) = (x₁ - x₂, y₁ - y₂)`$

**几何意义——为什么是"平行四边形法则"？**

想象两个人同时拉一个箱子。一个人用力 $`𝐚`$ 向东拉，另一个人用力 $`𝐛`$ 向北拉。箱子实际受到的合力就是 $`𝐚 + 𝐛`$。

- **加法——平行四边形法则**：以两个向量为邻边作平行四边形，从起点出发的对角线即为它们的和。
- **减法——三角形法则**：$`𝐚 - 𝐛`$ 等于从 $`𝐛`$ 的终点指向 $`𝐚`$ 的终点的向量。

**实用的口诀**：
- 对于位移向量：**终点减起点**。从 $`A`$ 到 $`B`$ 的向量 $`\overrightarrow{AB} = 𝐫_B - 𝐫_A`$
- 对于加法：首尾相连，从第一个起点指向最后一个终点

---

### 2.1.6 向量的数乘

向量乘以一个标量 $`c`$，相当于每个分量都乘以 $`c`$：

$`c (x, y) = (cx, cy)`$

数乘的几何效果：
- 如果 $`c > 0`$，向量沿原方向拉伸（$`c > 1`$）或压缩（$`0 < c < 1`$）
- 如果 $`c < 0`$，向量反向

数乘后的模：

$`|c𝐯| = |c| · |𝐯|`$

> **为什么模会乘以 $`|c|`$ 而不是 $`c`$？** 因为如果 $`c = -2`$，向量的长度变为原来的 2 倍，方向反转。长度是正数，所以取 $`|c| = 2`$。

---

### 2.1.7 向量相等

两个向量相等当且仅当它们的对应分量分别相等。即：

$`(x₁, y₁) = (x₂, y₂) ⟺ x₁ = x₂`$ 且 $`y₁ = y₂`$

这称为**向量相等原理**（equating like vectors）——它是解向量方程的核心工具。

> **为什么这个原理如此有用？** 一个向量方程实际上包含了两个独立的标量方程（一个对应 $`x`$ 分量，一个对应 $`y`$ 分量）。这让我们能从两个方向分别求解未知数。

---

### 2.1.8 垂直向量（正交向量）

#### 什么是垂直？

两个非零向量 $`𝐮`$ 和 $`𝐯`$ 垂直（或称正交）当且仅当它们之间的夹角是 $`90°`$。

#### 如何判断垂直？——点积

两个向量的**点积（内积，dot product）**定义为：

$`𝐮 · 𝐯 = u_x v_x + u_y v_y`$

点积的结果是一个**标量**（不是向量），因此也称为"标量积"。

**垂直的判定条件**：

$`𝐮 ⟂ 𝐯 ⟺ 𝐮 · 𝐯 = 0`$

> **为什么点积为零代表垂直？**
>
> 从几何角度理解，点积还有另一个等价的定义：
> $`𝐮 · 𝐯 = |𝐮| |𝐯| \cos θ`$
> 其中 $`θ`$ 是两个向量之间的夹角。
>
> 推导这个等价关系：利用余弦定理。设 $`𝐮`$ 和 $`𝐯`$ 的夹角为 $`θ`$，则向量 $`𝐮 - 𝐯`$ 的模满足：
> $`|𝐮 - 𝐯|² = |𝐮|² + |𝐯|² - 2|𝐮||𝐯|\cos θ`$
> 同时展开左边：
> $`|𝐮 - 𝐯|² = (u_x - v_x)² + (u_y - v_y)² = (u_x² + u_y²) + (v_x² + v_y²) - 2(u_x v_x + u_y v_y)`$
> 比较两式，得：
> $`|𝐮|² + |𝐯|² - 2|𝐮||𝐯|\cos θ = |𝐮|² + |𝐯|² - 2(u_x v_x + u_y v_y)`$
> 因此 $`u_x v_x + u_y v_y = |𝐮||𝐯|\cos θ`$。
>
> 当 $`θ = 90°`$ 时，$`\cos 90° = 0`$，所以点积为零。

#### 水平向量与垂直向量

一个特殊的例子：**水平向量**的 $`y`$ 分量为零，即 $`𝐡 = (h_x, 0)`$（$`h_x ≠ 0`$），它的方向平行于 $`x`$ 轴。**垂直向量**的 $`x`$ 分量为零，即 $`𝐯 = (0, v_y)`$（$`v_y ≠ 0`$），它的方向平行于 $`y`$ 轴。

水平向量总是垂直于垂直向量。用点积验证：

$`(h_x, 0) · (0, v_y) = h_x · 0 + 0 · v_y = 0`$

#### 通过斜率判断垂直

如果两个非零向量都不与坐标轴平行（即 $`x`$ 和 $`y`$ 分量均不为零），我们也可以用斜率来判断垂直。设向量 $`𝐮`$ 的斜率为 $`k₁ = u_y / u_x`$，向量 $`𝐯`$ 的斜率为 $`k₂ = v_y / v_x`$，那么：

$`𝐮 ⟂ 𝐯 ⟺ k₁ · k₂ = -1`$

> **推导**：由点积为零的条件：
> $`u_x v_x + u_y v_y = 0 ⟹ u_x v_x = -u_y v_y ⟹ (u_y / u_x) · (v_y / v_x) = -1`$
> 即 $`k₁ · k₂ = -1`$。

> **关于零向量的说明**：零向量 $`𝟎 = (0, 0)`$ 没有固定方向。按照惯例，在技术讨论中它被视为与所有向量既平行又垂直，但在实际解题中我们通常排除它。

---

### 📌 例题 2.1：向量基础运算

**例题 1**（向量表示、模与单位向量）

已知两点 $`A(1, 2)`$ 和 $`B(5, -1)`$。

（a）求向量 $`\overrightarrow{AB}`$ 用 $`𝐢`$-$`𝐣`$ 形式表示。
（b）求 $`|\overrightarrow{AB}|`$。
（c）求与 $`\overrightarrow{AB}`$ 同方向的单位向量。

**思路分析**：
- 从 $`A`$ 到 $`B`$ 的向量 = $`B`$ 的位置减 $`A`$ 的位置
- 模 = 各分量平方和的平方根
- 单位向量 = 原向量除以模

**解**：

（a）

$`\overrightarrow{AB} = 𝐫_B - 𝐫_A = (5𝐢 - 𝐣) - (𝐢 + 2𝐣) = (5-1)𝐢 + (-1-2)𝐣 = 4𝐢 - 3𝐣`$

（b）

$`|\overrightarrow{AB}| = √(4² + (-3)²) = √(16 + 9) = √25 = 5`$

（c）同方向的单位向量为：

$`𝐯̂ = (4𝐢 - 3𝐣) / 5 = (4/5)𝐢 - (3/5)𝐣`$

验证模长：$`√((4/5)² + (-3/5)²) = √(16/25 + 9/25) = √(25/25) = 1`$。✓

---

**例题 2**（向量加法、数乘与向量相等——解方程组）

已知 $`𝐚 = 2𝐢 + 3𝐣`$，$`𝐛 = -𝐢 + 2𝐣`$。求：

（a）$`𝐚 + 𝐛`$
（b）$`2𝐚 - 3𝐛`$
（c）实数 $`p`$ 和 $`q`$ 使得 $`p𝐚 + q𝐛 = 7𝐢 + 8𝐣`$

**思路分析**：
- 加法和数乘都是对分量分别操作
- 对于第（c）问，先展开左侧，利用向量相等原理（$`𝐢`$ 和 $`𝐣`$ 的系数分别相等）建立方程组

**解**：

（a）

$`𝐚 + 𝐛 = (2𝐢 + 3𝐣) + (-𝐢 + 2𝐣) = (2-1)𝐢 + (3+2)𝐣 = 𝐢 + 5𝐣`$

（b）

$`2𝐚 - 3𝐛 = 2(2𝐢 + 3𝐣) - 3(-𝐢 + 2𝐣) = (4𝐢 + 6𝐣) + (3𝐢 - 6𝐣) = 7𝐢 + 0𝐣 = 7𝐢`$

（c）设 $`p𝐚 + q𝐛 = 7𝐢 + 8𝐣`$，即：

$`p(2𝐢 + 3𝐣) + q(-𝐢 + 2𝐣) = 7𝐢 + 8𝐣`$

先展开括号：

$`2p𝐢 + 3p𝐣 - q𝐢 + 2q𝐣 = 7𝐢 + 8𝐣`$

合并 $`𝐢`$ 和 $`𝐣`$ 的系数：

$`(2p - q)𝐢 + (3p + 2q)𝐣 = 7𝐢 + 8𝐣`$

利用向量相等原理，$`𝐢`$ 的系数必须相等，$`𝐣`$ 的系数也必须相等：

$`\begin{cases} 2p - q = 7 & \text{（① } 𝐢 \text{ 系数相等）} \\ 3p + 2q = 8 & \text{（② } 𝐣 \text{ 系数相等）} \end{cases}`$

解这个方程组。从①式得 $`q = 2p - 7`$，代入②式：

$`3p + 2(2p - 7) = 8 ⟹ 3p + 4p - 14 = 8 ⟹ 7p = 22 ⟹ p = 22/7`$

于是 $`q = 2 × (22/7) - 7 = 44/7 - 49/7 = -5/7`$。

因此 $`p = 22/7`$，$`q = -5/7`$。✓

---

**例题 3**（位置向量、位移与垂直向量判定）

三点 $`P`$、$`Q`$、$`R`$ 的位置向量分别为 $`𝐩 = 3𝐢 + 𝐣`$，$`𝐪 = 5𝐢 - 2𝐣`$，$`𝐫 = -2𝐢 + 4𝐣`$。

（a）求 $`\overrightarrow{PQ}`$ 和 $`\overrightarrow{PR}`$。
（b）已知 $`\overrightarrow{PS} = 2\overrightarrow{PQ}`$，求 $`S`$ 的位置向量。
（c）判断 $`\overrightarrow{PQ}`$ 与 $`\overrightarrow{PR}`$ 是否垂直。

**思路分析**：
- $`\overrightarrow{PQ} = 𝐪 - 𝐩`$（终点减起点）
- $`\overrightarrow{PS} = 𝐬 - 𝐩`$，代入已知条件解出 $`𝐬`$
- 垂直判定：计算点积，若为零则垂直

**解**：

（a）

$`\overrightarrow{PQ} = 𝐪 - 𝐩 = (5𝐢 - 2𝐣) - (3𝐢 + 𝐣) = 2𝐢 - 3𝐣`$

$`\overrightarrow{PR} = 𝐫 - 𝐩 = (-2𝐢 + 4𝐣) - (3𝐢 + 𝐣) = -5𝐢 + 3𝐣`$

（b）设 $`S`$ 的位置向量为 $`𝐬`$。由 $`\overrightarrow{PS} = 𝐬 - 𝐩`$ 且 $`\overrightarrow{PS} = 2\overrightarrow{PQ}`$，得：

$`𝐬 - 𝐩 = 2(2𝐢 - 3𝐣) = 4𝐢 - 6𝐣`$

所以：

$`𝐬 = 𝐩 + (4𝐢 - 6𝐣) = (3𝐢 + 𝐣) + (4𝐢 - 6𝐣) = 7𝐢 - 5𝐣`$

因此 $`S`$ 的坐标为 $`(7, -5)`$。

（c）计算点积：

$`\overrightarrow{PQ} · \overrightarrow{PR} = (2)(-5) + (-3)(3) = -10 - 9 = -19 ≠ 0`$

因为点积不为零，所以 $`\overrightarrow{PQ}`$ 与 $`\overrightarrow{PR}`$ **不垂直**。

> **如果用斜率法验证**：
> $`k_{PQ} = -3/2 = -1.5`$
> $`k_{PR} = 3/(-5) = -0.6`$
> $`k_{PQ} · k_{PR} = (-1.5)(-0.6) = 0.9 ≠ -1`$
> 同样得出不垂直。✓

---

## 2.2 向量的实际运用

### 2.2.1 向量几何——深度解析

向量是解决平面几何问题的强大工具。通过将几何关系转化为向量方程，我们可以用代数方法精确求解，避免画图的不精确性。

#### 核心思想

**向量几何的本质**：用向量运算（加减、数乘）来表示几何关系。

| 几何关系 | 向量表达 |
|:---|:---|
| 从 $`A`$ 到 $`B`$ 的线段 | $`\overrightarrow{AB} = 𝐫_B - 𝐫_A`$ |
| $`A`$ 和 $`B`$ 的中点 $`M`$ | $`𝐫_M = (𝐫_A + 𝐫_B) / 2`$ |
| $`P`$ 分 $`AB`$ 为 $`m:n`$ | $`𝐫_P = (n𝐫_A + m𝐫_B) / (m+n)`$ |
| $`AB ∥ CD`$ | $`\overrightarrow{AB} = k · \overrightarrow{CD}`$ |
| $`AB ⟂ CD`$ | $`\overrightarrow{AB} · \overrightarrow{CD} = 0`$ |
| $`A,B,C`$ 共线 | $`\overrightarrow{AB} = k · \overrightarrow{BC}`$（存在 $`k`$） |

#### 中点公式——为什么是这样？

设 $`M`$ 是 $`AB`$ 的中点。从 $`A`$ 到 $`M`$ 的位移是 $`\overrightarrow{AB}`$ 的一半：

$`𝐫_M = 𝐫_A + (1/2)\overrightarrow{AB} = 𝐫_A + (1/2)(𝐫_B - 𝐫_A) = (2𝐫_A + 𝐫_B - 𝐫_A)/2 = (𝐫_A + 𝐫_B)/2`$

#### 分点公式——详细推导

设点 $`P`$ 分线段 $`AB`$ 为 $`AP:PB = m:n`$（即 $`P`$ 靠近 $`A`$ 的方向上，$`AP`$ 占 $`m`$ 份，$`PB`$ 占 $`n`$ 份）。

这意味着 $`P`$ 位于从 $`A`$ 到 $`B`$ 的 $`m/(m+n)`$ 处（从 $`A`$ 算起）。所以：

$`\begin{aligned} 𝐫_P &= 𝐫_A + \frac{m}{m+n}\overrightarrow{AB} \\ &= 𝐫_A + \frac{m}{m+n}(𝐫_B - 𝐫_A) \\ &= \frac{(m+n)𝐫_A + m𝐫_B - m𝐫_A}{m+n} \\ &= \frac{n𝐫_A + m𝐫_B}{m+n} \end{aligned}`$

> **记忆技巧**：分点公式中，$`A`$ 的系数是 $`n`$（对面那段的比例），$`B`$ 的系数是 $`m`$（对面那段的比例）。交叉相乘！
>
> 例如 $`AP:PB = 2:3`$（$`m=2, n=3`$），则：
> $`𝐫_P = (3𝐫_A + 2𝐫_B) / 5`$
> $`A`$ 的系数是 $`3`$（对面的 $`PB`$ 是 $`3`$ 份），$`B`$ 的系数是 $`2`$（对面的 $`AP`$ 是 $`2`$ 份）。

#### 平行向量

两个非零向量 $`𝐚`$ 和 $`𝐛`$ 平行（即方向相同或相反）当且仅当存在一个实数 $`k`$ 使得：

$`𝐚 = k𝐛`$

即一个向量是另一个向量的标量倍。

> **如何判断平行？** 检查两个向量的对应分量是否成比例：
> 若 $`𝐚 = (a_x, a_y)`$，$`𝐛 = (b_x, b_y)`$，且 $`a_x / b_x = a_y / b_y`$（分母不为零），则它们平行。
> 注意这个比例可以是负数（方向相反）。

#### 三点共线的判定

三点 $`A`$、$`B`$、$`C`$ 共线当且仅当 $`\overrightarrow{AB}`$ 与 $`\overrightarrow{AC}`$ 平行（或 $`\overrightarrow{AB}`$ 与 $`\overrightarrow{BC}`$ 平行）。

**为什么？** 如果三点共线，那么从 $`A`$ 到 $`B`$ 和从 $`A`$ 到 $`C`$ 的位移方向相同或相反，即存在标量 $`k`$ 使 $`\overrightarrow{AC} = k · \overrightarrow{AB}`$。

---

### 📌 例题 2.2：向量几何应用

**例题 1**（平行四边形与中点——配图说明）

在平行四边形 $`ABCD`$ 中，$`A`$、$`B`$、$`C`$ 的位置向量分别为 $`𝐚 = 𝐢 + 2𝐣`$，$`𝐛 = 4𝐢 + 3𝐣`$，$`𝐜 = 6𝐢 + 𝐣`$。求：

（a）$`D`$ 的位置向量
（b）对角线 $`AC`$ 与 $`BD`$ 的交点 $`M`$ 的位置向量

**思路分析**：

> 先理解平行四边形的结构。顶点按 $`A → B → C → D → A`$ 的顺序排列。
>
> **平行四边形的关键性质**：对边平行且相等。
> - $`AB ∥ DC`$ 且 $`AB = DC`$
> - $`AD ∥ BC`$ 且 $`AD = BC`$
>
> 这意味着 $`\overrightarrow{AD} = \overrightarrow{BC}`$ 或 $`\overrightarrow{AB} = \overrightarrow{DC}`$。
>
> 另外，平行四边形的对角线**互相平分**，即 $`AC`$ 的中点 = $`BD`$ 的中点。

**解**：

（a）在平行四边形 $`ABCD`$ 中，对边 $`AD`$ 和 $`BC`$ 平行且相等，所以 $`\overrightarrow{AD} = \overrightarrow{BC}`$。

先求 $`\overrightarrow{BC} = 𝐜 - 𝐛`$：

$`\overrightarrow{BC} = (6𝐢 + 𝐣) - (4𝐢 + 3𝐣) = 2𝐢 - 2𝐣`$

因为 $`\overrightarrow{AD} = \overrightarrow{BC} = 2𝐢 - 2𝐣`$，而 $`\overrightarrow{AD} = 𝐝 - 𝐚`$，所以：

$`𝐝 = 𝐚 + \overrightarrow{AD} = (𝐢 + 2𝐣) + (2𝐢 - 2𝐣) = 3𝐢`$

所以 $`D`$ 的坐标为 $`(3, 0)`$。

**验证**：也可用 $`\overrightarrow{AB} = \overrightarrow{DC}`$ 来求。
$`\overrightarrow{AB} = 𝐛 - 𝐚 = (4𝐢 + 3𝐣) - (𝐢 + 2𝐣) = 3𝐢 + 𝐣`$
$`\overrightarrow{DC} = 𝐜 - 𝐝`$，所以 $`𝐜 - 𝐝 = 3𝐢 + 𝐣`$，解得 $`𝐝 = 𝐜 - (3𝐢 + 𝐣) = (6𝐢 + 𝐣) - (3𝐢 + 𝐣) = 3𝐢`$，一致。✓

（b）平行四边形的对角线互相平分。因此 $`M`$ 既是 $`AC`$ 的中点，也是 $`BD`$ 的中点。

用 $`AC`$ 的中点计算：

$`𝐦 = (𝐚 + 𝐜) / 2 = ((𝐢 + 2𝐣) + (6𝐢 + 𝐣)) / 2 = (7𝐢 + 3𝐣) / 2 = 3.5𝐢 + 1.5𝐣`$

验证用 $`BD`$ 的中点：$`(𝐛 + 𝐝) / 2 = ((4𝐢 + 3𝐣) + 3𝐢) / 2 = (7𝐢 + 3𝐣) / 2`$，一致。✓

---

**例题 2**（共线判定与比值——如何证明三点共线）

三点 $`A`$、$`B`$、$`C`$ 的位置向量分别为 $`𝐚 = 2𝐢 + 𝐣`$，$`𝐛 = 5𝐢 + 4𝐣`$，$`𝐜 = 8𝐢 + 7𝐣`$。

（a）证明 $`A`$、$`B`$、$`C`$ 三点共线。
（b）求 $`AB:BC`$ 的比值。

**思路分析**：

> 证明三点共线的标准方法：
> 1. 计算 $`\overrightarrow{AB}`$ 和 $`\overrightarrow{BC}`$（或 $`\overrightarrow{AB}`$ 和 $`\overrightarrow{AC}`$）
> 2. 判断它们是否平行（即是否存在标量 $`k`$ 使得一个等于另一个乘以 $`k`$）
> 3. 如果平行且有一个公共点（$`B`$ 是 $`\overrightarrow{AB}`$ 和 $`\overrightarrow{BC}`$ 的公共点），则三点共线

**解**：

（a）计算向量：

$`\overrightarrow{AB} = 𝐛 - 𝐚 = (5𝐢 + 4𝐣) - (2𝐢 + 𝐣) = 3𝐢 + 3𝐣`$

$`\overrightarrow{BC} = 𝐜 - 𝐛 = (8𝐢 + 7𝐣) - (5𝐢 + 4𝐣) = 3𝐢 + 3𝐣`$

观察发现 $`\overrightarrow{BC} = \overrightarrow{AB}`$，即 $`\overrightarrow{BC} = 1 · \overrightarrow{AB}`$。存在标量 $`k = 1`$ 使得 $`\overrightarrow{BC} = k\overrightarrow{AB}`$，所以 $`\overrightarrow{AB} ∥ \overrightarrow{BC}`$。又因为这两个向量都通过点 $`B`$，故 $`A`$、$`B`$、$`C`$ 共线。

> **注意**：也可以验证 $`\overrightarrow{AC}`$ 与 $`\overrightarrow{AB}`$ 的关系。
> $`\overrightarrow{AC} = 𝐜 - 𝐚 = (8𝐢 + 7𝐣) - (2𝐢 + 𝐣) = 6𝐢 + 6𝐣 = 2(3𝐢 + 3𝐣) = 2\overrightarrow{AB}`$
> 同样得出共线结论。

（b）因为 $`\overrightarrow{BC} = \overrightarrow{AB}`$，且方向相同，所以 $`\overrightarrow{AB}`$ 和 $`\overrightarrow{BC}`$ 的长度相等。即 $`AB = BC`$，因此 $`AB:BC = 1:1`$。

换句话说，$`B`$ 是线段 $`AC`$ 的中点。

---

**例题 3**（分点公式 + 垂直验证——综合应用）

在 $`△OAB`$ 中，$`C`$ 在 $`OA`$ 上且 $`OC:CA = 2:1`$，$`D`$ 在 $`AB`$ 上且 $`AD:DB = 3:1`$。设 $`\overrightarrow{OA} = 𝐚`$，$`\overrightarrow{OB} = 𝐛`$。

（a）用 $`𝐚`$ 和 $`𝐛`$ 表示 $`\overrightarrow{OD}`$。
（b）用 $`𝐚`$ 和 $`𝐛`$ 表示 $`\overrightarrow{CD}`$。
（c）已知 $`𝐚 = 3𝐢 + 2𝐣`$，$`𝐛 = 𝐢 + 6𝐣`$，判断 $`\overrightarrow{OC}`$ 与 $`\overrightarrow{OD}`$ 是否垂直。

**思路分析**：

> 这是一个"用基底表示向量"的典型问题。$`𝐚`$ 和 $`𝐛`$ 是基底，所有其他向量都要用它们表示。
>
> 关键步骤：
> 1. 确定分点 $`C`$ 在 $`OA`$ 上的位置：$`OC:CA = 2:1`$ 意味着 $`OC = (2/3)OA`$
> 2. 确定分点 $`D`$ 在 $`AB`$ 上的位置：$`AD:DB = 3:1`$ 意味着 $`AD = (3/4)AB`$
> 3. 用分点公式或直接加减法表示向量
> 4. 垂直判定：代入具体数值后计算点积

**解**：

（a）先表示 $`D`$ 的位置。

方法一（分点公式）：$`D`$ 分 $`AB`$ 为 $`AD:DB = 3:1`$，即从 $`A`$ 到 $`B`$ 的方向上，$`D`$ 位于 $`3/4`$ 处。

由分点公式（$`m=3, n=1`$）：

$`𝐫_D = (n𝐫_A + m𝐫_B) / (m+n) = (1 · 𝐚 + 3 · 𝐛) / (3+1) = (𝐚 + 3𝐛) / 4`$

方法二（直接法）：从 $`A`$ 出发走到 $`B`$ 的 $`3/4`$ 处。

$`\overrightarrow{OD} = \overrightarrow{OA} + (3/4)\overrightarrow{AB}`$

而 $`\overrightarrow{AB} = \overrightarrow{OB} - \overrightarrow{OA} = 𝐛 - 𝐚`$，所以：

$`\overrightarrow{OD} = 𝐚 + (3/4)(𝐛 - 𝐚) = 𝐚 + (3/4)𝐛 - (3/4)𝐚 = (1/4)𝐚 + (3/4)𝐛`$

注意 $`(𝐚 + 3𝐛) / 4 = (1/4)𝐚 + (3/4)𝐛`$，两种方法结果一致。✓

（b）$`C`$ 在 $`OA`$ 上且 $`OC:CA = 2:1`$，所以 $`C`$ 分 $`\overrightarrow{OA}`$ 为 $`2:1`$（从 $`O`$ 算起）。因此：

$`\overrightarrow{OC} = (2/3)\overrightarrow{OA} = (2/3)𝐚`$

于是：

$`\overrightarrow{CD} = \overrightarrow{OD} - \overrightarrow{OC} = ((1/4)𝐚 + (3/4)𝐛) - (2/3)𝐚`$

通分计算 $`𝐚`$ 的系数：$`1/4 - 2/3 = 3/12 - 8/12 = -5/12`$。

所以：

$`\overrightarrow{CD} = -(5/12)𝐚 + (3/4)𝐛`$

（c）代入 $`𝐚 = 3𝐢 + 2𝐣`$，$`𝐛 = 𝐢 + 6𝐣`$：

先求 $`\overrightarrow{OC}`$：

$`\overrightarrow{OC} = (2/3)(3𝐢 + 2𝐣) = 2𝐢 + (4/3)𝐣`$

再求 $`\overrightarrow{OD}`$：

$`\begin{aligned} \overrightarrow{OD} &= (1/4)(3𝐢 + 2𝐣) + (3/4)(𝐢 + 6𝐣) \\ &= ((3/4)+(3/4))𝐢 + ((1/2)+(9/2))𝐣 \\ &= (6/4)𝐢 + (10/2)𝐣 \\ &= (3/2)𝐢 + 5𝐣 \end{aligned}`$

计算点积：

$`\overrightarrow{OC} · \overrightarrow{OD} = (2)(3/2) + (4/3)(5) = 3 + 20/3 = 9/3 + 20/3 = 29/3 ≠ 0`$

点积不为零，所以 $`\overrightarrow{OC}`$ 与 $`\overrightarrow{OD}`$ **不垂直**。

---

### 2.2.2 速度的合成与分解

速度是一个向量。当一个物体同时参与两个或更多运动时，它的合速度是这些速度的向量和。

#### 相对速度公式

设物体 $`A`$ 相对于参考系 $`C`$ 的速度为 $`𝐯_{A/C}`$，物体 $`A`$ 相对于物体 $`B`$ 的速度为 $`𝐯_{A/B}`$，物体 $`B`$ 相对于参考系 $`C`$ 的速度为 $`𝐯_{B/C}`$，则：

$`𝐯_{A/C} = 𝐯_{A/B} + 𝐯_{B/C}`$

> **直观理解**：
> - 你在火车上行走，速度为 $`𝐯_{A/B}`$（你相对于火车）
> - 火车相对于地面行驶，速度为 $`𝐯_{B/C}`$（火车相对于地面）
> - 你相对于地面的速度 $`𝐯_{A/C}`$ 就是两者之和
>
> **另一个例子**：一艘船在河中行驶。
> - 船在静水中的速度 = $`𝐯_{B/W}`$（船相对于水）
> - 水流速度 = $`𝐯_{W/G}`$（水相对于地面）
> - 船相对于地面的实际速度 = $`𝐯_{B/W} + 𝐯_{W/G}`$

#### 速度的分解

与合成相反，将一个速度向量分解为两个互相垂直的分量（通常沿水平方向和垂直方向）称为**速度的分解**。

设速度 $`𝐯`$ 的大小为 $`v`$（速率），与水平方向的夹角为 $`θ`$，则：

$`𝐯 = (v\cos θ)𝐢 + (v\sin θ)𝐣`$

其中 $`v_x = v\cos θ`$ 是水平分量，$`v_y = v\sin θ`$ 是垂直分量。

> **推导**：将速度向量投影到 $`x`$ 轴和 $`y`$ 轴上。在直角三角形中，邻边 = 斜边 × cos θ，对边 = 斜边 × sin θ。

---

#### 相撞问题

两个运动物体相撞的条件是：在**同一时刻**，它们的位置向量**相等**。即：

$`𝐫₁(t) = 𝐫₂(t)`$

对于匀速直线运动，位置向量满足：

$`𝐫(t) = 𝐫₀ + 𝐯t`$

其中 $`𝐫₀`$ 是初始位置向量，$`𝐯`$ 是速度向量。因此相撞条件可以展开为：

$`𝐫₀₁ + 𝐯₁ t = 𝐫₀₂ + 𝐯₂ t`$

这是一个关于时间 $`t`$ 的向量方程。它等价于两个分量方程分别相等，从而可以解出 $`t`$ 并验证是否一致。

**解题步骤**：
1. 写出两物体的位置向量 $`𝐫₁(t)`$ 和 $`𝐫₂(t)`$。
2. 令 $`𝐫₁(t) = 𝐫₂(t)`$，得到两个分量方程。
3. 分别解出 $`t`$。如果两个 $`t`$ 值相等且 $`≥ 0`$，则相撞；否则不相撞。

> **注意**："相撞"要求同时在同一位置。如果两船在不同时间到达同一点，那只叫"相遇"而非"相撞"。

---

### 📌 例题 2.3：速度合成与相撞问题

**例题 1**（速度合成——船与水流）

一艘船在静水中的速度是 $`6`$ m/s 向东。水流以 $`4`$ m/s 的速度向北流动。求船相对于地面的速度的大小和方向。

**思路分析**：
- 船相对于水的速度 $`𝐯_{B/W}`$ = 向东 $`6`$ m/s
- 水相对于地面的速度 $`𝐯_{W/G}`$ = 向北 $`4`$ m/s
- 船相对于地面的速度 $`𝐯_{B/G} = 𝐯_{B/W} + 𝐯_{W/G}`$

**解**：

设正东方向为 $`+x`$ 轴，正北方向为 $`+y`$ 轴。

船相对于水的速度：$`𝐯_{B/W} = 6𝐢`$
水流相对于地面的速度：$`𝐯_{W/G} = 4𝐣`$

由速度合成公式：

$`𝐯_{B/G} = 𝐯_{B/W} + 𝐯_{W/G} = 6𝐢 + 4𝐣`$

合速度的大小（速率）：

$`|𝐯_{B/G}| = √(6² + 4²) = √(36 + 16) = √52 = 2√13 ≈ 7.21`$ m/s

合速度的方向：设 $`θ`$ 为与正东方向的夹角（逆时针为正）。

$`\tan θ = 4/6 = 2/3 ⟹ θ = \arctan(2/3) ≈ 33.69°`$

因此船相对于地面的速度大小为 $`2√13`$ m/s，方向为北偏东 $`33.69°`$（即从正东方向逆时针旋转 $`33.69°`$）。

> **生活实例**：这就是为什么船过河时，如果直接朝对岸开，会被水流冲偏。要想直接到达正对岸，船头必须朝上游方向倾斜。

---

**例题 2**（速度分解——斜抛的初速度）

一个球以初速度 $`20`$ m/s、与水平方向成 $`30°`$ 角斜向上抛出。

（a）求初速度的水平分量和垂直分量。
（b）写出初速度的向量形式。

**思路分析**：
- 速度的大小是 $`20`$，方向是 $`30°`$ 仰角
- 水平分量 $`v_x = v\cos θ`$，垂直分量 $`v_y = v\sin θ`$

**解**：

（a）设水平向右为 $`+x`$ 轴，竖直向上为 $`+y`$ 轴。

水平分量：

$`v_x = v\cos θ = 20 × \cos 30° = 20 × (√3 / 2) = 10√3 ≈ 17.32`$ m/s

垂直分量：

$`v_y = v\sin θ = 20 × \sin 30° = 20 × (1/2) = 10`$ m/s

（b）初速度的向量形式：

$`𝐯₀ = 10√3 𝐢 + 10 𝐣`$ m/s

> **物理意义**：
> - 如果没有空气阻力，水平分量 $`v_x`$ 在整个运动过程中保持不变（因为水平方向不受力）
> - 而垂直分量 $`v_y`$ 受重力影响以 $`-g`$ 的加速度变化（$`g ≈ 9.8`$ m/s²）
> - 所以任意时刻的速度为 $`𝐯(t) = 10√3 𝐢 + (10 - gt) 𝐣`$
> - 这为第 10 章的运动学问题奠定了基础

---

**例题 3**（相撞问题——两船是否会相撞）

船 $`P`$ 从点 $`(0, 0)`$ 出发，以速度 $`𝐯_P = (3𝐢 + 4𝐣)`$ km/h 航行。船 $`Q`$ 从点 $`(10, 5)`$ km 出发，以速度 $`𝐯_Q = (-2𝐢 + 𝐣)`$ km/h 航行。两船同时出发，判断它们是否会相撞。

**思路分析**：
1. 写出两船的位置向量（都是 $`𝐫₀ + 𝐯t`$ 的形式）
2. 令 $`𝐫_P(t) = 𝐫_Q(t)`$
3. 得到两个分量方程，分别解 $`t`$
4. 如果 $`t`$ 值一致且 $`≥ 0`$，相撞；否则不相撞

**解**：

设 $`t`$ 为出发后的时间（小时）。

$`P`$ 的位置向量（从原点出发）：

$`𝐫_P(t) = (0, 0) + (3, 4) t = (3t, 4t)`$

$`Q`$ 的位置向量（从 $`(10,5)`$ 出发）：

$`𝐫_Q(t) = (10, 5) + (-2, 1) t = (10 - 2t, 5 + t)`$

如果两船相撞，则存在某个 $`t ≥ 0`$ 使得 $`𝐫_P(t) = 𝐫_Q(t)`$，即：

$`(3t, 4t) = (10 - 2t, 5 + t)`$

这给出两个分量方程：

$`\begin{cases} x\text{分量：} & 3t = 10 - 2t ⟹ 5t = 10 ⟹ t = 2 \\ y\text{分量：} & 4t = 5 + t ⟹ 3t = 5 ⟹ t = 5/3 \end{cases}`$

两个 $`t`$ 值不相等（$`2 ≠ 5/3`$），因此不存在同时满足两个分量方程的时刻。两船不会相撞。

> **为什么不相撞？** 即使 $`x`$ 坐标在 $`t=2`$ 时相等，$`y`$ 坐标在 $`t=2`$ 时分别为 $`4×2 = 8`$ 和 $`5+2=7`$，不相等。所以两船永远不会在同一时刻到达同一点。

---

## 2.3 变化率入门（微积分铺垫）

### 2.3.1 为何研究变化率？

在物理世界中，很少有事物是静止的。一辆行驶的汽车，它的位置在变化；一个充气的气球，它的体积在变化；一个加热的金属棒，它的温度在变化。**变化率**就是描述"一个量随另一个量变化得有多快"的数学工具。

在 2.1 和 2.2 节中，我们用向量描述了位置、速度和加速度。现在我们要问一个更深入的问题：**如何精确地定义"瞬时"变化率？**

---

### 2.3.2 从平均变化率到瞬时变化率

让我们从一个具体的运动学例子开始。

一个质点沿直线运动，它的位移 $`s`$（单位：米）与时间 $`t`$（单位：秒）的关系为：

$`s(t) = t²`$

我们想知道 $`t = 1`$ 秒这一**瞬间**的速度。

#### 第一步：平均速度

如果取一个时间区间 $`[1, 1 + Δt]`$，质点在区间内的平均速度是：

$`\text{平均速度} = (s(1 + Δt) - s(1)) / Δt`$

代入 $`s(t) = t²`$：

$`((1 + Δt)² - 1²) / Δt = (1 + 2Δt + (Δt)² - 1) / Δt = (2Δt + (Δt)²) / Δt = 2 + Δt`$

#### 第二步：让 $`Δt`$ 越来越小

我们让 $`Δt`$ 逐渐趋近于 0，观察平均速度的变化：

| $`Δt`$（秒） | 平均速度（m/s） |
|:---:|:---:|
| 0.1 | $`2 + 0.1 = 2.1`$ |
| 0.01 | $`2 + 0.01 = 2.01`$ |
| 0.001 | $`2 + 0.001 = 2.001`$ |
| 0.0001 | $`2 + 0.0001 = 2.0001`$ |
| $`→ 0`$ | $`→ 2`$ |

随着 $`Δt`$ 越来越接近 0，平均速度越来越接近 **2**。

#### 第三步：极限

当 $`Δt`$ 趋近于 0 时，平均速度 $`2 + Δt`$ 趋近于 2。我们记：

$`v(1) = \lim_{Δt → 0} (s(1 + Δt) - s(1)) / Δt = \lim_{Δt → 0} (2 + Δt) = 2`$

这个极限值就是质点在 $`t = 1`$ 时的**瞬时速度**。

> **重要理解**：我们从不令 $`Δt = 0`$（那样会得到 $`0/0`$，没有意义）。我们让 $`Δt`$ 无限趋近于 0，观察比值趋近于哪个固定值。这个"趋近的目标"就是导数。
>
> 用极限的语言说：**当 $`Δt`$ 趋近于 0 时，平均速度的极限就是瞬时速度。**

---

### 2.3.3 导数的一般定义

一般地，对于函数 $`y = f(x)`$，它在 $`x = a`$ 处的**导数**（即瞬时变化率）定义为：

$`f'(a) = \lim_{h → 0} (f(a + h) - f(a)) / h`$

其中 $`h`$ 就是前面例子中的 $`Δx`$ 或 $`Δt`$。

如果这个极限存在，我们就说 $`f`$ 在 $`x = a`$ 处**可导**。

**导数的记号**：
- 莱布尼茨记号：$`dy/dx`$ 或 $`(d/dx)f(x)`$
- 拉格朗日记号：$`f'(x)`$
- 牛顿记号（常用于物理）：$`ẏ`$

> 在考纲 14.1 中，只要求对极限有直观理解，不要求从第一原理求导。但我们这里仍会展示几个基本推导，帮助你建立直觉。

---

### 2.3.4 用定义求导——基础推导

让我们用极限定义推导几个基本函数的导数。

#### 推导 1：$`f(x) = x²`$

$`\begin{aligned} f'(x) &= \lim_{h → 0} ((x + h)² - x²) / h \\ &= \lim_{h → 0} (x² + 2xh + h² - x²) / h \\ &= \lim_{h → 0} (2xh + h²) / h \\ &= \lim_{h → 0} (2x + h) \\ &= 2x \end{aligned}`$

因此 $`(d/dx)(x²) = 2x`$。

**几何意义**：函数 $`y = x²`$ 在任意点 $`x`$ 处的切线斜率为 $`2x`$。在 $`x = 1`$ 处斜率为 $`2`$，在 $`x = 3`$ 处斜率为 $`6`$。

#### 推导 2：$`f(x) = x³`$

$`f'(x) = \lim_{h → 0} ((x + h)³ - x³) / h`$

展开 $`(x + h)³ = x³ + 3x²h + 3xh² + h³`$：

$`\begin{aligned} f'(x) &= \lim_{h → 0} (x³ + 3x²h + 3xh² + h³ - x³) / h \\ &= \lim_{h → 0} (3x²h + 3xh² + h³) / h \\ &= \lim_{h → 0} (3x² + 3xh + h²) \\ &= 3x² \end{aligned}`$

因此 $`(d/dx)(x³) = 3x²`$。

#### 推导 3：$`f(x) = 1/x`$（$`x ≠ 0`$）

$`f'(x) = \lim_{h → 0} (1/(x + h) - 1/x) / h`$

先通分分子：

$`1/(x + h) - 1/x = (x - (x + h)) / (x(x + h)) = -h / (x(x + h))`$

所以：

$`\begin{aligned} f'(x) &= \lim_{h → 0} (-h / (x(x + h))) · (1/h) \\ &= \lim_{h → 0} (-1 / (x(x + h))) \\ &= -1 / x² \end{aligned}`$

因此 $`(d/dx)(1/x) = -1/x²`$。

---

### 2.3.5 幂法则

从上面的推导中，我们可以观察出一个模式：

| $`f(x)`$ | $`f'(x)`$ |
|:---:|:---:|
| $`x²`$ | $`2x`$ |
| $`x³`$ | $`3x²`$ |
| $`x¹`$ | $`1`$（即 $`1 · x⁰`$） |
| $`1/x = x⁻¹`$ | $`-1/x² = -x⁻²`$ |

这个模式就是**幂法则**：对任意实数 $`n`$，

$`\boxed{(d/dx)(xⁿ) = n xⁿ⁻¹}`$

> **幂法则的完整推导**（利用二项式定理，仅对正整数 $`n`$）：
>
> 考虑 $`f(x) = xⁿ`$，其中 $`n`$ 为正整数。利用二项式定理展开 $`(x + h)ⁿ`$：
> $`(x + h)ⁿ = xⁿ + n xⁿ⁻¹h + C(n,2)xⁿ⁻²h² + C(n,3)xⁿ⁻³h³ + ... + hⁿ`$
> 因此：
> $`\begin{aligned} f'(x) &= \lim_{h → 0} (xⁿ + n xⁿ⁻¹h + C(n,2)xⁿ⁻²h² + ... + hⁿ - xⁿ) / h \\ &= \lim_{h → 0} (n xⁿ⁻¹ + C(n,2)xⁿ⁻²h + C(n,3)xⁿ⁻³h² + ... + hⁿ⁻¹) \\ &= n xⁿ⁻¹ \end{aligned}`$
> 因为所有含 $`h`$ 的项在 $`h → 0`$ 时都趋于 0。

---

### 2.3.6 变化率在向量中的应用：运动学

现在回到向量的语境中。如果质点的位置向量 $`𝐫(t)`$ 随时间变化，那么它的速度向量和加速度向量就是位置向量对时间的变化率。

设 $`𝐫(t) = x(t)𝐢 + y(t)𝐣`$，则：

$`𝐯(t) = d𝐫/dt = (dx/dt)𝐢 + (dy/dt)𝐣`$

$`𝐚(t) = d𝐯/dt = d²𝐫/dt² = (d²x/dt²)𝐢 + (d²y/dt²)𝐣`$

也就是说，对向量函数求导，就是对它的各个分量分别求导。

---

### 2.3.7 从加速度到速度和位置（积分铺垫）

在物理中，加速度 $`𝐚(t)`$ 是速度 $`𝐯(t)`$ 的变化率。如果我们知道加速度并想恢复出速度，需要做微分的逆运算——这称为**积分**（将在第 7 章详细学习）。

基本关系是：

- 速度 $`𝐯(t) = ∫ 𝐚(t) dt + 𝐯₀`$（其中 $`𝐯₀`$ 是初始速度）
- 位置 $`𝐫(t) = ∫ 𝐯(t) dt + 𝐫₀`$（其中 $`𝐫₀`$ 是初始位置）

或者用定积分的形式（更适合已知确定时间段的问题）：

$`𝐯(t) = 𝐯₀ + ∫₀ᵗ 𝐚(u) du, \quad 𝐫(t) = 𝐫₀ + ∫₀ᵗ 𝐯(u) du`$

这里先建立直观理解：**微分**是求变化率（位置 → 速度 → 加速度），**积分**是求累积量（加速度 → 速度 → 位置），两者互为逆运算。

---

### 📌 例题 2.4：变化率入门

**例题 1**（用极限定义求瞬时速度）

一个质点的位移（单位：米）与时间（单位：秒）的关系为 $`s(t) = 3t² - 2t + 1`$。

（a）求从 $`t = 2`$ 到 $`t = 2 + h`$ 的平均速度。
（b）利用极限求 $`t = 2`$ 时的瞬时速度。

**思路分析**：
- 平均速度 = $`(s(2+h) - s(2)) / h`$
- 瞬时速度 = $`\lim_{h → 0}`$ 平均速度

**解**：

（a）

$`\begin{aligned} \text{平均速度} &= (s(2 + h) - s(2)) / h \\ &= ([3(2+h)² - 2(2+h) + 1] - [3(4) - 4 + 1]) / h \\ &= ([3(4 + 4h + h²) - 4 - 2h + 1] - [12 - 4 + 1]) / h \\ &= ([12 + 12h + 3h² - 4 - 2h + 1] - 9) / h \\ &= ([9 + 10h + 3h²] - 9) / h \\ &= (10h + 3h²) / h = 10 + 3h \end{aligned}`$

（b）瞬时速度是 $`h → 0`$ 时平均速度的极限：

$`v(2) = \lim_{h → 0} (10 + 3h) = 10`$ m/s

因此质点在 $`t = 2`$ 秒时的瞬时速度为 $`10`$ m/s。

> **验证**：用幂法则直接求导：$`s'(t) = 6t - 2`$，$`s'(2) = 12 - 2 = 10`$，一致。✓

---

**例题 2**（用极限定义求一般导数 + 幂法则验证）

用导数的极限定义求 $`f(x) = 4x - x²`$ 的导数 $`f'(x)`$。

**解**：

$`\begin{aligned} f'(x) &= \lim_{h → 0} (f(x + h) - f(x)) / h \\ &= \lim_{h → 0} ([4(x + h) - (x + h)²] - [4x - x²]) / h \\ &= \lim_{h → 0} (4x + 4h - (x² + 2xh + h²) - 4x + x²) / h \\ &= \lim_{h → 0} (4h - 2xh - h²) / h \\ &= \lim_{h → 0} (4 - 2x - h) \\ &= 4 - 2x \end{aligned}`$

因此 $`f'(x) = 4 - 2x`$。

**验证**：使用幂法则对 $`f(x) = 4x - x²`$ 逐项求导：
- $`(d/dx)(4x) = 4 · 1 · x⁰ = 4`$
- $`(d/dx)(-x²) = -2x¹ = -2x`$
- 相加得 $`f'(x) = 4 - 2x`$，与极限结果一致。✓

---

**例题 3**（向量的变化率 + 积分逆运算）

一个质点在平面内运动，它的位置向量为：

$`𝐫(t) = (t³ - 3t)𝐢 + (t² - 2t)𝐣`$

其中 $`t`$ 以秒为单位，位置以米为单位。

（a）求速度向量 $`𝐯(t)`$。
（b）求加速度向量 $`𝐚(t)`$。
（c）求 $`t = 2`$ 秒时的速度和加速度向量。
（d）已知该质点的加速度恒为 $`𝐚(t) = 6t𝐢 + 2𝐣`$，初速度 $`𝐯₀ = -3𝐢 - 2𝐣`$，初始位置 $`𝐫₀ = 𝟎`$。用积分求 $`𝐯(t)`$ 和 $`𝐫(t)`$，验证与（a）（b）一致。

**思路分析**：
- 速度 = 位置的导数（分量分别求导）
- 加速度 = 速度的导数（分量分别求导）
- 积分是微分的逆运算：已知加速度，积分一次得速度（加常数），再积分一次得位置（加常数）
- 常数由初始条件确定

**解**：

（a）速度是位置对时间的导数，对各分量分别求导：

$`𝐯(t) = d𝐫/dt = (d/dt)(t³ - 3t) 𝐢 + (d/dt)(t² - 2t) 𝐣`$

使用幂法则：
- $`(d/dt)(t³) = 3t²`$
- $`(d/dt)(-3t) = -3`$
- $`(d/dt)(t²) = 2t`$
- $`(d/dt)(-2t) = -2`$

因此：

$`𝐯(t) = (3t² - 3)𝐢 + (2t - 2)𝐣`$

（b）加速度是速度对时间的导数：

$`𝐚(t) = d𝐯/dt = (d/dt)(3t² - 3) 𝐢 + (d/dt)(2t - 2) 𝐣`$

$`𝐚(t) = (6t)𝐢 + 2𝐣`$

（c）代入 $`t = 2`$：

$`𝐯(2) = (3 × 4 - 3)𝐢 + (4 - 2)𝐣 = 9𝐢 + 2𝐣`$ m/s

速度的大小：$`|𝐯(2)| = √(9² + 2²) = √(81 + 4) = √85 ≈ 9.22`$ m/s

$`𝐚(2) = (6 × 2)𝐢 + 2𝐣 = 12𝐢 + 2𝐣`$ m/s²

加速度的大小：$`|𝐚(2)| = √(12² + 2²) = √(144 + 4) = √148 = 2√37 ≈ 12.17`$ m/s²

（d）已知 $`𝐚(t) = (6t)𝐢 + 2𝐣`$，对加速度积分求速度：

$`𝐯(t) = ∫ 𝐚(t) dt = (∫ 6t dt)𝐢 + (∫ 2 dt)𝐣`$

$`= (3t² + C₁)𝐢 + (2t + C₂)𝐣`$

由 $`𝐯₀ = 𝐯(0) = -3𝐢 - 2𝐣`$，代入 $`t = 0`$ 得 $`C₁ = -3`$，$`C₂ = -2`$。所以：

$`𝐯(t) = (3t² - 3)𝐢 + (2t - 2)𝐣`$

与（a）一致。✓

再对速度积分求位置：

$`𝐫(t) = ∫ 𝐯(t) dt = (∫ (3t² - 3) dt)𝐢 + (∫ (2t - 2) dt)𝐣`$

$`= (t³ - 3t + D₁)𝐢 + (t² - 2t + D₂)𝐣`$

由 $`𝐫₀ = 𝐫(0) = 𝟎`$，代入 $`t = 0`$ 得 $`D₁ = 0`$，$`D₂ = 0`$。所以：

$`𝐫(t) = (t³ - 3t)𝐢 + (t² - 2t)𝐣`$

与原始位置函数一致。✓

---

## 2.4 本章练习题

以下练习题按考试难度编写，涵盖本章所有知识点，题号前面标注了对应的考纲编号。

---

**13.1–13.3 题组：向量基础与几何**

**1.** 已知 $`𝐚 = 2𝐢 - 𝐣`$，$`𝐛 = 𝐢 + 3𝐣`$。

（a）求 $`𝐚 + 2𝐛`$。
（b）求 $`|2𝐚 - 𝐛|`$。
（c）求与 $`3𝐚 + 𝐛`$ 同方向的单位向量。

---

**2.** 在 $`△ABC`$ 中，$`P`$ 是 $`BC`$ 的中点，$`Q`$ 是 $`CA`$ 的中点。设 $`\overrightarrow{AB} = 𝐩`$，$`\overrightarrow{AC} = 𝐪`$。

（a）用 $`𝐩`$ 和 $`𝐪`$ 表示 $`\overrightarrow{BC}`$。
（b）用 $`𝐩`$ 和 $`𝐪`$ 表示 $`\overrightarrow{PQ}`$。
（c）证明 $`PQ ∥ AB`$ 且 $`PQ = (1/2) AB`$。

---

**3.** 三点 $`A`$、$`B`$、$`C`$ 的位置向量分别为 $`𝐚 = 3𝐢 + 2𝐣`$，$`𝐛 = 5𝐢 + 6𝐣`$，$`𝐜 = 9𝐢 + 14𝐣`$。

（a）证明 $`A`$、$`B`$、$`C`$ 三点共线。
（b）求 $`AB:BC`$ 的比值。

---

**13.3–13.4 题组：向量几何与运动**

**4.** 在 $`△OAB`$ 中，$`P`$ 在 $`OA`$ 上且 $`OP:PA = 1:2`$，$`Q`$ 在 $`AB`$ 上且 $`AQ:QB = 2:3`$。设 $`\overrightarrow{OA} = 𝐚`$，$`\overrightarrow{OB} = 𝐛`$。

（a）用 $`𝐚`$ 和 $`𝐛`$ 表示 $`\overrightarrow{OP}`$。
（b）用 $`𝐚`$ 和 $`𝐛`$ 表示 $`\overrightarrow{OQ}`$。
（c）用 $`𝐚`$ 和 $`𝐛`$ 表示 $`\overrightarrow{PQ}`$。

---

**5.** 船 $`A`$ 从点 $`(0, 5)`$ 出发，以速度 $`𝐯_A = (2𝐢 + 3𝐣)`$ km/h 航行。船 $`B`$ 从点 $`(10, 0)`$ 出发，以速度 $`𝐯_B = (-3𝐢 + 4𝐣)`$ km/h 航行。两船同时出发。

（a）写出两船的位置向量 $`𝐫_A(t)`$ 和 $`𝐫_B(t)`$。
（b）判断两船是否会相撞。

---

**6.** 一艘船在静水中的速度为 $`10`$ m/s，船头指向正北方向。水流的速度为 $`6`$ m/s 流向正东方向。

（a）求船相对于地面的合速度向量。
（b）求合速度的大小和方向（与正北方向的夹角）。

---

**14.1 题组：变化率入门**

**7.** 一个质点的位移 $`s`$（米）与时间 $`t`$（秒）的关系为 $`s(t) = 4t² + 3t`$。

（a）求从 $`t = 1`$ 到 $`t = 1 + h`$ 的平均速度。
（b）利用极限求 $`t = 1`$ 时的瞬时速度。

---

**8.** 一个质点在平面内运动，它的位置向量为：

$`𝐫(t) = (2t² + t)𝐢 + (3t - 1)𝐣`$

（a）求速度向量 $`𝐯(t)`$。
（b）求加速度向量 $`𝐚(t)`$。
（c）求 $`t = 2`$ 时速度的大小。

---

**综合题**

**9.** 在平行四边形 $`ABCD`$ 中，$`A`$、$`B`$、$`C`$ 的位置向量分别为 $`𝐚 = 2𝐢 + 𝐣`$，$`𝐛 = 5𝐢 + 3𝐣`$，$`𝐜 = 4𝐢 + 6𝐣`$。

（a）求 $`D`$ 的位置向量。
（b）判断 $`\overrightarrow{AB}`$ 与 $`\overrightarrow{AD}`$ 是否垂直。
（c）求平行四边形 $`ABCD`$ 的面积。

> 提示：平行四边形的面积 $`= |\overrightarrow{AB}| × |\overrightarrow{AD}| × \sin θ`$，其中 $`θ`$ 是两边的夹角。或者用行列式公式：面积 $`= |x₁ y₂ - x₂ y₁|`$，其中 $`\overrightarrow{AB} = (x₁, y₁)`$，$`\overrightarrow{AD} = (x₂, y₂)`$。

---

## 练习题答案

**1.**

（a）$`𝐚 + 2𝐛 = (2𝐢 - 𝐣) + 2(𝐢 + 3𝐣) = (2𝐢 - 𝐣) + (2𝐢 + 6𝐣) = 4𝐢 + 5𝐣`$

（b）$`2𝐚 - 𝐛 = 2(2𝐢 - 𝐣) - (𝐢 + 3𝐣) = (4𝐢 - 2𝐣) - (𝐢 + 3𝐣) = 3𝐢 - 5𝐣`$

$`|2𝐚 - 𝐛| = √(3² + (-5)²) = √(9 + 25) = √34`$

（c）$`3𝐚 + 𝐛 = 3(2𝐢 - 𝐣) + (𝐢 + 3𝐣) = (6𝐢 - 3𝐣) + (𝐢 + 3𝐣) = 7𝐢`$

$`|7𝐢| = 7`$，所以单位向量 $`= 7𝐢 / 7 = 𝐢`$

---

**2.**

（a）$`\overrightarrow{BC} = \overrightarrow{BA} + \overrightarrow{AC} = -\overrightarrow{AB} + \overrightarrow{AC} = -𝐩 + 𝐪`$

（b）$`P`$ 是 $`BC`$ 的中点，所以 $`𝐫_P = (𝐫_B + 𝐫_C) / 2`$。$`Q`$ 是 $`CA`$ 的中点，所以 $`𝐫_Q = (𝐫_C + 𝐫_A) / 2`$。

$`\begin{aligned} \overrightarrow{PQ} &= 𝐫_Q - 𝐫_P \\ &= (𝐫_C + 𝐫_A)/2 - (𝐫_B + 𝐫_C)/2 \\ &= (𝐫_A - 𝐫_B)/2 \\ &= (1/2)\overrightarrow{BA} = -(1/2)\overrightarrow{AB} = -(1/2)𝐩 \end{aligned}`$

（c）由（b）得 $`\overrightarrow{PQ} = -(1/2)𝐩 = -(1/2)\overrightarrow{AB}`$，所以 $`\overrightarrow{PQ} ∥ \overrightarrow{AB}`$ 且 $`|\overrightarrow{PQ}| = (1/2)|\overrightarrow{AB}|`$，即 $`PQ = (1/2)AB`$。✓

> 这条性质叫做**三角形中位线定理**：三角形两边中点的连线平行于第三边且等于第三边的一半。

---

**3.**

（a）

$`\overrightarrow{AB} = 𝐛 - 𝐚 = (5𝐢 + 6𝐣) - (3𝐢 + 2𝐣) = 2𝐢 + 4𝐣`$

$`\overrightarrow{BC} = 𝐜 - 𝐛 = (9𝐢 + 14𝐣) - (5𝐢 + 6𝐣) = 4𝐢 + 8𝐣`$

$`\overrightarrow{BC} = 2(2𝐢 + 4𝐣) = 2\overrightarrow{AB}`$，存在 $`k = 2`$ 使得 $`\overrightarrow{BC} = k\overrightarrow{AB}`$，所以 $`A`$、$`B`$、$`C`$ 共线。

（b）$`|\overrightarrow{AB}| = √(2² + 4²) = √(4 + 16) = √20 = 2√5`$

$`|\overrightarrow{BC}| = √(4² + 8²) = √(16 + 64) = √80 = 4√5`$

所以 $`AB:BC = 2√5 : 4√5 = 1:2`$

---

**4.**

（a）$`OP:PA = 1:2`$，所以 $`OP:OA = 1:3`$，即 $`\overrightarrow{OP} = (1/3)𝐚`$

（b）$`AQ:QB = 2:3`$，所以 $`AQ:AB = 2:5`$，$`Q`$ 从 $`A`$ 到 $`B`$ 的 $`2/5`$ 处。

$`\begin{aligned} \overrightarrow{OQ} &= \overrightarrow{OA} + (2/5)\overrightarrow{AB} \\ &= 𝐚 + (2/5)(𝐛 - 𝐚) \\ &= 𝐚 + (2/5)𝐛 - (2/5)𝐚 \\ &= (3/5)𝐚 + (2/5)𝐛 \end{aligned}`$

（c）

$`\overrightarrow{PQ} = \overrightarrow{OQ} - \overrightarrow{OP} = ((3/5)𝐚 + (2/5)𝐛) - (1/3)𝐚 = ((3/5) - (1/3))𝐚 + (2/5)𝐛`$

通分：$`3/5 - 1/3 = 9/15 - 5/15 = 4/15`$

所以 $`\overrightarrow{PQ} = (4/15)𝐚 + (2/5)𝐛`$

---

**5.**

（a）

$`𝐫_A(t) = (0, 5) + (2, 3) t = (2t, 5 + 3t)`$

$`𝐫_B(t) = (10, 0) + (-3, 4) t = (10 - 3t, 4t)`$

（b）令 $`𝐫_A(t) = 𝐫_B(t)`$：

$`\begin{cases} 2t = 10 - 3t ⟹ 5t = 10 ⟹ t = 2 \\ 5 + 3t = 4t ⟹ 5 = t ⟹ t = 5 \end{cases}`$

两个 $`t`$ 值不相等（$`2 ≠ 5`$），所以两船不会相撞。

---

**6.**

（a）设正北为 $`+y`$ 轴，正东为 $`+x`$ 轴。

船相对于水的速度：$`𝐯_{B/W} = 10𝐣`$
水相对于地面的速度：$`𝐯_{W/G} = 6𝐢`$

合速度：$`𝐯_{B/G} = 6𝐢 + 10𝐣`$

（b）大小：$`|𝐯_{B/G}| = √(6² + 10²) = √(36 + 100) = √136 = 2√34 ≈ 11.66`$ m/s

方向：设 $`θ`$ 为与正北方向的夹角。

$`\tan θ = 6/10 = 0.6 ⟹ θ = \arctan(0.6) ≈ 30.96°`$

所以方向为东偏北 $`30.96°`$（或北偏东 $`59.04°`$）。

---

**7.**

（a）

$`\begin{aligned} (s(1+h) - s(1)) / h &= ([4(1+h)² + 3(1+h)] - [4 + 3]) / h \\ &= ([4(1 + 2h + h²) + 3 + 3h] - 7) / h \\ &= (4 + 8h + 4h² + 3 + 3h - 7) / h \\ &= (11h + 4h²) / h = 11 + 4h \end{aligned}`$

（b）$`v(1) = \lim_{h → 0} (11 + 4h) = 11`$ m/s

验证：$`s'(t) = 8t + 3`$，$`s'(1) = 8 + 3 = 11`$ ✓

---

**8.**

（a）$`𝐯(t) = d𝐫/dt = (4t + 1)𝐢 + 3𝐣`$

（b）$`𝐚(t) = d𝐯/dt = 4𝐢`$

（c）$`𝐯(2) = (4 × 2 + 1)𝐢 + 3𝐣 = 9𝐢 + 3𝐣`$

$`|𝐯(2)| = √(9² + 3²) = √(81 + 9) = √90 = 3√10 ≈ 9.49`$ m/s

---

**9.**

（a）平行四边形 $`ABCD`$ 中，$`\overrightarrow{AD} = \overrightarrow{BC}`$。

$`\overrightarrow{BC} = 𝐜 - 𝐛 = (4𝐢 + 6𝐣) - (5𝐢 + 3𝐣) = -𝐢 + 3𝐣`$

所以 $`𝐝 = 𝐚 + \overrightarrow{BC} = (2𝐢 + 𝐣) + (-𝐢 + 3𝐣) = 𝐢 + 4𝐣`$

（b）$`\overrightarrow{AB} = 𝐛 - 𝐚 = (5𝐢 + 3𝐣) - (2𝐢 + 𝐣) = 3𝐢 + 2𝐣`$

$`\overrightarrow{AD} = 𝐝 - 𝐚 = (𝐢 + 4𝐣) - (2𝐢 + 𝐣) = -𝐢 + 3𝐣`$

点积：$`\overrightarrow{AB} · \overrightarrow{AD} = (3)(-1) + (2)(3) = -3 + 6 = 3 ≠ 0`$

所以 $`\overrightarrow{AB}`$ 与 $`\overrightarrow{AD}`$ **不垂直**。

（c）方法一（用叉积的行列式公式）：

$`\text{面积} = |x₁ y₂ - x₂ y₁|`$

其中 $`\overrightarrow{AB} = (3, 2)`$，$`\overrightarrow{AD} = (-1, 3)`$。

$`\text{面积} = |3 × 3 - 2 × (-1)| = |9 + 2| = 11`$

方法二（用 $`|\overrightarrow{AB}| · |\overrightarrow{AD}| · \sin θ`$）：

$`|\overrightarrow{AB}| = √(3² + 2²) = √13, \quad |\overrightarrow{AD}| = √((-1)² + 3²) = √10`$

由点积 $`\overrightarrow{AB} · \overrightarrow{AD} = |\overrightarrow{AB}||\overrightarrow{AD}|\cos θ`$：

$`3 = √13 · √10 · \cos θ ⟹ \cos θ = 3 / √130`$

$`\sin θ = √(1 - \cos²θ) = √(1 - 9/130) = √(121/130) = 11 / √130`$

$`\text{面积} = √13 · √10 · (11 / √130) = √130 · (11 / √130) = 11`$

平行四边形的面积为 $`11`$ 平方单位。✓

---

## 本章总结

### 考纲覆盖核对

| 考纲编号 | 内容 | 对应节次 | 例题 | 练习题 |
|:---:|------|:---:|:---:|:---:|
| 13.1 | 向量记号 | 2.1.1 | 2.1(1) | 1 |
| 13.2 | 位置向量与单位向量 | 2.1.2, 2.1.4 | 2.1(1) | 1(c) |
| 13.3 | 模、加减、数乘、向量几何 | 2.1.3, 2.1.5–2.1.8, 2.2.1 | 2.1, 2.2 | 1–4, 9 |
| 13.4 | 速度合成与分解、相撞问题 | 2.2.2 | 2.3 | 5, 6 |
| 14.1 | 变化率与极限思想 | 2.3 | 2.4 | 7, 8 |

### 核心公式速查表

**向量部分**：

| 概念 | 公式 |
|:---|:---|
| 向量表示 | $`𝐯 = x𝐢 + y𝐣 = (x, y)`$ |
| 模 | $`|𝐯| = √(x² + y²)`$ |
| 单位向量 | $`𝐯̂ = 𝐯 / |𝐯|`$ |
| 位移 | $`\overrightarrow{AB} = 𝐫_B - 𝐫_A`$ |
| 中点 | $`𝐫_M = (𝐫_A + 𝐫_B) / 2`$ |
| 分点（$`AP:PB = m:n`$） | $`𝐫_P = (n𝐫_A + m𝐫_B) / (m+n)`$ |
| 平行条件 | $`𝐚 = k𝐛`$（存在标量 $`k`$） |
| 垂直条件（点积） | $`𝐮 · 𝐯 = u_x v_x + u_y v_y = 0`$ |
| 斜率法判垂直 | $`k₁ · k₂ = -1`$ |
| 速度合成 | $`𝐯_{A/C} = 𝐯_{A/B} + 𝐯_{B/C}`$ |
| 速度分解 | $`𝐯 = (v\cos θ)𝐢 + (v\sin θ)𝐣`$ |
| 匀速直线运动 | $`𝐫(t) = 𝐫₀ + 𝐯t`$ |
| 相撞条件 | $`𝐫₁(t) = 𝐫₂(t)`$ |

**变化率部分**：

| 概念 | 公式 |
|:---|:---|
| 导数定义 | $`f'(a) = \lim_{h → 0} (f(a + h) - f(a)) / h`$ |
| 幂法则 | $`(d/dx)(xⁿ) = n xⁿ⁻¹`$ |
| 速度（向量） | $`𝐯(t) = d𝐫/dt = (dx/dt)𝐢 + (dy/dt)𝐣`$ |
| 加速度（向量） | $`𝐚(t) = d𝐯/dt = (d²x/dt²)𝐢 + (d²y/dt²)𝐣`$ |
| 积分求速度 | $`𝐯(t) = ∫ 𝐚(t) dt + 𝐯₀`$ |
| 积分求位置 | $`𝐫(t) = ∫ 𝐯(t) dt + 𝐫₀`$ |

### 学习路线图

从本章出发，后续章节将这样深化你的理解：

- **第 5 章（微分）**：系统学习求导法则（链式法则、积法则、商法则），以及用导数求切线、法线和极值
- **第 7 章（积分）**：学习微分的逆运算——积分，掌握从加速度求速度和位置的完整方法
- **第 10 章（综合应用）**：将向量和微积分结合，解决完整的运动学问题

---
---

# 第 3 章：二次函数（含多项式因式）

## 本章引言

二次函数是 IGCSE 附加数学（0606）中贯穿始终的核心工具。从解方程到求极值，从不等式到曲线分析，二次函数的知识无处不在。本章将二次函数的概念体系与多项式因式有机结合，形成从"因式"到"函数"再到"图像"的完整逻辑链条。

**考纲对照表**：

| 考纲编号 | 内容 | 对应小节 |
|:---:|:---|:---:|
| **3.1** | 余数定理与因式定理 | §3.1.1, §3.1.2 |
| **3.2** | 分解多项式（三次 → 线性×二次） | §3.1.3 |
| **3.3** | 解三次方程 | §3.1.4 |
| **2.1** | 配方法或微分求二次函数极值 | §3.4, §3.7 |
| **2.2** | 利用最值画草图或求值域 | §3.4 |
| **2.3** | 判别式与根的条件、直线与曲线位置关系 | §3.2 |
| **2.4** | 解二次方程（因式分解、公式法、配方法） | §3.3 |
| **2.5** | 二次不等式（代数法或图像法） | §3.5 |
| **4.4** | 三个线性因式乘积的图像与绝对值图像 | §3.6.1, §3.6.2 |
| **4.5** | 三次不等式的图像解法 | §3.6.3 |

---

## 3.1 多项式的因式

### 3.1.1 余数定理

**定理陈述**：当多项式 $`f(x)`$ 除以线性因式 $`(x - a)`$ 时，余数等于 $`f(a)`$。

**完整推导**：

设 $`f(x)`$ 除以 $`(x - a)`$，得到商式 $`q(x)`$ 和余数 $`R`$。由于除式 $`(x - a)`$ 是一次式，余数 $`R`$ 的次数必须低于除式的次数，所以 $`R`$ 只能是一个常数。因此有恒等式：

$`
f(x) = (x - a) · q(x) + R
`$

这个等式对**所有** $`x`$ 的值都成立——这是多项式恒等式的基本性质。

现在，我们巧妙地选择 $`x = a`$ 代入。之所以选 $`x = a`$，是因为这样可以使 $`(x - a)`$ 这一项变为零，从而消去包含 $`q(x)`$ 的项：

$`
f(a) = (a - a) · q(a) + R = 0 · q(a) + R = R
`$

因此 $`R = f(a)`$。这就是余数定理的全部内容。

**为什么这个定理如此有用？**

要得到余数，不需要做完整的长除法，只需要把 $`x = a`$ 代入原多项式计算即可。这大大简化了计算，尤其是当多项式次数较高时。

**推广形式**：

如果除式是 $`(ax + b)`$，则令 $`ax + b = 0`$ 解出 $`x = -b/a`$，余数为：

$`
R = f(-b/a)
`$

思路完全相同——找到使除式为零的 $`x`$ 值后代入。

---

**例题 1**：多项式 $`f(x) = 2x^3 - 3x^2 + 4x - 5`$ 除以 $`x - 2`$，求余数。

**详解**：

除式为 $`x - 2`$，令 $`x - 2 = 0`$ 得 $`x = 2`$。由余数定理，余数 $`R = f(2)`$。

代入计算，逐项进行：

$`
f(2) = 2(2)^3 - 3(2)^2 + 4(2) - 5
= 2×8 - 3×4 + 8 - 5
= 16 - 12 + 8 - 5 = 7
`$

所以余数为 $`7`$。

---

**例题 2**：多项式 $`f(x) = x^3 + kx^2 - 2x + 3`$ 除以 $`x + 1`$ 余数为 $`5`$，求常数 $`k`$ 的值。

**详解**：

除式为 $`x + 1`$，令 $`x + 1 = 0`$ 得 $`x = -1`$。注意这里的符号：$`x + 1 = 0`$ 的解是 $`x = -1`$，不是 $`x = 1`$。这是一个常见的易错点。

由余数定理，余数等于 $`f(-1)`$，题目已知余数为 5，所以 $`f(-1) = 5`$。

代入计算：

$`
f(-1) = (-1)^3 + k(-1)^2 - 2(-1) + 3 = 5
-1 + k + 2 + 3 = 5
k + 4 = 5
k = 1
`$

所以 $`k = 1`$。

---

**例题 3**：多项式 $`f(x) = 6x^3 + x^2 - 8x + 2`$ 除以 $`2x - 1`$，求余数。

**详解**：

除式是 $`2x - 1`$，不是简单的 $`(x - a)`$ 形式。令 $`2x - 1 = 0`$，解得 $`x = 1/2`$。

代入 $`x = 1/2`$ 时，注意分数运算的准确性：

$`
f(1/2) = 6(1/2)^3 + (1/2)^2 - 8(1/2) + 2
= 6 × 1/8 + 1/4 - 4 + 2
= 6/8 + 1/4 - 2
= 3/4 + 1/4 - 2
= 1 - 2 = -1
`$

所以余数为 $`-1`$。

---

**例题 4**：多项式 $`f(x) = 4x^4 - 3x^3 + 2x^2 - x + 5`$ 除以 $`x - 1`$，求余数。

**详解**：

令 $`x - 1 = 0`$ 得 $`x = 1`$。余数 $`R = f(1)`$。

$`
f(1) = 4(1)^4 - 3(1)^3 + 2(1)^2 - 1 + 5
= 4 - 3 + 2 - 1 + 5 = 7
`$

余数为 $`7`$。即使多项式次数更高，余数定理仍然适用。

---

**例题 5**：多项式 $`f(x) = x^4 - 2x^3 + 3x^2 - 4x + 2`$ 除以 $`x + 2`$，求余数。

**详解**：

令 $`x + 2 = 0`$ 得 $`x = -2`$。

$`
f(-2) = (-2)^4 - 2(-2)^3 + 3(-2)^2 - 4(-2) + 2
= 16 - 2(-8) + 3(4) + 8 + 2
= 16 + 16 + 12 + 8 + 2 = 54
`$

余数为 $`54`$。

---

**例题 6**：多项式 $`f(x) = 8x^3 - 4x^2 + 2x - 1`$ 除以 $`2x + 3`$，求余数。

**详解**：

令 $`2x + 3 = 0`$ 得 $`x = -3/2`$。余数 $`R = f(-3/2)`$。

$`
f(-3/2) = 8(-3/2)^3 - 4(-3/2)^2 + 2(-3/2) - 1
= 8(-27/8) - 4(9/4) - 3 - 1
= -27 - 9 - 3 - 1 = -40
`$

余数为 $`-40`$。

---

### 3.1.2 因式定理

**定理陈述**：如果 $`f(a) = 0`$，那么 $`(x - a)`$ 是 $`f(x)`$ 的一个因式。反之，如果 $`(x - a)`$ 是 $`f(x)`$ 的一个因式，那么 $`f(a) = 0`$。

**推导**：

因式定理是余数定理的直接推论。由余数定理，$`f(x)`$ 除以 $`(x - a)`$ 的余数等于 $`f(a)`$。如果 $`f(a) = 0`$，意味着余数为零——也就是说 $`(x - a)`$ **完全整除** $`f(x)`$，没有余数，所以 $`(x - a)`$ 是 $`f(x)`$ 的一个因式。

反过来，如果 $`(x - a)`$ 是 $`f(x)`$ 的因式，那么 $`f(x) = (x - a) · q(x)`$，代入 $`x = a`$ 得 $`f(a) = 0`$。

**实际意义**：

要判断 $`(x - a)`$ 是不是 $`f(x)`$ 的因式，只需要看 $`f(a)`$ 是否等于 0。这比做长除法快得多。同时，因式定理是**分解三次多项式的起点**——通过试根找到一个零点，就等于找到了一个线性因式。

**试根策略**：

对于整系数多项式，可能的有理根为 $`± (常数项的因数)/(首项系数的因数)`$。对于三次多项式，通常从最简单的 $`±1, ±2, ±3`$ 开始试。

---

**例题 1**：证明 $`(x - 2)`$ 是 $`f(x) = x^3 - 3x^2 - 4x + 12`$ 的一个因式。

**详解**：

要证明 $`(x - 2)`$ 是因式，由因式定理，只需证明 $`f(2) = 0`$。

$`
f(2) = 2^3 - 3(2)^2 - 4(2) + 12
= 8 - 12 - 8 + 12 = 0
`$

因为 $`f(2) = 0`$，所以 $`(x - 2)`$ 是 $`f(x)`$ 的一个因式。✓

---

**例题 2**：多项式 $`f(x) = 2x^3 + ax^2 + bx - 6`$ 有因式 $`(x - 1)`$ 和 $`(x + 2)`$，求 $`a`$ 和 $`b`$ 的值。

**详解**：

由因式定理：
- $`(x - 1)`$ 是因式 ⇒ $`f(1) = 0`$
- $`(x + 2)`$ 是因式 ⇒ $`f(-2) = 0`$

由 $`f(1) = 0`$：

$`
2 + a + b - 6 = 0 ⇒ a + b = 4 (1)
`$

由 $`f(-2) = 0`$：

$`
-16 + 4a - 2b - 6 = 0 ⇒ 4a - 2b = 22 (2)
`$

联立 (1)(2)：由 (1) 得 $`b = 4 - a`$，代入 (2)：

$`
4a - 2(4 - a) = 22 ⇒ 4a - 8 + 2a = 22 ⇒ 6a = 30 ⇒ a = 5
`$

所以 $`b = 4 - 5 = -1`$。

---

**例题 3**：已知 $`x - 2`$ 是 $`f(x) = x^3 - px^2 + 5x - 2`$ 的一个因式，求 $`p`$ 的值并将 $`f(x)`$ 完全因式分解。

**详解**：

由因式定理，$`f(2) = 0`$：

$`
8 - 4p + 10 - 2 = 16 - 4p = 0 ⇒ p = 4
`$

所以 $`f(x) = x^3 - 4x^2 + 5x - 2`$。

用综合除法除以 $`(x - 2)`$：

```
2 | 1  -4   5  -2
  |     2  -4   2
  ----------------
    1  -2   1   0
```

商式为 $`x^2 - 2x + 1 = (x - 1)^2`$。

因此：

$`
f(x) = (x - 2)(x - 1)^2
`$

---

**例题 4**：已知 $`x - 3`$ 是 $`f(x) = 2x^3 - 7x^2 + 2x + 3`$ 的一个因式，将 $`f(x)`$ 分解。

**详解**：

综合除法：

```
3 | 2  -7   2   3
  |     6  -3  -3
  ----------------
    2  -1  -1   0
```

商式为 $`2x^2 - x - 1`$。

分解二次式：$`2x^2 - x - 1 = 2x^2 - 2x + x - 1 = 2x(x - 1) + 1(x - 1) = (2x + 1)(x - 1)`$。

因此：

$`
f(x) = (x - 3)(2x + 1)(x - 1)
`$

---

**例题 5**：多项式 $`f(x) = 3x^3 + kx^2 - 4x + 4`$ 有因式 $`(x + 1)`$，求 $`k`$ 的值。

**详解**：

$`(x + 1)`$ 是因式 ⇒ $`f(-1) = 0`$。

$`
f(-1) = -3 + k + 4 + 4 = k + 5 = 0 ⇒ k = -5
`$

---

**例题 6**：已知 $`x - 1`$ 和 $`x - 2`$ 都是 $`f(x) = x^3 + ax^2 + bx + c`$ 的因式，且 $`f(x)`$ 除以 $`x + 1`$ 余数为 $`-12`$，求 $`a, b, c`$ 的值并写出 $`f(x)`$ 的完整因式分解。

**详解**：

由已知条件：

$`
f(1) = 1 + a + b + c = 0 ⇒ a + b + c = -1 (1)
f(2) = 8 + 4a + 2b + c = 0 ⇒ 4a + 2b + c = -8 (2)
f(-1) = -1 + a - b + c = -12 ⇒ a - b + c = -11 (3)
`$

(2) - (1)：$`3a + b = -7 (4)`$
(1) - (3)：$`2b = 10 ⇒ b = 5`$

代入 (4)：$`3a + 5 = -7 ⇒ 3a = -12 ⇒ a = -4`$

代入 (1)：$`-4 + 5 + c = -1 ⇒ c = -2`$

所以 $`f(x) = x^3 - 4x^2 + 5x - 2`$。

验证：
- $`f(1) = 1 - 4 + 5 - 2 = 0`$ ✓
- $`f(2) = 8 - 16 + 10 - 2 = 0`$ ✓
- $`f(-1) = -1 - 4 - 5 - 2 = -12`$ ✓

用综合除法除以 $`(x - 1)`$：

```
1 | 1  -4   5  -2
  |     1  -3   2
  ----------------
    1  -3   2   0
```

商式为 $`x^2 - 3x + 2 = (x - 1)(x - 2)`$。

所以 $`f(x) = (x - 1)^2(x - 2)`$。

---

### 3.1.3 分解三次多项式

分解三次多项式 $`f(x) = ax^3 + bx^2 + cx + d`$ 就像解一个"三层锁"：先找到一把钥匙（一个线性因式），打开第一层锁后露出一个二次锁，再用熟悉的二次分解方法打开它。

**标准流程**：

**Step 1：试根——用因式定理找线性因式**

试 $`x = ±1, ±2, ±3, ...`$ 以及分数 $`± (常数项的因数)/(首项系数的因数)`$。找到 $`p`$ 使得 $`f(p) = 0`$，则 $`(x - p)`$ 是一个因式。

**Step 2：综合除法——求二次商式**

将 $`f(x)`$ 除以 $`(x - p)`$，得到二次式 $`Ax^2 + Bx + C`$。

**Step 3：分解二次式**

用因式分解、求根公式或配方法分解 $`Ax^2 + Bx + C`$。

**Step 4：完整因式分解**

写出 $`f(x) = (x - p) × (二次因式的分解)`$。

---

#### 综合除法详解

综合除法是一种简洁的多项式除法格式，只处理系数，不写变量。

**步骤**（以 $`f(x) = a₃x³ + a₂x² + a₁x + a₀`$ 除以 $`(x - p)`$ 为例）：

```
p |  a₃   a₂   a₁   a₀
  |     p·b₂ p·b₁ p·b₀
  ------------------------
     b₂   b₁   b₀   R
```

其中：
- $`b₂ = a₃`$（下拉首项系数）
- $`b₁ = a₂ + p·b₂`$
- $`b₀ = a₁ + p·b₁`$
- $`R = a₀ + p·b₀`$（余数）

商式为 $`b₂x² + b₁x + b₀`$，余数为 $`R`$。

---

**例题 1**：分解 $`f(x) = x³ - 6x² + 11x - 6`$。

**详解**：

**Step 1：试根**。从最简单的 $`x = 1`$ 开始试：

$`
f(1) = 1 - 6 + 11 - 6 = 0
`$

所以 $`(x - 1)`$ 是一个因式。

**Step 2：综合除法**。

```
    1 |  1   -6   11   -6
      |      1   -5    6
      -------------------
         1   -5    6    0
```

**详细过程说明**：
- 将系数 $`1, -6, 11, -6`$ 依次排开。
- 最左边的数字 $`1`$ 来自 $`(x - 1)`$ 的根 $`x = 1`$。
- **下拉**：首项系数 $`1`$ 直接写到下面。
- **第 1 步**：$`1×1 = 1`$，写到第二行第二个位置。$`-6 + 1 = -5`$ 写到下面。
- **第 2 步**：$`(-5)×1 = -5`$，写到第二行第三个位置。$`11 + (-5) = 6`$ 写到下面。
- **第 3 步**：$`6×1 = 6`$，写到第二行第四个位置。$`-6 + 6 = 0`$ 写到下面。

下面第三行的数字 $`1, -5, 6, 0`$ 的含义：
- $`1, -5, 6`$ 是商式的系数：$`1x² - 5x + 6`$
- 最后的 $`0`$ 是余数——为 0 说明整除，验证了 $`(x - 1)`$ 确实为因式。

**Step 3：分解二次式**。

$`
x² - 5x + 6 = (x - 2)(x - 3)
`$

找到两个数乘积为 $`6`$、和为 $`-5`$：$`-2`$ 和 $`-3`$。

**Step 4：完整分解**。

$`
f(x) = (x - 1)(x - 2)(x - 3)
`$

---

**例题 2**：分解 $`f(x) = 2x³ - 3x² - 11x + 6`$。

**详解**：

**Step 1：试根**。

首项系数为 2，常数项为 6，可能的有理根为 $`±1, ±2, ±3, ±6, ±1/2, ±3/2`$。

试 $`x = 2`$：$`f(2) = 16 - 12 - 22 + 6 = -12 ≠ 0`$。
试 $`x = -2`$：

$`
f(-2) = -16 - 12 + 22 + 6 = 0
`$

所以 $`(x + 2)`$ 是一个因式（注意 $`x = -2`$ 对应 $`x + 2 = 0`$）。

**Step 2：综合除法**。

```
    -2 |  2   -3   -11    6
       |     -4    14    -6
       --------------------
          2   -7     3    0
```

**过程说明**：
- 最左边是 $`-2`$（根的值）。
- 下拉 $`2`$。
- $`2×(-2) = -4`$，$`-3 + (-4) = -7`$。
- $`(-7)×(-2) = 14`$，$`-11 + 14 = 3`$。
- $`3×(-2) = -6`$，$`6 + (-6) = 0`$。

商式为 $`2x² - 7x + 3`$。

**Step 3：分解二次式 $`2x² - 7x + 3`$**。

$`a×c = 2×3 = 6`$。找两个数乘积为 $`6`$、和为 $`-7`$：$`(-1)×(-6) = 6`$，$`(-1) + (-6) = -7`$。

拆分并分组：

$`
2x² - 7x + 3 = 2x² - x - 6x + 3
= x(2x - 1) - 3(2x - 1)
= (2x - 1)(x - 3)
`$

**Step 4：完整分解**。

$`
f(x) = (x + 2)(2x - 1)(x - 3)
`$

---

**例题 3**：分解 $`f(x) = 3x³ + 6x² - 3x - 6`$。

**详解**：

**Step 0 观察**：所有系数都能被 3 整除，先提取公因式 3：

$`
f(x) = 3(x³ + 2x² - x - 2)
`$

令 $`g(x) = x³ + 2x² - x - 2`$。

**Step 1：试根**。试 $`x = 1`$：$`g(1) = 1 + 2 - 1 - 2 = 0`$。所以 $`(x - 1)`$ 是 $`g(x)`$ 的因式。

**Step 2：综合除法**。

```
    1 |  1    2   -1   -2
      |       1    3    2
      --------------------
         1    3    2    0
```

商式为 $`x² + 3x + 2`$。

**Step 3：分解二次式**。

$`
x² + 3x + 2 = (x + 1)(x + 2)
`$

**Step 4：完整分解**。

$`
f(x) = 3(x - 1)(x + 1)(x + 2)
`$

---

**例题 4**：分解 $`f(x) = 2x³ - 9x² + 7x + 6`$。

**详解**：

**Step 1：试根**。

试 $`x = 1`$：$`f(1) = 2 - 9 + 7 + 6 = 6 ≠ 0`$。
试 $`x = -1`$：$`f(-1) = -2 - 9 - 7 + 6 = -12 ≠ 0`$。
试 $`x = 2`$：$`f(2) = 16 - 36 + 14 + 6 = 0`$。✓

所以 $`(x - 2)`$ 是一个因式。

**Step 2：综合除法**。

```
    2 |  2   -9    7    6
      |       4  -10   -6
      --------------------
         2   -5   -3    0
```

商式为 $`2x² - 5x - 3`$。

**Step 3：分解二次式 $`2x² - 5x - 3`$**。

$`a×c = 2×(-3) = -6`$。找两个数乘积为 $`-6`$、和为 $`-5`$：$`(-6)×1 = -6`$，$`(-6) + 1 = -5`$。

$`
2x² - 6x + x - 3 = 2x(x - 3) + 1(x - 3) = (2x + 1)(x - 3)
`$

**Step 4：完整分解**。

$`
f(x) = (x - 2)(2x + 1)(x - 3)
`$

---

**例题 5**：分解 $`f(x) = 6x³ - 7x² - 7x + 6`$。

**详解**：

**Step 1：试根**。

试 $`x = 1`$：$`f(1) = 6 - 7 - 7 + 6 = -2 ≠ 0`$。
试 $`x = -1`$：$`f(-1) = -6 - 7 + 7 + 6 = 0`$。✓

所以 $`(x + 1)`$ 是一个因式。

**Step 2：综合除法**。

```
    -1 |  6   -7   -7    6
       |     -6   13   -6
       --------------------
          6  -13    6    0
```

商式为 $`6x² - 13x + 6`$。

**Step 3：分解二次式 $`6x² - 13x + 6`$**。

$`a×c = 6×6 = 36`$。找两个数乘积为 $`36`$、和为 $`-13`$：$`(-9)×(-4) = 36`$，$`(-9) + (-4) = -13`$。

$`
6x² - 9x - 4x + 6 = 3x(2x - 3) - 2(2x - 3) = (3x - 2)(2x - 3)
`$

**Step 4：完整分解**。

$`
f(x) = (x + 1)(3x - 2)(2x - 3)
`$

---

**例题 6**：分解 $`f(x) = x³ + 2x² - 5x - 6`$。

**详解**：

**Step 1：试根**。

试 $`x = 1`$：$`f(1) = 1 + 2 - 5 - 6 = -8 ≠ 0`$。
试 $`x = -1`$：$`f(-1) = -1 + 2 + 5 - 6 = 0`$。✓

所以 $`(x + 1)`$ 是一个因式。

**Step 2：综合除法**。

```
    -1 |  1    2   -5   -6
       |     -1   -1    6
       --------------------
          1    1   -6    0
```

商式为 $`x² + x - 6`$。

**Step 3：分解二次式**。

$`
x² + x - 6 = (x + 3)(x - 2)
`$

**Step 4：完整分解**。

$`
f(x) = (x + 1)(x + 3)(x - 2)
`$

---

### 3.1.4 解三次方程

解三次方程 $`ax³ + bx² + cx + d = 0`$ 的流程就是**将三次多项式分解因式，然后令每个因式等于零**。因为如果几个因式的乘积为零，则至少其中一个因式为零。

---

**例题 1**：解 $`x³ - 4x² + x + 6 = 0`$。

**详解**：

试根：$`f(2) = 8 - 16 + 2 + 6 = 0`$，所以 $`(x - 2)`$ 是因式。

综合除法：

```
    2 |  1   -4    1    6
      |       2   -4   -6
      --------------------
         1   -2   -3    0
```

商式 $`x² - 2x - 3 = (x - 3)(x + 1)`$。

所以 $`(x - 2)(x - 3)(x + 1) = 0`$。

解得：

$`
x = 2, x = 3, x = -1
`$

---

**例题 2**：解 $`x³ - 5x² + 8x - 4 = 0`$。

**详解**：

试 $`x = 1`$：$`f(1) = 1 - 5 + 8 - 4 = 0`$。

综合除法：

```
    1 |  1   -5    8   -4
      |       1   -4    4
      --------------------
         1   -4    4    0
```

商式 $`x² - 4x + 4 = (x - 2)²`$。

所以 $`(x - 1)(x - 2)² = 0`$。

解得：

$`
x = 1 或 x = 2 (二重根)
`$

---

**例题 3**：已知 $`x = 2`$ 是 $`2x³ - 3x² - 18x + k = 0`$ 的一个根，求 $`k`$ 并解此方程。

**详解**：

代入 $`x = 2`$：$`16 - 12 - 36 + k = -32 + k = 0 ⇒ k = 32`$。

方程为 $`2x³ - 3x² - 18x + 32 = 0`$。已知 $`(x - 2)`$ 是因式。

综合除法：

```
    2 |  2   -3   -18   32
      |       4     2   -32
      ----------------------
         2    1   -16    0
```

商式 $`2x² + x - 16`$。用求根公式：

$`
x = (-1 ± √(1 + 128)) / 4 = (-1 ± √129) / 4
`$

所以解为：

$`
x = 2, x = (-1 + √129) / 4, x = (-1 - √129) / 4
`$

---

**例题 4**：解 $`x³ - 2x² - 5x + 6 = 0`$。

**详解**：

试 $`x = 1`$：$`f(1) = 1 - 2 - 5 + 6 = 0`$。✓

综合除法：

```
    1 |  1   -2   -5    6
      |       1   -1   -6
      --------------------
         1   -1   -6    0
```

商式 $`x² - x - 6 = (x - 3)(x + 2)`$。

所以 $`(x - 1)(x - 3)(x + 2) = 0`$。

解得：

$`
x = 1, x = 3, x = -2
`$

---

**例题 5**：解 $`2x³ - x² - 13x - 6 = 0`$。

**详解**：

试 $`x = -1`$：$`f(-1) = -2 - 1 + 13 - 6 = 4 ≠ 0`$。
试 $`x = -2`$：$`f(-2) = -16 - 4 + 26 - 6 = 0`$。✓

综合除法：

```
    -2 |  2   -1   -13   -6
       |     -4    10    6
       ---------------------
          2   -5    -3    0
```

商式 $`2x² - 5x - 3 = (2x + 1)(x - 3)`$。

所以 $`(x + 2)(2x + 1)(x - 3) = 0`$。

解得：

$`
x = -2, x = -1/2, x = 3
`$

---

**例题 6**：已知 $`x = 3`$ 是 $`x³ + 4x² - 11x + k = 0`$ 的一个根，求 $`k`$ 并解此方程。

**详解**：

代入 $`x = 3`$：$`27 + 36 - 33 + k = 30 + k = 0 ⇒ k = -30`$。

方程为 $`x³ + 4x² - 11x - 30 = 0`$。已知 $`(x - 3)`$ 是因式。

综合除法：

```
    3 |  1    4   -11   -30
      |       3    21    30
      ---------------------
         1    7    10     0
```

商式 $`x² + 7x + 10 = (x + 2)(x + 5)`$。

所以 $`(x - 3)(x + 2)(x + 5) = 0`$。

解得：

$`
x = 3, x = -2, x = -5
`$

---

## 3.2 判别式 Δ

### 3.2.1 判别式的定义与三种情况

对于一元二次方程 $`ax² + bx + c = 0`$（$`a ≠ 0`$），求根公式为：

$`
x = (-b ± √(b² - 4ac)) / (2a)
`$

根号内的表达式 $`b² - 4ac`$ 决定了根的性质。我们定义**判别式**：

$`
Δ = b² - 4ac
`$

**为什么 Δ 能决定根的情况？**

| Δ 的值 | √Δ 的情况 | 根的情况 |
|:---:|:---|:---|
| Δ > 0 | √Δ 是正实数 | $`(-b ± 正数)/(2a)`$，两个不同的根 |
| Δ = 0 | √Δ = 0 | $`(-b ± 0)/(2a) = -b/(2a)`$，一个重根 |
| Δ < 0 | √Δ 在实数范围内无意义 | 无实数根 |

---

**例题 1**：判断下列方程的根的情况：
(a) $`2x² - 3x + 1 = 0`$
(b) $`4x² - 4x + 1 = 0`$
(c) $`3x² + 2x + 5 = 0`$

**详解**：

(a) $`Δ = (-3)² - 4(2)(1) = 9 - 8 = 1 > 0`$，两个不等实根。

(b) $`Δ = (-4)² - 4(4)(1) = 16 - 16 = 0`$，重根。

(c) $`Δ = 2² - 4(3)(5) = 4 - 60 = -56 < 0`$，无实根。

---

**例题 2**：方程 $`x² + kx + 4 = 0`$ 有重根，求 $`k`$。

**详解**：

$`Δ = k² - 16 = 0 ⇒ k = ±4`$。

---

**例题 3**：方程 $`2x² - 3x + k = 0`$ 有两个不等实根，求 $`k`$ 的范围。

**详解**：

$`Δ = 9 - 8k > 0 ⇒ k < 9/8`$。

---

**例题 4**：方程 $`x² - 2x + (k - 3) = 0`$ 无实根，求 $`k`$ 的范围。

**详解**：

$`Δ = (-2)² - 4(1)(k - 3) = 4 - 4k + 12 = 16 - 4k < 0 ⇒ 4k > 16 ⇒ k > 4`$。

---

**例题 5**：方程 $`kx² + 3x + 2 = 0`$ 有重根，求 $`k`$ 的值。

**详解**：

注意 $`k`$ 是二次项系数，$`k ≠ 0`$。

$`Δ = 9 - 8k = 0 ⇒ k = 9/8`$。

---

**例题 6**：方程 $`3x² - 4x + k = 0`$ 有两个不等实根，求 $`k`$ 的范围。

**详解**：

$`Δ = 16 - 12k > 0 ⇒ k < 4/3`$。

---

### 3.2.2 直线与曲线的位置关系

将直线 $`y = mx + c`$ 代入曲线消去 $`y`$，得到关于 $`x`$ 的二次方程：

| Δ 的值 | 交点个数 | 位置关系 |
|:---:|:---:|:---|
| Δ > 0 | 2 个 | 相交于两点 |
| Δ = 0 | 1 个 | 相切 |
| Δ < 0 | 0 个 | 不相交 |

---

**例题 1**：判断 $`y = 2x + 1`$ 与 $`y = x² - 3x + 4`$ 的位置关系。

代入得 $`x² - 5x + 3 = 0`$，$`Δ = 25 - 12 = 13 > 0`$，相交于两点。

---

**例题 2**：求 $`k`$ 使 $`y = 3x + k`$ 与 $`y = x² - x + 2`$ 相切。

代入得 $`x² - 4x + (2 - k) = 0`$，$`Δ = 16 - 4(2 - k) = 8 + 4k = 0 ⇒ k = -2`$。

---

**例题 3**：求 $`c`$ 使 $`y = x + c`$ 与 $`y = 2x² - 3x + 1`$ 不相交。

代入得 $`2x² - 4x + (1 - c) = 0`$，$`Δ = 16 - 8(1 - c) = 8 + 8c < 0 ⇒ c < -1`$。

---

**例题 4**：求 $`k`$ 使直线 $`y = 2x + k`$ 与曲线 $`y = x² - 2x + 3`$ 相交于两点。

代入得 $`x² - 4x + (3 - k) = 0`$，$`Δ = 16 - 4(3 - k) = 4 + 4k > 0 ⇒ k > -1`$。

---

**例题 5**：直线 $`y = mx + 1`$ 与曲线 $`y = x² - 3x + 5`$ 相切，求 $`m`$。

代入得 $`x² - (m + 3)x + 4 = 0`$。

$`Δ = (m + 3)² - 16 = 0 ⇒ (m + 3)² = 16 ⇒ m + 3 = ±4 ⇒ m = 1`$ 或 $`m = -7`$。

---

**例题 6**：求 $`c`$ 使直线 $`y = 5x + c`$ 与曲线 $`y = x² + x + 2`$ 相切。

代入得 $`x² + x + 2 = 5x + c ⇒ x² - 4x + (2 - c) = 0`$。

$`Δ = 16 - 4(2 - c) = 8 + 4c = 0 ⇒ c = -2`$。

---

## 3.3 解二次方程

解二次方程 $`ax² + bx + c = 0`$ 有三种核心方法：

| 方法 | 适用场景 | 优点 | 缺点 |
|:---|:---|:---|:---|
| **因式分解法** | 表达式容易分解 | 最快，答案精确 | 并非所有二次式都可分解 |
| **公式法** | 任何情况 | 万能，按部就班 | 计算稍繁，可能含根号 |
| **配方法** | 需要顶点形式时 | 同时得到极值信息 | 步骤稍多 |

---

### 3.3.1 因式分解法

**核心原理**：若 $`A × B = 0`$，则 $`A = 0`$ 或 $`B = 0`$。

**对于 $`x² + bx + c = 0`$（首项系数为 1）**：

找 $`p, q`$ 满足 $`p + q = b`$ 且 $`pq = c`$，则 $`x² + bx + c = (x + p)(x + q)`$。

因为 $`(x + p)(x + q) = x² + (p + q)x + pq`$。

**对于 $`ax² + bx + c = 0`$（首项系数不为 1）**：

找 $`p, q`$ 满足 $`pq = ac`$ 且 $`p + q = b`$，拆分中间项后分组提取公因式。

---

**例题 1**：解 $`x² - 5x + 6 = 0`$。

找 $`p+q=-5`$，$`pq=6`$：$`(-2)+(-3)=-5`$，$`(-2)×(-3)=6`$。

$`(x-2)(x-3)=0 ⇒ x=2`$ 或 $`x=3`$。

---

**例题 2**：解 $`2x² - 5x - 3 = 0`$。

$`ac = 2×(-3) = -6`$，找 $`p+q=-5`$，$`pq=-6`$：$`(-6)+1=-5`$，$`(-6)×1=-6`$。

拆分：$`2x² - 6x + x - 3 = 2x(x-3) + 1(x-3) = (2x+1)(x-3)`$。

$`x = -1/2`$ 或 $`x = 3`$。

---

**例题 3**：解 $`3x² + 2 = 7x`$。

移项：$`3x² - 7x + 2 = 0`$。$`ac=6`$，$`(-6)+(-1)=-7`$，$`(-6)×(-1)=6`$。

$`3x² - 6x - x + 2 = 3x(x-2) - 1(x-2) = (3x-1)(x-2)`$。

$`x = 1/3`$ 或 $`x = 2`$。

---

**例题 4**：解 $`x² + x - 12 = 0`$。

找 $`p+q=1`$，$`pq=-12`$：$`4+(-3)=1`$，$`4×(-3)=-12`$。

$`(x+4)(x-3)=0 ⇒ x=-4`$ 或 $`x=3`$。

---

**例题 5**：解 $`6x² - 5x - 6 = 0`$。

$`ac = 6×(-6) = -36`$，找 $`p+q=-5`$，$`pq=-36`$：$`(-9)+4=-5`$，$`(-9)×4=-36`$。

$`6x² - 9x + 4x - 6 = 3x(2x-3) + 2(2x-3) = (3x+2)(2x-3)`$。

$`x = -2/3`$ 或 $`x = 3/2`$。

---

**例题 6**：解 $`4x² + 4x = 3`$。

移项：$`4x² + 4x - 3 = 0`$。$`ac = 4×(-3) = -12`$，找 $`p+q=4`$，$`pq=-12`$：$`6+(-2)=4`$，$`6×(-2)=-12`$。

$`4x² + 6x - 2x - 3 = 2x(2x+3) - 1(2x+3) = (2x-1)(2x+3)`$。

$`x = 1/2`$ 或 $`x = -3/2`$。

---

### 3.3.2 公式法

**求根公式**：

$`
x = (-b ± √(b² - 4ac)) / (2a)
`$

**完整推导**（配方法）：

$`
ax² + bx + c = 0
x² + (b/a)x + c/a = 0 (两边除以 a)
x² + (b/a)x = -c/a (移项)
x² + (b/a)x + (b/(2a))² = (b/(2a))² - c/a (配方)
(x + b/(2a))² = (b² - 4ac) / (4a²)
x + b/(2a) = ± √(b² - 4ac) / (2a)
x = (-b ± √(b² - 4ac)) / (2a)
`$

---

**例题 1**：解 $`2x² - 3x - 4 = 0`$。

$`a=2, b=-3, c=-4`$。

$`
x = (3 ± √(9 + 32)) / 4 = (3 ± √41) / 4
`$

---

**例题 2**：解 $`x² - 2x - 3 = 0`$。

$`
x = (2 ± √(4 + 12)) / 2 = (2 ± 4) / 2
`$

$`x=3`$ 或 $`x=-1`$。

---

**例题 3**：解 $`4x² - 12x + 9 = 0`$。

$`
x = (12 ± √(144 - 144)) / 8 = 12/8 = 3/2 (重根)
`$

---

**例题 4**：解 $`x² - 3x - 5 = 0`$。

$`
x = (3 ± √(9 + 20)) / 2 = (3 ± √29) / 2
`$

---

**例题 5**：解 $`3x² + 2x - 4 = 0`$。

$`
x = (-2 ± √(4 + 48)) / 6 = (-2 ± √52) / 6 = (-2 ± 2√13) / 6 = (-1 ± √13) / 3
`$

---

**例题 6**：解 $`5x² + 2x - 1 = 0`$。

$`
x = (-2 ± √(4 + 20)) / 10 = (-2 ± √24) / 10 = (-2 ± 2√6) / 10 = (-1 ± √6) / 5
`$

---

### 3.3.3 配方法

配方法将 $`ax² + bx + c`$ 写成 $`a(x + p)² + q`$ 的形式。

**完整推导**：

$`
ax² + bx + c = a(x² + (b/a)x) + c
= a[x² + (b/a)x + (b/(2a))² - (b/(2a))²] + c
= a[(x + b/(2a))² - b²/(4a²)] + c
= a(x + b/(2a))² + (c - b²/(4a))
`$

**最终形式**：

$`
ax² + bx + c = a(x + b/(2a))² + (4ac - b²)/(4a)
`$

---

**例题 1**：用配方法解 $`x² - 6x + 5 = 0`$。

$`
x² - 6x + 5 = 0
x² - 6x = -5
x² - 6x + 9 = -5 + 9 (加 (6/2)² = 9)
(x - 3)² = 4
x - 3 = ±2
x = 5 或 x = 1
`$

---

**例题 2**：用配方法解 $`2x² - 8x + 3 = 0`$。

$`
2x² - 8x + 3 = 0
2(x² - 4x) = -3
2[(x² - 4x + 4) - 4] = -3
2(x - 2)² - 8 = -3
2(x - 2)² = 5
(x - 2)² = 5/2
x = 2 ± √10 / 2
`$

---

**例题 3**：将 $`f(x) = 3x² + 12x + 5`$ 写成 $`a(x + p)² + q`$ 的形式。

$`
f(x) = 3x² + 12x + 5
= 3(x² + 4x) + 5
= 3[(x + 2)² - 4] + 5
= 3(x + 2)² - 12 + 5
= 3(x + 2)² - 7
`$

顶点为 $`(-2, -7)`$，$`a=3>0`$ 开口向上。

---

**例题 4**：用配方法解 $`x² - 8x + 13 = 0`$。

$`
x² - 8x + 13 = 0
x² - 8x = -13
x² - 8x + 16 = -13 + 16 (加 (8/2)² = 16)
(x - 4)² = 3
x - 4 = ±√3
x = 4 ± √3
`$

---

**例题 5**：用配方法解 $`2x² - 8x + 5 = 0`$。

$`
2x² - 8x + 5 = 0
2(x² - 4x) = -5
2[(x - 2)² - 4] = -5
2(x - 2)² - 8 = -5
2(x - 2)² = 3
(x - 2)² = 3/2
x = 2 ± √6 / 2
`$

---

**例题 6**：将 $`f(x) = -2x² + 8x - 5`$ 写成 $`a(x + p)² + q`$ 的形式。

$`
f(x) = -2x² + 8x - 5
= -2(x² - 4x) - 5
= -2[(x - 2)² - 4] - 5
= -2(x - 2)² + 8 - 5
= -2(x - 2)² + 3
`$

顶点为 $`(2, 3)`$，$`a=-2<0`$ 开口向下。

---

### 三法对比——同一个方程用三种方法解

解 $`x² - 4x - 5 = 0`$：

**因式分解法**（最快）：
$`(x - 5)(x + 1) = 0`$，$`x = 5`$ 或 $`x = -1`$。

**公式法**：
$`x = (4 ± √(16 + 20)) / 2 = (4 ± 6) / 2`$，$`x = 5`$ 或 $`x = -1`$。

**配方法**：
$`(x - 2)² = 9`$，$`x - 2 = ±3`$，$`x = 5`$ 或 $`x = -1`$。

三法结果相同，但因式分解法最快捷——**能用分解法时优先用分解法**。

---

## 3.4 配方法与最值

### 3.4.1 用配方法求最值

$`f(x) = a(x + p)² + q`$ 中，由于 $`(x + p)² ≥ 0`$：
- $`a > 0`$：$`f(x) ≥ q`$，最小值 $`q`$ 在 $`x = -p`$ 处。
- $`a < 0`$：$`f(x) ≤ q`$，最大值 $`q`$ 在 $`x = -p`$ 处。

**为什么？** 因为 $`(x + p)²`$ 的最小值是 0（当 $`x = -p`$ 时），此时 $`f(x) = q`$。

---

**例题 1**：求 $`f(x) = x² - 4x + 7`$ 的最小值。

$`f(x) = (x - 2)² + 3`$。$`a=1>0`$，当 $`x=2`$ 时取最小值 $`3`$。

---

**例题 2**：求 $`f(x) = -2x² + 12x - 10`$ 的最大值。

$`f(x) = -2(x - 3)² + 8`$。$`a=-2<0`$，当 $`x=3`$ 时取最大值 $`8`$。

---

**例题 3**：求 $`f(x) = x² - 2x - 3`$ 在 $`[0, 4]`$ 上的最值。

$`f(x) = (x - 1)² - 4`$。顶点 $`(1, -4)`$ 在区间内，最小值 $`-4`$。
$`f(0) = -3`$，$`f(4) = 5`$。最大值 $`5`$。

---

**例题 4**：求 $`f(x) = x² + 6x + 11`$ 的最小值。

$`f(x) = (x + 3)² + 2`$。最小值 $`2`$ 在 $`x = -3`$ 处。

---

**例题 5**：求 $`f(x) = -x² + 4x + 1`$ 的最大值。

$`f(x) = -(x - 2)² + 5`$。最大值 $`5`$ 在 $`x = 2`$ 处。

---

**例题 6**：求 $`f(x) = x² - 4x + 5`$ 在 $`[1, 5]`$ 上的值域。

$`f(x) = (x - 2)² + 1`$，顶点 $`(2, 1)`$ 在区间内。$`f(1)=2`$，$`f(5)=10`$。值域 $`[1, 10]`$。

---

### 3.4.2 画草图

四个关键要素：开口方向、顶点、$`x`$ 轴交点、$`y`$ 轴交点。

---

**例题 1**：$`f(x) = x² - 2x - 3`$。
开口向上，顶点 $`(1, -4)`$，$`x`$ 轴交点 $`(-1,0)`$ 和 $`(3,0)`$，$`y`$ 轴交点 $`(0,-3)`$。

**例题 2**：$`f(x) = -x² + 4x`$。
开口向下，顶点 $`(2, 4)`$，$`x`$ 轴交点 $`(0,0)`$ 和 $`(4,0)`$。

**例题 3**：$`f(x) = x² + 2x + 3`$。
开口向上，顶点 $`(-1, 2)`$，$`Δ < 0`$ 无 $`x`$ 轴交点，$`y`$ 轴交点 $`(0,3)`$。完全在 $`x`$ 轴上方。

**例题 4**：$`f(x) = -2x² + 4x + 6`$。
开口向下，顶点 $`(1, 8)`$，$`x`$ 轴交点 $`(-1,0)`$ 和 $`(3,0)`$，$`y`$ 轴交点 $`(0,6)`$。

---

### 3.4.3 求值域

步骤：配方→判断顶点是否在定义域内→算端点→比较。

**例题 1**：$`f(x) = 2x² - 8x + 3`$ 在 ℝ 上。
$`f(x)=2(x-2)²-5`$，值域 $`[-5, ∞)`$。

**例题 2**：$`f(x) = x² - 2x + 3`$ 在 $`[-1,3]`$ 上。
$`f(x)=(x-1)²+2`$，顶点在区间内，$`f(1)=2`$，$`f(-1)=6`$，$`f(3)=6`$，值域 $`[2,6]`$。

**例题 3**：$`f(x) = x² - 4x + 5`$ 在 $`[3,5]`$ 上。
$`f(x)=(x-2)²+1`$，顶点 $`x=2`$ 不在区间内，函数单调递增，$`f(3)=2`$，$`f(5)=10`$，值域 $`[2,10]`$。

**例题 4**：$`f(x) = -x² + 2x + 3`$ 在 $`[-1,4]`$ 上。
$`f(x)=-(x-1)²+4`$，顶点 $`(1,4)`$ 在区间内。$`f(-1)=0`$，$`f(4)=-5`$。值域 $`[-5,4]`$。

---

## 3.5 二次不等式

### 3.5.1 代数法（符号表法）

**完整步骤**：

**第 1 步**：整理为 $`ax² + bx + c (> / < / ≥ / ≤) 0`$。若 $`a < 0`$，两边乘 $`-1`$ 并反转不等号。

**第 2 步**：解对应方程 $`ax² + bx + c = 0`$，得根 $`x₁ ≤ x₂`$。

**第 3 步**：写出解集。

**符号法则**（$`a > 0`$ 时）：

```
符号：   +      -      +
       ———|—————|—————>
          x₁    x₂
```

- **大于取两边**：$`ax² + bx + c > 0 ⇒ x < x₁`$ 或 $`x > x₂`$
- **小于取中间**：$`ax² + bx + c < 0 ⇒ x₁ < x < x₂`$

**为什么这个符号法则成立？**

因为 $`a > 0`$ 时抛物线开口向上。在两根之间，抛物线在 $`x`$ 轴下方（负值）；在两根之外，抛物线在 $`x`$ 轴上方（正值）。

**特殊情况**：

| Δ 的情况 | $`>0`$ | $`<0`$ |
|:---|:---|:---|
| Δ = 0（a>0） | $`x ≠ x₁`$ | 无解 |
| Δ < 0（a>0） | ℝ（全体实数） | 无解 |

---

**例题 1**：解 $`x² - 5x + 6 > 0`$。

$`(x - 2)(x - 3) > 0`$。根为 $`x = 2`$ 和 $`x = 3`$。$`a = 1 > 0`$。

"大于取两边"：$`x < 2`$ 或 $`x > 3`$。

验证：取 $`x=0`$：$`0-0+6=6 > 0`$ ✓；取 $`x=2.5`$：$`6.25-12.5+6=-0.25 < 0`$ ✓。

---

**例题 2**：解 $`2x² - 5x - 3 ≤ 0`$。

$`(2x + 1)(x - 3) ≤ 0`$。根为 $`x = -1/2`$ 和 $`x = 3`$。$`a = 2 > 0`$。

"小于取中间"（含端点）：$`-1/2 ≤ x ≤ 3`$。

---

**例题 3**：解 $`x² + 2x + 3 > 0`$。

$`Δ = 4 - 12 = -8 < 0`$，$`a = 1 > 0`$。恒正。解集为 ℝ。

---

**例题 4**：解 $`-x² + 3x + 4 > 0`$。

$`a = -1 < 0`$，两边乘 $`-1`$：$`x² - 3x - 4 < 0`$。

$`(x - 4)(x + 1) < 0`$。根为 $`x = -1`$ 和 $`x = 4`$。

"小于取中间"：$`-1 < x < 4`$。

---

**例题 5**：解 $`x² - 4x + 4 ≥ 0`$。

$`(x - 2)² ≥ 0`$。平方数恒 $`≥ 0`$，解集为 ℝ。

---

**例题 6**：解 $`x² - 4x + 4 < 0`$。

$`(x - 2)² < 0`$。平方数不可能小于零，**无解**。

---

### 3.5.2 图像法

画出 $`y = ax² + bx + c`$ 的草图，查看抛物线在 $`x`$ 轴上方的部分（$`> 0`$）或下方的部分（$`< 0`$）。

---

**例题 1**：用图像法解 $`x² - x - 2 < 0`$。

$`y = x² - x - 2`$，$`a=1>0`$，开口向上。$`x`$ 轴交点 $`(-1,0)`$ 和 $`(2,0)`$。

图像在 $`-1 < x < 2`$ 之间位于 $`x`$ 轴下方。解集：$`-1 < x < 2`$。

---

**例题 2**：已知 $`x² - 2kx + 4 > 0`$ 对所有实数 $`x`$ 恒成立，求 $`k`$。

$`a=1>0`$，需 $`Δ < 0`$：$`4k² - 16 < 0 ⇒ k² < 4 ⇒ -2 < k < 2`$。

---

**例题 3**：解 $`x² - 4 ≥ 3x`$。

移项：$`x² - 3x - 4 ≥ 0`$。$`(x - 4)(x + 1) ≥ 0`$。

"大于取两边"（含端点）：$`x ≤ -1`$ 或 $`x ≥ 4`$。

---

**例题 4**：解 $`x² - 3x - 10 > 0`$。

$`(x - 5)(x + 2) > 0`$。$`x < -2`$ 或 $`x > 5`$。

---

**例题 5**：解 $`2x² + 5x - 3 ≤ 0`$。

$`(2x - 1)(x + 3) ≤ 0`$。$`-3 ≤ x ≤ 1/2`$。

---

**例题 6**：解 $`x² - 6x + 9 ≥ 0`$。

$`(x - 3)² ≥ 0`$，解集 ℝ。

---

### 3.5.3 含参数的不等式问题

**例题 1**：求 $`k`$ 使 $`x² + kx + 9 ≥ 0`$ 对一切 $`x`$ 恒成立。

$`a=1>0`$，需 $`Δ ≤ 0`$：$`k² - 36 ≤ 0 ⇒ -6 ≤ k ≤ 6`$。

---

**例题 2**：求 $`k`$ 使 $`kx² + 2x + 3 > 0`$ 对一切 $`x`$ 恒成立。

若 $`k = 0`$：$`2x+3>0`$ 不是恒成立，排除。
若 $`k ≠ 0`$：需 $`k > 0`$ 且 $`Δ = 4 - 12k < 0 ⇒ k > 1/3`$。

所以 $`k > 1/3`$。

---

**例题 3**：求 $`k`$ 使 $`x² - 2kx + 4 > 0`$ 对所有实数 $`x`$ 恒成立。

$`Δ = 4k² - 16 < 0 ⇒ k² < 4 ⇒ -2 < k < 2`$。

---

**例题 4**：求 $`k`$ 使 $`(k-1)x² + 2x + 1 > 0`$ 对一切 $`x`$ 恒成立。

若 $`k-1 = 0`$，即 $`k=1`$：$`2x+1 > 0`$，不是恒成立。
若 $`k-1 ≠ 0`$：需 $`k-1 > 0`$ 且 $`Δ = 4 - 4(k-1) = 8 - 4k < 0 ⇒ k > 2`$。

所以 $`k > 2`$。

---

## 3.6 三次多项式的图像与不等式

### 3.6.1 三个线性因式乘积的图像

$`f(x) = a(x - p)(x - q)(x - r)`$ 的图像特征：

**(1) $`x`$ 轴截距**：$`x = p, q, r`$（三个零点）。

**(2) $`y`$ 轴截距**：$`f(0) = a(-p)(-q)(-r) = -a·pqr`$。

**(3) 端点行为**——取决于最高次项 $`ax³`$：

当 $`|x|`$ 很大时，最高次项 $`ax³`$ 起主导作用：

| a 的符号 | x → -∞ | x → +∞ |
|:---:|:---:|:---:|
| a > 0 | f(x) → -∞（左下） | f(x) → +∞（右上） |
| a < 0 | f(x) → +∞（左上） | f(x) → -∞（右下） |

**(4) 零点处的行为**：
- **单根**（因式次数为 1）：图像**穿过** $`x`$ 轴。
- **二重根**（因式次数为 2）：图像**触及并反弹**（相切于 $`x`$ 轴）。

**画草图步骤**：
1. 标出 $`x`$ 轴截距 $`p, q, r`$。
2. 标出 $`y`$ 轴截距。
3. 根据 $`a`$ 的符号确定两端走向。
4. 用平滑的 S 形曲线连接各点，在单根处穿过，在二重根处反弹。

---

**例题 1**：画出 $`f(x) = (x + 1)(x - 1)(x - 3)`$ 的草图。

$`x`$ 截距：$`x = -1, 1, 3`$（三个单根，全部穿过）。
$`y`$ 截距：$`f(0) = (1)(-1)(-3) = 3`$，过 $`(0, 3)`$。
$`a = 1 > 0`$：左端向下，右端向上。

走向：从第三象限 → 在 $`x = -1`$ 从下到上穿过 → 局部极大值 → 到 $`x = 1`$ 从上到下穿过 → 局部极小值 → 到 $`x = 3`$ 从下到上穿过 → 向右上延伸。

---

**例题 2**：画出 $`f(x) = -(x + 2)(x - 1)(x - 4)`$ 的草图。

$`x`$ 截距：$`x = -2, 1, 4`$。
$`y`$ 截距：$`f(0) = -(2)(-1)(-4) = 8`$，过 $`(0, -8)`$。
$`a = -1 < 0`$：左端向上，右端向下。

走向：从第二象限 → 在 $`x = -2`$ 从上到下穿过 → 局部极小值 → 到 $`x = 1`$ 从下到上穿过 → 局部极大值 → 到 $`x = 4`$ 从上到下穿过 → 向右下延伸。

---

**例题 3**：画出 $`f(x) = (x - 1)²(x + 2)`$ 的草图。

$`x`$ 截距：$`x = -2`$（单根，穿过），$`x = 1`$（二重根，触及反弹）。
$`y`$ 截距：$`f(0) = (1)²(2) = 2`$，过 $`(0, 2)`$。
$`a = 1 > 0`$：左端向下，右端向上。

走向：从第三象限 → 在 $`x = -2`$ 从下到上穿过 → 局部极大值 → 下降到 $`x = 1`$ 处触及 $`x`$ 轴并反弹 → 向右上延伸。

---

**例题 4**：画出 $`f(x) = -(x + 1)(x - 2)²`$ 的草图。

$`x`$ 截距：$`x = -1`$（单根，穿过），$`x = 2`$（二重根，触及反弹）。
$`y`$ 截距：$`f(0) = -(1)(4) = -4`$，过 $`(0, -4)`$。
$`a = -1 < 0`$：左端向上，右端向下。

走向：从第二象限 → 在 $`x = -1`$ 从上到下穿过 → 局部极小值 → 上升到 $`x = 2`$ 处触及 $`x`$ 轴并反弹 → 向右下延伸。

---

**例题 5**：画出 $`f(x) = (x + 3)(x + 1)(x - 2)`$ 的草图。

$`x`$ 截距：$`x = -3, -1, 2`$（三个单根）。
$`y`$ 截距：$`f(0) = (3)(1)(-2) = -6`$，过 $`(0, -6)`$。
$`a = 1 > 0`$：左端向下，右端向上。

---

**例题 6**：画出 $`f(x) = -2(x + 1)(x - 1)(x - 3)`$ 的草图。

$`x`$ 截距：$`x = -1, 1, 3`$。
$`y`$ 截距：$`f(0) = -2(1)(-1)(-3) = -6`$，过 $`(0, -6)`$。
$`a = -2 < 0`$：左端向上，右端向下。

---

### 3.6.2 绝对值图像

**核心规则**（必须牢记）：

$`
y = |f(x)|
`$

- **保留** $`f(x) ≥ 0`$ 的部分（$`x`$ 轴上方的部分**不变**）
- **翻折** $`f(x) < 0`$ 的部分（将 $`x`$ 轴下方的部分**沿 $`x`$ 轴向上对称翻转**）

即：**负数部分翻折到正半轴**。

因此在 $`y = |f(x)|`$ 的图像中：
1. 图像永远在 $`x`$ 轴**上方**（或轴上），不可能有负值。
2. 在 $`f(x) = 0`$ 的零点处，绝对值图像会出现**尖点**（因为从正变负或从负变正时，斜率突然反转方向）。
3. 原函数在 $`x`$ 轴上方的部分保持不变，下方的部分被"折叠"上来。

---

**例题 1**：画出 $`y = |(x + 1)(x - 1)(x - 3)|`$ 的草图。

先分析 $`f(x) = (x + 1)(x - 1)(x - 3)`$ 的符号：

| 区间 | x<-1 | -1<x<1 | 1<x<3 | x>3 |
|:---:|:---:|:---:|:---:|:---:|
| f(x) | - | + | - | + |

将负的部分（$`x < -1`$ 和 $`1 < x < 3`$）沿 $`x`$ 轴向上翻折。

最终图像在 $`x = -1, 1, 3`$ 处与 $`x`$ 轴相交（尖点），其余部分全部在 $`x`$ 轴上方。

---

**例题 2**：画出 $`y = |(x - 1)(x - 2)(x - 4)|`$ 的草图。

分析 $`f(x) = (x - 1)(x - 2)(x - 4)`$ 的符号：

$`f(0) = (-1)(-2)(-4) = -8`$，说明 $`x=0`$ 时 $`f(x) < 0`$。

| 区间 | x<1 | 1<x<2 | 2<x<4 | x>4 |
|:---:|:---:|:---:|:---:|:---:|
| f(x) | - | + | - | + |

将负的区域（$`x<1`$ 和 $`2<x<4`$）沿 $`x`$ 轴向上翻折。尖点在 $`x=1, 2, 4`$。

---

**例题 3**：求 $`|(x + 1)(x - 1)(x - 2)| = 2`$ 的实根个数。

符号分析：$`x<-1`$（-），$`-1<x<1`$（+），$`1<x<2`$（-），$`x>2`$（+）。

做绝对值变换后，$`|f(0)| = |(1)(-1)(-2)| = 2`$，$`(0,2)`$ 是一个交点。

水平线 $`y=2`$ 与图像相交：
- $`x<-1`$：1 个交点
- $`-1<x<1`$：2 个交点
- $`1<x<2`$：2 个交点
- $`x>2`$：1 个交点

总共 **5 个交点**，即方程有 5 个实根。

---

**例题 4**：画出 $`y = |(x + 2)(x - 1)²|`$ 的草图。

$`f(x) = (x + 2)(x - 1)²`$。

符号：$`x<-2`$（-），$`-2<x<1`$（+），$`x>1`$（+）。

将 $`x<-2`$ 的部分沿 $`x`$ 轴向上翻折。在 $`x=-2`$ 处有尖点；在 $`x=1`$ 处是平滑触及（二重根，原函数在此不改变符号，不需要翻折）。

---

**例题 5**：画出 $`y = |(x+1)(x-2)(x-5)|`$ 的草图。

符号：$`x<-1`$（-），$`-1<x<2`$（+），$`2<x<5`$（-），$`x>5`$（+）。

翻折 $`x<-1`$ 和 $`2<x<5`$ 部分。尖点在 $`x=-1, 2, 5`$。

---

**例题 6**：求 $`|(x+2)(x-1)(x-4)|=3`$ 的实根个数（估算）。

图像在 $`x<-2`$ 和 $`1<x<4`$ 部分被翻折上来。水平线 $`y=3`$ 与图像大约有 5 个交点，即约 5 个实根。

---

### 3.6.3 三次不等式的图像解法

对于 $`f(x) ≥ d`$（$`>`, `≤`, `<` 同理），其中 $`f(x) = a(x-p)(x-q)(x-r)`$：

1. 画出 $`y = f(x)`$ 的草图。
2. 画水平线 $`y = d`$。
3. 找出交点横坐标（解 $`f(x) = d`$）。
4. 读取满足不等式的 $`x`$ 区间。

**当 $`d = 0`$ 时**，水平线就是 $`x`$ 轴，问题简化为分析 $`f(x)`$ 的符号。

---

**例题 1**：$`f(x) = (x+1)(x-1)(x-3)`$，解 $`f(x) ≤ 0`$。

符号表：

| 区间 | x<-1 | -1<x<1 | 1<x<3 | x>3 |
|:---:|:---:|:---:|:---:|:---:|
| f(x) | - | + | - | + |

$`f(x) ≤ 0`$ 对应负或零的区间（含端点）：

$`
x ≤ -1 或 1 ≤ x ≤ 3
`$

---

**例题 2**：$`f(x) = -(x+1)(x-2)(x-5)`$，解 $`f(x) > 0`$。

先考虑 $`g(x) = (x+1)(x-2)(x-5)`$，$`a_g = 1 > 0`$。

$`g(x)`$ 符号：$`x<-1`$（-），$`-1<x<2`$（+），$`2<x<5`$（-），$`x>5`$（+）。

$`f(x) = -g(x)`$，符号与 $`g(x)`$ 相反：

| 区间 | x<-1 | -1<x<2 | 2<x<5 | x>5 |
|:---:|:---:|:---:|:---:|:---:|
| f(x) | + | - | + | - |

$`f(x) > 0`$ 对应正区间（不含端点）：

$`
x < -1 或 2 < x < 5
`$

---

**例题 3**：$`f(x) = (x+2)(x-1)(x-4)`$，解 $`f(x) ≥ 8`$。

展开：$`f(x) = x³ - 3x² - 6x + 8`$。

解 $`f(x) = 8`$：

$`
x³ - 3x² - 6x + 8 = 8 ⇒ x³ - 3x² - 6x = 0 ⇒ x(x² - 3x - 6) = 0
`$

$`x = 0`$ 或 $`x = (3 ± √33) / 2`$。$`(3 - √33)/2 ≈ -1.372`$，$`(3 + √33)/2 ≈ 4.372`$。

$`a = 1 > 0`$，左端向下，右端向上。$`f(x) ≥ 8`$ 的解集为：

$`
(3 - √33)/2 ≤ x ≤ 0 或 x ≥ (3 + √33)/2
`$

---

**例题 4**：$`f(x) = (x+1)(x-2)(x-4)`$，解 $`f(x) ≤ 0`$。

符号：$`x<-1`$（-），$`-1<x<2`$（+），$`2<x<4`$（-），$`x>4`$（+）。

$`≤ 0`$：$`x ≤ -1`$ 或 $`2 ≤ x ≤ 4`$。

---

**例题 5**：$`f(x) = -(x+2)(x-1)(x-5)`$，解 $`f(x) ≥ 0`$。

$`f(x) ≥ 0 ⇔ -(x+2)(x-1)(x-5) ≥ 0 ⇔ (x+2)(x-1)(x-5) ≤ 0`$。

符号：$`x<-2`$（-），$`-2<x<1`$（+），$`1<x<5`$（-），$`x>5`$（+）。

$`(x+2)(x-1)(x-5) ≤ 0`$：$`x ≤ -2`$ 或 $`1 ≤ x ≤ 5`$。

---

**例题 6**：解 $`(x+1)(x-1)(x-3) ≥ 4`$。

$`f(x) = (x+1)(x-1)(x-3) = x³ - 3x² - x + 3`$。

$`f(x) = 4 ⇒ x³ - 3x² - x - 1 = 0`$。此三次方程不易因式分解，在实际考试中通常会给出交点坐标或可通过计算器求解。

---

## 3.7 用微分求二次函数的极值

### 3.7.1 微分法的引入

在学习微分之前，我们用配方法求极值。有了微分这个工具后，我们可以用导数来求极值，这为后续学习更复杂函数（三次函数、指数函数、三角函数等）的极值问题打下基础。

**核心思想**：函数在极值点处的切线是水平的，即斜率为零，即**导数为零**。

对于二次函数 $`f(x) = ax² + bx + c`$：

**第 1 步：求导**。

由幂法则 $`d/dx (xⁿ) = n·xⁿ⁻¹`$：

$`
f'(x) = d/dx (ax² + bx + c) = 2ax + b
`$

**推导过程**：
- $`d/dx (ax²) = a·2x = 2ax`$
- $`d/dx (bx) = b·1 = b`$
- $`d/dx (c) = 0`$（常数的导数为零）

**第 2 步：令导数为零，找到驻点**。

$`
2ax + b = 0 ⇒ x = -b/(2a)
`$

这正是我们用配方法得到的顶点横坐标！

**第 3 步：代入原函数求极值**。

$`
f(-b/(2a)) = a(-b/(2a))² + b(-b/(2a)) + c
= a·b²/(4a²) - b²/(2a) + c
= -b²/(4a) + c = (4ac - b²)/(4a)
`$

**第 4 步：用二阶导数判断极值类型**。

二阶导数是一阶导数的导数：

$`
f''(x) = d/dx (2ax + b) = 2a
`$

- $`f''(x) = 2a > 0`$（即 $`a > 0`$）：**最小值**。因为 $`f'' > 0`$ 意味着 $`f'`$ 在增加，$`f'`$ 从负到正经过零，函数先减后增。
- $`f''(x) = 2a < 0`$（即 $`a < 0`$）：**最大值**。因为 $`f'' < 0`$ 意味着 $`f'`$ 在减少，$`f'`$ 从正到负经过零，函数先增后减。

**对照配方法**：

微分法得到 $`x = -b/(2a)`$，配方法得到 $`f(x) = a(x + b/(2a))² + (4ac-b²)/(4a)`$，顶点横坐标都是 $`-b/(2a)`$。两种方法完全等价。

---

**例题 1**：用微分法求 $`f(x) = 2x² - 8x + 5`$ 的极值。

$`f'(x) = 4x - 8`$。令 $`f'(x) = 0`$：$`4x = 8 ⇒ x = 2`$。

$`f''(x) = 4 > 0`$，最小值。

$`f(2) = 8 - 16 + 5 = -3`$。

**验证**（配方法）：$`f(x) = 2(x - 2)² - 3`$，最小值 $`-3`$。✓

---

**例题 2**：用微分法求 $`f(x) = -3x² + 12x - 7`$ 的极值。

$`f'(x) = -6x + 12`$。令 $`f'(x) = 0`$：$`x = 2`$。

$`f''(x) = -6 < 0`$，最大值。

$`f(2) = -12 + 24 - 7 = 5`$。

**验证**（配方法）：$`f(x) = -3(x - 2)² + 5`$，最大值 $`5`$。✓

---

**例题 3**：已知 $`f(x) = x² + kx + 4`$ 在 $`x = 3`$ 处取极值，求 $`k`$ 并判断。

$`f'(x) = 2x + k`$。$`f'(3) = 6 + k = 0 ⇒ k = -6`$。

$`f''(x) = 2 > 0`$，极小值。

$`f(3) = 9 - 18 + 4 = -5`$。

---

**例题 4**：用微分法求 $`f(x) = 5x² - 20x + 3`$ 的极值，与配方法对比。

**微分法**：$`f'(x) = 10x - 20 = 0 ⇒ x = 2`$。$`f''(x) = 10 > 0`$，最小值 $`f(2) = 20 - 40 + 3 = -17`$。

**配方法**：$`f(x) = 5(x-2)² - 17`$，最小值 $`-17`$。两种方法结果一致。✓

**对比**：微分法步骤更直接，不需要配方运算。

---

**例题 5**：用微分法求 $`f(x) = -4x² + 24x - 31`$ 的极值。

$`f'(x) = -8x + 24 = 0 ⇒ x = 3`$。$`f''(x) = -8 < 0`$，最大值 $`f(3) = -36 + 72 - 31 = 5`$。

---

**例题 6**：已知 $`f(x) = 3x² + 2mx + 5`$ 在 $`x = -2`$ 处取极值，求 $`m`$ 和极值。

$`f'(x) = 6x + 2m`$。$`f'(-2) = -12 + 2m = 0 ⇒ m = 6`$。

$`f''(x) = 6 > 0`$，极小值。$`f(-2) = 12 - 24 + 5 = -7`$。

---

**例题 7**：用微分法证明 $`f(x) = x² + 4x + 7`$ 的最小值为 $`3`$。

$`f'(x) = 2x + 4 = 0 ⇒ x = -2`$。$`f''(x) = 2 > 0`$，最小值 $`f(-2) = 4 - 8 + 7 = 3`$。证毕。✓

---

**例题 8**：某矩形花园的周长为 $`40`$ 米，求面积的最大值。

设长为 $`x`$ 米，则宽为 $`20 - x`$ 米。

面积 $`A(x) = x(20 - x) = 20x - x² = -x² + 20x`$。

$`A'(x) = -2x + 20 = 0 ⇒ x = 10`$。$`A''(x) = -2 < 0`$，最大值。

最大面积 $`A(10) = 100`$ 平方米。

---

**例题 9**：求 $`f(x) = 2x² - 12x + 5`$ 的极值，并用配方法验证。

**微分法**：$`f'(x) = 4x - 12 = 0 ⇒ x = 3`$。$`f''(x) = 4 > 0`$，最小值 $`f(3) = 18 - 36 + 5 = -13`$。

**配方法验证**：$`f(x) = 2(x-3)² - 13`$，最小值 $`-13`$。✓

---

**例题 10**：已知 $`f(x) = ax² + 6x + 2`$ 在 $`x = -1`$ 处取极值，求 $`a`$ 和极值。

$`f'(x) = 2ax + 6`$。$`f'(-1) = -2a + 6 = 0 ⇒ a = 3`$。

$`f''(x) = 2a = 6 > 0`$，极小值。$`f(-1) = 3 - 6 + 2 = -1`$。

---

**例题 11**：用微分法求 $`f(x) = 4x² - 16x + 7`$ 的极值。

$`f'(x) = 8x - 16 = 0 ⇒ x = 2`$。$`f''(x) = 8 > 0`$，最小值 $`f(2) = 16 - 32 + 7 = -9`$。

---

**例题 12**：用微分法求 $`f(x) = -5x² + 30x - 41`$ 的极值。

$`f'(x) = -10x + 30 = 0 ⇒ x = 3`$。$`f''(x) = -10 < 0`$，最大值 $`f(3) = -45 + 90 - 41 = 4`$。

---

**例题 13**：已知 $`f(x) = 2x² + kx + 3`$ 在 $`x = -2`$ 处取极值，求 $`k`$ 和极值。

$`f'(x) = 4x + k`$。$`f'(-2) = -8 + k = 0 ⇒ k = 8`$。

$`f''(x) = 4 > 0`$，极小值。$`f(-2) = 8 - 16 + 3 = -5`$。

---

**例题 14**：已知 $`f(x) = -3x² + mx + 1`$ 在 $`x = 1`$ 处取极值，求 $`m`$ 和极值。

$`f'(x) = -6x + m`$。$`f'(1) = -6 + m = 0 ⇒ m = 6`$。

$`f''(x) = -6 < 0`$，最大值。$`f(1) = -3 + 6 + 1 = 4`$。

---

**例题 15**：用微分法和配方法分别求 $`f(x) = 3x² - 18x + 25`$ 的极值，对比两种方法。

**微分法**：$`f'(x) = 6x - 18 = 0 ⇒ x = 3`$。$`f''(x) = 6 > 0`$，最小值 $`f(3) = 27 - 54 + 25 = -2`$。

**配方法**：$`f(x) = 3(x² - 6x) + 25 = 3[(x-3)² - 9] + 25 = 3(x-3)² - 27 + 25 = 3(x-3)² - 2`$。最小值 $`-2`$ 在 $`x=3`$ 处。✓

**对比**：微分法只需三步（求导→令零→二阶导），配方法需要配方运算。对于二次函数两者难度相当，但对于更复杂的函数微分法更具优势。

---

## 本章总结

| 知识点 | 核心内容 |
|:---|:---|
| **余数定理** | $`f(x) ÷ (x-a)`$ 余数为 $`f(a)`$；$`f(x) ÷ (ax+b)`$ 余数为 $`f(-b/a)`$ |
| **因式定理** | $`f(a)=0 ⇔ (x-a)`$ 是因式 |
| **三次式分解流程** | 试根 → 综合除法 → 分解二次式 |
| **综合除法** | 只处理系数的除法格式，比长除法更简洁 |
| **判别式 Δ** | $`Δ = b²-4ac`$；$`Δ>0`$ 两不等根，$`Δ=0`$ 重根，$`Δ<0`$ 无实根 |
| **直线与曲线** | 代入消 y → 二次方程 → Δ 判相交/相切/不相交 |
| **解二次方程三法** | 因式分解（最快）、公式法（万能）、配方法（顶点形式） |
| **最值** | $`a(x+p)²+q`$，$`a>0`$ 最小 q，$`a<0`$ 最大 q |
| **二次不等式** | $`a>0`$ 时"大于取两边，小于取中间" |
| **三次图像** | $`a>0`$ 左下到右上，$`a<0`$ 左上到右下；单根穿过，二重根反弹 |
| **绝对值图像** | 负的部分向上翻折到正半轴，零点处有尖点 |
| **三次不等式** | 画图 → 画水平线 y=d → 读区间 |

---
---

# 第 4 章：函数（线性、三次、指数、对数）

## 考纲对照

本章覆盖 IGCSE 0606 附加数学（2028–2030）考纲中 **Topic 1: Functions** 与 **Topic 6: Logarithmic and exponential functions** 的全部内容。

| 考纲条目 | 对应本节 |
|---------|---------|
| **1.1** 理解函数（含垂直检验线）、定义域、值域、单射（水平检验线）、多对一、反函数、复合函数；解释一个关系式是否为函数 | §4.1, §4.4 |
| **1.2** 求函数的定义域和值域（含反函数和复合函数）；理解定义域限制使反函数/复合函数存在；理解 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$`，`$\text{Range}(gf) \subseteq \text{Range}(g)$` | §4.1, §4.4 |
| **1.3** 识别并使用函数记号：`$f(x)$`、`$f:x \mapsto$`、`$f^{-1}(x)$`、`$fg(x)$`、`$f^2(x)$`（`$f^2$` 不用于三角函数） | §4.1 |
| **1.4** 理解 `$y=f(x)$` 与 `$y=|f(x)|$` 的关系，其中 `$f(x)$` 可以是线性、二次、三次或三角（`$a\sin bx+c$`、`$a\cos bx+c$`、`$a\tan bx+c$` 形式） | §4.5 |
| **1.5** 用文字解释给定函数为什么没有反函数 | §4.4 |
| **1.6** 求单射函数的反函数（如 `$f(x)=e^{2x}$` 的反函数为 `$\frac12\ln x$`） | §4.4 |
| **1.7** 构造并使用复合函数，理解 `$fg$` 一般不等于 `$gf$` | §4.4 |
| **1.8** 用草图表示函数与其反函数关于 `$y=x$` 对称的关系 | §4.4 |
| **6.1** 了解并使用对数与指数函数的性质与图形（含 `$\ln x$` 和 `$e^x$`、渐近线） | §4.3 |
| **6.2** 了解并使用对数运算法则（含换底公式） | §4.3 |
| **6.3** 解 `$a^x = b$` 形式的方程 | §4.3 |

> **学习路径**：§4.1（概念基础）→ §4.2（线性与三次）→ §4.3（指数与对数）→ §4.4（反函数与复合）→ §4.5（绝对值图形）→ 练习题

---

## 4.1 函数的基础概念

### 4.1.1 什么是函数？

函数是一种特殊的对应关系：它将**定义域**中的每一个输入值 `$x$`，对应到**恰好一个**输出值 `$y = f(x)$`。我们称 `$y$` 是 `$x$` 的**像（image）**。

$$`f: D \to \mathbb{R}, \quad x \mapsto f(x)`$$

其中 `$D$` 是定义域，**值域（range / image set）**是所有可能的输出值 `$f(x)$` 的集合。

**函数必须满足的条件**：对于定义域中的每个 `$x$`，必须有且仅有一个 `$y$` 与之对应。这是函数的**垂直检验线法则**——任何竖直线与函数图形最多相交一次。

**如何解释一个关系式是否为函数**（考纲 1.1）：

对于定义域中的每一个 `$x$` 值，关系式只给出唯一的一个 `$y$` 值。

- `$y = x^2$` **是**函数：每个 `$x$` 对应唯一一个 `$y$`。
- `$x = y^2$` **不是**函数：例如 `$x=4$` 时，`$y$` 可以是 `$2$` 或 `$-2$`，一个 `$x$` 对应两个 `$y$`。
- `$y = \pm\sqrt{x}$` **不是**函数：每个正 `$x$` 对应两个 `$y$` 值。

### 4.1.2 单射（一对一）与多对一

- **单射（one–one function / injective）**：定义域中不同的 `$x$` 对应不同的 `$y$`。即：若 `$x_1 \neq x_2$`，则 `$f(x_1) \neq f(x_2)$`。检验方法——**水平检验线法则**：任何水平线与图形最多相交一次。

- **多对一（many–one function）**：不同的 `$x$` 可以对应同一个 `$y$`。例如 `$f(x) = x^2$`，`$f(2) = f(-2) = 4$`。水平检验线 `$y=4$` 与图形相交于两点。

> ⚠️ **只有单射函数才有反函数**。这是因为反函数需要将每个输出值唯一地映射回输入值——如果原函数是多对一的，那么反函数将是一对多，不满足函数的定义。

### 4.1.3 函数记号

IGCSE 0606 中使用的函数记号包括：

- **`$f(x)$`**：最常见的记号，如 `$f(x) = 2e^x$`
- **`$f: x \mapsto$`**：映射记号，如 `$f: x \mapsto \lg x$`，表示"`$f$` 将 `$x$` 映射到 `$\lg x$`"
- **`$f^{-1}(x)$`**：反函数记号
- **`$fg(x)$`**：复合函数记号，`$fg(x) = f(g(x))$`
- **`$f^2(x)$`**：表示 `$f(f(x))$`，即 `$f$` 复合自身

> ⚠️ **重要**：记号 `$f^2(x)$` **不适用于三角函数**。例如 `$\sin^2 x$` 表示 `$(\sin x)^2$`，而不是 `$\sin(\sin x)$`。

### 4.1.4 定义域与值域的求法

求定义域时需要考虑的限制因素：

1. **分母不为零**：`$f(x) = 1/g(x) \Rightarrow g(x) \neq 0$`
2. **偶次根号内非负**：`$f(x) = \sqrt{g(x)} \Rightarrow g(x) \geq 0$`
3. **对数的真数为正**：`$f(x) = \log_a(g(x)) \Rightarrow g(x) > 0$`

**值域的求法**：

**方法一——配方法**（适用于二次函数）：
将 `$f(x) = ax^2 + bx + c$` 写成 `$a(x-h)^2 + k$` 的形式。
- 若 `$a > 0$`，最小值为 `$k$`，值域为 `$[k, \infty)$`
- 若 `$a < 0$`，最大值为 `$k$`，值域为 `$(-\infty, k]$`

**方法二——观察法**（适用于单调函数）：
如果函数在定义域上单调递增或递减，值域就是两端函数值的区间。

**方法三——反函数法**：
若函数有反函数，原函数的值域等于反函数的定义域。

---

### 例题 4.1（定义域与值域）

**例 1**（基础型——根号与分母）：求 `$f(x) = \sqrt{x-2} + 1/(x-3)$` 的定义域。

**解**：

**第一步**（根号条件）：`$x-2 \geq 0 \Rightarrow x \geq 2$`。

**第二步**（分母条件）：`$x-3 \neq 0 \Rightarrow x \neq 3$`。

**第三步**（取交集）：定义域为 `$[2, 3) \cup (3, \infty)$`。

---

**例 2**（复合型——分离常数求值域）：函数 `$f(x) = (2x+1)/(x-1)$`，`$x \neq 1$`。

(i) 求值域；
(ii) 判断 `$f$` 是否为单射。

**解**：

(i) **分离常数**：将分子改写成与分母相关的形式。

$$`f(x) = \frac{2x+1}{x-1} = \frac{2(x-1) + 3}{x-1}`$$

这一步的思路：分母是 `$x-1$`，我们希望分子中也出现 `$x-1$` 以便约分。将 `$2x$` 写成 `$2(x-1) + 2$`，再加上原分子中的 `$1$`，得到 `$2(x-1) + 3$`。

继续化简：

$$`f(x) = \frac{2(x-1)}{x-1} + \frac{3}{x-1} = 2 + \frac{3}{x-1}`$$

因为 `$3/(x-1) \neq 0$` 对一切 `$x \neq 1$` 成立，所以 `$f(x)$` 永远取不到 `$2$`。

**值域为 `$\mathbb{R} \setminus \{2\}$`**。

(ii) 判断单射：设 `$f(a) = f(b)$`，则：

$$`2 + \frac{3}{a-1} = 2 + \frac{3}{b-1}`$$

两边减去 `$2$`：

$$`\frac{3}{a-1} = \frac{3}{b-1}`$$

两边取倒数再乘以 `$3$`：

$$`a-1 = b-1 \Rightarrow a = b`$$

因此 `$f$` 是单射。从图形上看，`$y = 2 + 3/(x-1)$` 是反比例函数经过平移后的结果，每条水平线（除 `$y=2$` 外）恰好相交一次。

---

**例 3**（半圆型值域）：已知 `$f(x) = \sqrt{4 - x^2}$`。

(i) 求定义域和值域；
(ii) 判断 `$f$` 是否有反函数。

**解**：

(i) **定义域**：`$4 - x^2 \geq 0 \Rightarrow x^2 \leq 4 \Rightarrow -2 \leq x \leq 2$`。定义域为 `$[-2, 2]$`。

**值域**：令 `$y = \sqrt{4 - x^2}$`，则 `$y \geq 0$` 且 `$y^2 = 4 - x^2$`，即 `$x^2 + y^2 = 4$`（`$y \geq 0$`）。

这是一个圆心在原点、半径为 `$2$` 的**上半圆**。`$y$` 的最小值出现在 `$x = \pm 2$` 时，`$y = 0$`；最大值出现在 `$x = 0$` 时，`$y = 2$`。因此值域为 `$[0, 2]$`。

(ii) `$f$` 不是单射，因为例如 `$f(-1) = f(1) = \sqrt{3}$`。水平检验线 `$y = \sqrt{3}$` 与上半圆相交于 `$(\pm 1, \sqrt{3})$` 两点。因此 `$f$` 没有反函数。

> 但如果将定义域限制为 `$x \in [0, 2]$`，则 `$f$` 变为单射，此时反函数为 `$f^{-1}(x) = \sqrt{4 - x^2}$`（定义域 `$[0, 2]$`）。

---

**例 4**（对数与分式复合型定义域）：求 `$f(x) = \ln(3x - 6) + 1/\sqrt{8 - 2x}$` 的定义域。

**解**：

**条件 1**（对数真数大于零）：`$3x - 6 > 0 \Rightarrow x > 2$`。

**条件 2**（分母根号内大于零——因为分母不能为零且根号在分母中）：`$8 - 2x > 0 \Rightarrow x < 4$`。

**取交集**：`$x > 2$` 且 `$x < 4$`，即 `$(2, 4)$`。

---

**例 5**（判断关系式是否为函数——考纲 1.1 要求）：判断下列关系式是否为函数，并解释原因。

(i) `$y = \pm \sqrt{x}$`，`$x \geq 0$`
(ii) `$y = x^3 - 2x + 1$`
(iii) `$x^2 + y^2 = 25$`

**解**：

(i) **不是函数**。对于同一个 `$x$`（例如 `$x=4$`），关系式给出两个 `$y$` 值：`$y = 2$` 和 `$y = -2$`。这违反了函数**每个输入对应唯一输出**的定义。垂直检验线 `$x=4$` 与图形相交于两点。

(ii) **是函数**。对于每一个实数 `$x$`，`$x^3 - 2x + 1$` 恰好计算出一个实数 `$y$`。垂直检验线在任何 `$x$` 处都只与图形相交一次。

(iii) **不是函数**。例如 `$x=0$` 时，`$y^2 = 25 \Rightarrow y = \pm 5$`，一个 `$x$` 对应两个 `$y$`。垂直检验线 `$x=0$` 与圆相交于 `$(0,5)$` 和 `$(0,-5)$` 两点。

---

## 4.2 线性函数与三次函数

### 4.2.1 线性函数 `$f(x) = mx + c$`

线性函数是最简单的函数类型。它的图形是一条直线。

**斜率 `$m$` 的几何意义**：`$x$` 每增加 `$1$` 个单位，`$y$` 增加 `$m$` 个单位。如果 `$m > 0$`，直线向右上方倾斜；`$m < 0$`，向右下方倾斜；`$m = 0$`，水平直线。

**截距 `$c$`**：直线与 `$y$` 轴的交点纵坐标，即 `$f(0)$` 的值。

**变化率特征**：线性函数的变化率是**常数**——无论在哪一点上，瞬时变化率都等于 `$m$`。这是线性函数区别于其他函数的最重要特性。

由两点 `$(x_1, y_1)$` 和 `$(x_2, y_2)$` 求斜率：

$$`m = \frac{y_2 - y_1}{x_2 - x_1}`$$

这个公式的推导思路：两点之间的垂直变化是 `$y_2 - y_1$`，水平变化是 `$x_2 - x_1$`，斜率就是"升程除以行程"。

**点斜式方程**：过点 `$(x_0, y_0)$` 且斜率为 `$m$` 的直线方程为：

$$`y - y_0 = m(x - x_0)`$$

### 4.2.2 三次函数 `$f(x) = ax^3 + bx^2 + cx + d$`（`$a \neq 0$`）

三次函数的图形呈 S 形（或反 S 形）。它的导数 `$f'(x) = 3ax^2 + 2bx + c$` 是一个二次函数，可能有两个零点（对应局部极值点），也可能只有一个或没有实数零点。

**三次函数的两种基本形态**：

1. **三个线性因式乘积形式**：`$f(x) = a(x - p)(x - q)(x - r)$`，与 `$x$` 轴交于 `$x = p, q, r$`。
2. **一个线性因式与一个二次因式的乘积**：`$f(x) = (x - p)(Ax^2 + Bx + C)$`，可能只有一个实根。

**端部行为（leading term test）**：
- 若 `$a > 0$`：当 `$x \to -\infty$` 时 `$f(x) \to -\infty$`；当 `$x \to \infty$` 时 `$f(x) \to \infty$`。
- 若 `$a < 0$`：当 `$x \to -\infty$` 时 `$f(x) \to \infty$`；当 `$x \to \infty$` 时 `$f(x) \to -\infty$`。

---

### 例题 4.2（线性与三次函数）

**例 1**（线性函数参数求解）：已知直线 `$L$` 经过点 `$A(2, 5)$` 和 `$B(4, 11)$`。

(i) 求 `$L$` 的方程；
(ii) 求 `$L$` 与 `$x$` 轴和 `$y$` 轴的交点。

**解**：

(i) **求斜率**：

$$`m = \frac{11 - 5}{4 - 2} = \frac{6}{2} = 3`$$

**写方程**：使用点斜式，取 `$A(2, 5)$` 作为已知点：

$$`y - 5 = 3(x - 2)`$$

化简：

$$`y - 5 = 3x - 6 \Rightarrow y = 3x - 1`$$

(ii) **`$y$` 截距**：令 `$x = 0$`，得 `$y = 3(0) - 1 = -1$`，即 `$(0, -1)$`。

**`$x$` 截距**：令 `$y = 0$`，得 `$0 = 3x - 1 \Rightarrow x = 1/3$`，即 `$(1/3, 0)$`。

---

**例 2**（三次函数图形与极值）：已知 `$f(x) = x^3 - 3x^2 + 2$`。

(i) 求 `$f'(x)$` 并解 `$f'(x) = 0$`；
(ii) 判断驻点的类型；
(iii) 求 `$f$` 与坐标轴的交点。

**解**：

(i) **求导**：

$$`f'(x) = \frac{d}{dx}(x^3) - \frac{d}{dx}(3x^2) + \frac{d}{dx}(2) = 3x^2 - 6x`$$

因式分解：`$f'(x) = 3x(x - 2)$`。

令 `$f'(x) = 0$`，得 `$3x(x-2) = 0$`，即 `$x = 0$` 或 `$x = 2$`。

(ii) 用一阶导数符号检验每个驻点附近的单调性变化：

**在 `$x = 0$` 处**：
- 取 `$x = -1$`（左侧）：`$f'(-1) = 3(-1)(-3) = 9 > 0$`，递增
- 取 `$x = 1$`（右侧）：`$f'(1) = 3(1)(-1) = -3 < 0$`，递减
- 从左到右：递增 → 递减 → **局部最大值**。`$f(0) = 2$`。

**在 `$x = 2$` 处**：
- 取 `$x = 1$`（左侧）：`$f'(1) = -3 < 0$`，递减
- 取 `$x = 3$`（右侧）：`$f'(3) = 3(3)(1) = 9 > 0$`，递增
- 从左到右：递减 → 递增 → **局部最小值**。`$f(2) = 8 - 12 + 2 = -2$`。

(iii) **`$y$` 截距**：`$f(0) = 2$`，即 `$(0, 2)$`。

**`$x$` 截距**：解 `$x^3 - 3x^2 + 2 = 0$`。

尝试有理根。检验 `$x = 1$`：`$f(1) = 1 - 3 + 2 = 0$`，所以 `$x-1$` 是因子。

用综合除法分解：

$$`\begin{array}{c|ccc}
1 & 1 & -3 & 0 & 2 \\
  &   & 1  & -2 & -2 \\
\hline
  & 1 & -2 & -2 & 0
\end{array}`$$

商为 `$x^2 - 2x - 2$`。用二次公式求根：

$$`x = \frac{2 \pm \sqrt{4 - 4(1)(-2)}}{2(1)} = \frac{2 \pm \sqrt{4 + 8}}{2} = \frac{2 \pm \sqrt{12}}{2} = \frac{2 \pm 2\sqrt{3}}{2} = 1 \pm \sqrt{3}`$$

三个 `$x$` 截距：`$x = 1$`，`$x = 1 + \sqrt{3}$`，`$x = 1 - \sqrt{3}$`。

---

**例 3**（三次函数的符号分析与不等式）：已知 `$f(x) = (x+2)(x-1)(x-3)$`。

(i) 求 `$f(x) = 0$` 的解；
(ii) 解 `$f(x) > 0$`；
(iii) 画出 `$y = f(x)$` 的草图。

**解**：

(i) 三个线性因子分别等于零：`$x = -2$`，`$x = 1$`，`$x = 3$`。

(ii) 将实轴按三个零点分为四个区间。在每个区间内取一个测试点，判断每个因子的符号，三个因子符号的乘积即为 `$f(x)$` 的符号：

| 区间 | 测试点 | `$(x+2)$` | `$(x-1)$` | `$(x-3)$` | 乘积符号 |
|------|--------|---------|---------|---------|---------|
| `$x < -2$` | `$x = -3$` | `$-1$`（负） | `$-4$`（负） | `$-6$`（负） | 负×负×负 = **负** |
| `$-2 < x < 1$` | `$x = 0$` | `$2$`（正） | `$-1$`（负） | `$-3$`（负） | 正×负×负 = **正** |
| `$1 < x < 3$` | `$x = 2$` | `$4$`（正） | `$1$`（正） | `$-1$`（负） | 正×正×负 = **负** |
| `$x > 3$` | `$x = 4$` | `$6$`（正） | `$3$`（正） | `$1$`（正） | 正×正×正 = **正** |

因此 `$f(x) > 0$` 的解为 `$-2 < x < 1$` 或 `$x > 3$`。

(iii) 草图要点：
- `$x$` 截距：`$(-2, 0)$`，`$(1, 0)$`，`$(3, 0)$`
- `$y$` 截距：`$f(0) = (2)(-1)(-3) = 6$`
- 端部行为：`$a = 1 > 0$`，所以 `$x \to -\infty$` 时 `$f \to -\infty$`，`$x \to \infty$` 时 `$f \to \infty$`
- 图形在三个零点之间依次穿越 `$x$` 轴

---

**例 4**（因式定理与三次函数分解）：已知 `$f(x) = x^3 + kx^2 - 4x - 4$` 能被 `$x+1$` 整除，求 `$k$` 的值，并完全因式分解 `$f(x)$`。

**解**：

**第一步**（应用因式定理）：如果 `$x+1$` 是因子，则 `$f(-1) = 0$`。

$$`f(-1) = (-1)^3 + k(-1)^2 - 4(-1) - 4 = -1 + k + 4 - 4 = k - 1`$$

令 `$f(-1) = 0$`，得 `$k - 1 = 0 \Rightarrow k = 1$`。

**第二步**（综合除法分解）：`$f(x) = x^3 + x^2 - 4x - 4$`。

除以 `$(x+1)$`：

$$`\begin{array}{c|ccc}
-1 & 1 & 1 & -4 & -4 \\
   &   & -1 & 0 & 4 \\
\hline
   & 1 & 0 & -4 & 0
\end{array}`$$

商为 `$x^2 - 4 = (x-2)(x+2)$`。

**第三步**（写出完全因式分解）：

$$`f(x) = (x+1)(x-2)(x+2)`$$

---

## 4.3 指数函数与对数函数

### 4.3.1 指数函数 `$f(x) = a^x$`（`$a > 0$`，`$a \neq 1$`）

指数函数的核心特征是：函数值的增长（或衰减）速度与函数本身的大小成正比。

**图形性质**（以 `$a > 1$` 为例）：
- 定义域：`$\mathbb{R}$`（全体实数）
- 值域：`$(0, \infty)$`（永远取正值）
- 恒过点 `$(0, 1)$`，因为 `$a^0 = 1$`
- 当 `$a > 1$` 时，函数严格递增；当 `$0 < a < 1$` 时，函数严格递减
- **水平渐近线**：`$y = 0$`（`$x$` 轴）。当 `$a > 1$` 时，`$x \to -\infty$` 时趋近于 `$y=0$`；当 `$0 < a < 1$` 时，`$x \to \infty$` 时趋近于 `$y=0$`。

**更一般的形式**：`$y = k e^{nx} + a$`
- 水平渐近线为 `$y = a$`
- `$k$` 控制垂直拉伸和方向（`$k > 0$` 时图形在渐近线上方，`$k < 0$` 时在下方）

**为什么 `$e$` 如此重要？**

$$`\frac{d}{dx}e^x = e^x`$$

`$e^x$` 是唯一**导数等于自身**的函数。如果用一般底数 `$a$`：

$$`\frac{d}{dx}a^x = a^x \ln a`$$

当 `$a = e$` 时，`$\ln e = 1$`，导数简化为 `$e^x$` 自身。

### 4.3.2 自然指数函数 `$e^x$` 的详细推导

**问题**：是否存在一个数 `$e$`，使得 `$e^x$` 的导数等于 `$e^x$`？

从一般指数函数 `$f(x) = a^x$` 出发。根据导数定义：

$$`f'(x) = \lim_{h \to 0} \frac{a^{x+h} - a^x}{h}`$$

**第一步**：提取公因子 `$a^x$`：

$$`f'(x) = \lim_{h \to 0} \frac{a^x(a^h - 1)}{h} = a^x \cdot \lim_{h \to 0} \frac{a^h - 1}{h}`$$

**第二步**：记 `$L(a) = \displaystyle\lim_{h \to 0} \frac{a^h - 1}{h}$`。这个极限依赖于 `$a$`。于是：

$$`f'(x) = a^x \cdot L(a) = f(x) \cdot L(a)`$$

**第三步**：我们希望 `$L(a) = 1$`，这样 `$f'(x) = f(x)$`。数值求解 `$L(a) = 1$`：

| `$a$` | `$L(a) \approx (a^{0.001} - 1)/0.001$` |
|-----|-------------------------------------------|
| `$2$` | `$\approx 0.693$` |
| `$2.7$` | `$\approx 0.993$` |
| `$2.71$` | `$\approx 0.996$` |
| `$2.718$` | `$\approx 0.999$` |
| `$2.71828$` | `$\approx 1.000$` |

这个特殊的数就是 `$e \approx 2.71828$`，它也可以定义为极限：

$$`e = \lim_{n \to \infty} \left(1 + \frac{1}{n}\right)^n`$$

**数值验证**：
- `$n = 1$`：`$(1+1)^1 = 2$`
- `$n = 10$`：`$(1.1)^{10} \approx 2.594$`
- `$n = 100$`：`$(1.01)^{100} \approx 2.705$`
- `$n = 10000$`：`$(1.0001)^{10000} \approx 2.718$`

### 4.3.3 对数函数 `$f(x) = \log_a x$`（`$a > 0$`，`$a \neq 1$`）

对数函数是指数函数的**反函数**：

$$`y = \log_a x \iff a^y = x \quad (x > 0)`$$

**图形性质**（以 `$a > 1$` 为例）：
- 定义域：`$(0, \infty)$`（真数必须为正）
- 值域：`$\mathbb{R}$`
- 恒过点 `$(1, 0)$`，因为 `$\log_a 1 = 0$`
- 当 `$a > 1$` 时，函数严格递增；当 `$0 < a < 1$` 时，函数严格递减
- **垂直渐近线**：`$x = 0$`（`$y$` 轴）。当 `$a > 1$` 时，`$x \to 0^+$` 时 `$\log_a x \to -\infty$`

**更一般的形式**：`$y = k \ln(ax + b)$`
- 垂直渐近线在 `$ax + b = 0$` 处，即 `$x = -b/a$`
- 定义域为 `$x > -b/a$`

**两个重要的特殊底数**：
- **常用对数**（以 `$10$` 为底）：`$\log_{10} x$`，有时记作 `$\lg x$`
- **自然对数**（以 `$e$` 为底）：`$\log_e x$`，记作 `$\ln x$`

**指数函数与对数函数互为反函数**：

$$`y = a^x \quad \stackrel{\text{反函数}}{\longleftrightarrow} \quad y = \log_a x`$$

**对称性推导**：
假设点 `$(p, q)$` 在 `$y = a^x$` 上，则 `$q = a^p$`。
由对数定义，`$p = \log_a q$`，所以点 `$(q, p)$` 在 `$y = \log_a x$` 上。
点 `$(p, q)$` 关于直线 `$y = x$` 的对称点正是 `$(q, p)$`（因为交换坐标）。
因此两个函数的图形关于 `$y = x$` 对称。

### 4.3.4 对数运算法则的详细推导

对数法则的本质：将对数运算**降级**——乘法变为加法，除法变为减法，指数变为系数。

**法则 1——积的对数**：`$\log_a (MN) = \log_a M + \log_a N$`

*推导*：
设 `$\log_a M = x$`，`$\log_a N = y$`。
由对数定义：`$a^x = M$`，`$a^y = N$`。
那么 `$MN = a^x \cdot a^y = a^{x+y}$`。
再由对数定义：`$\log_a (MN) = x + y = \log_a M + \log_a N$`。□

**法则 2——商的对数**：`$\log_a (M/N) = \log_a M - \log_a N$`

*推导*：
设 `$\log_a M = x$`，`$\log_a N = y$`。
则 `$M/N = a^x / a^y = a^{x-y}$`。
因此 `$\log_a (M/N) = x - y = \log_a M - \log_a N$`。□

**法则 3——幂的对数**：`$\log_a (M^k) = k \log_a M$`

*推导*：
设 `$\log_a M = x$`，则 `$a^x = M$`。
于是 `$M^k = (a^x)^k = a^{kx}$`。
由定义：`$\log_a (M^k) = kx = k \log_a M$`。□

**法则 4——换底公式**：`$\log_a M = (\log_b M)/(\log_b a)$`

*推导*：
设 `$y = \log_a M$`，则 `$a^y = M$`。
两边取以 `$b$` 为底的对数：
`$\log_b (a^y) = \log_b M$`
`$y \log_b a = \log_b M$`
`$y = (\log_b M)/(\log_b a)$`
因此 `$\log_a M = (\log_b M)/(\log_b a)$`。□

换底公式最常见的两种应用形式：

$$`\log_a M = \frac{\log M}{\log a} \quad \text{（换为常用对数）},
\qquad
\log_a M = \frac{\ln M}{\ln a} \quad \text{（换为自然对数）}`$$

**逆恒等式**（直接来自反函数定义）：

$$`a^{\log_a x} = x \quad (x > 0), \qquad \log_a (a^x) = x \quad (x \in \mathbb{R})`$$

### 4.3.5 解指数方程 `$a^x = b$`

**基本方法**：两边取对数，利用幂法则将对数中的指数"降"为系数。

$$`a^x = b \quad \Rightarrow \quad \ln(a^x) = \ln b \quad \Rightarrow \quad x \ln a = \ln b \quad \Rightarrow \quad x = \frac{\ln b}{\ln a}`$$

也可以用常用对数：`$x = (\log b)/(\log a)$`。

> ⚠️ **注意**：只有当 `$b > 0$` 时方程才有实数解。若 `$b \leq 0$`，方程 `$a^x = b$` 无实数解（因为指数函数的值域为 `$(0, \infty)$`）。

---

### 例题 4.3（指数与对数函数）

**例 1**（对数化简与求值）：化简 `$\log_2 24 - \log_2 3 + \log_2 (1/2)$`。

**解**：

**第一步**（用商法则合并前两项）：

$$`\log_2 24 - \log_2 3 = \log_2 \frac{24}{3} = \log_2 8`$$

**第二步**（用积法则继续合并）：

$$`\log_2 8 + \log_2 \frac{1}{2} = \log_2 \left(8 \times \frac{1}{2}\right) = \log_2 4`$$

**第三步**（求值）：`$\log_2 4 = 2$`，因为 `$2^2 = 4$`。

所以表达式的值为 `$2$`。

---

**例 2**（换底公式与对数方程）：

(i) 用自然对数表示 `$\log_5 20$` 并化简；
(ii) 解方程 `$\log_2 (x+1) + \log_2 (x-1) = 3$`。

**解**：

(i) 用换底公式：

$$`\log_5 20 = \frac{\ln 20}{\ln 5}`$$

分解 `$20 = 4 \times 5$`，所以 `$\ln 20 = \ln(4 \times 5) = \ln 4 + \ln 5 = \ln(2^2) + \ln 5 = 2\ln 2 + \ln 5$`。

因此：

$$`\log_5 20 = \frac{2\ln 2 + \ln 5}{\ln 5} = \frac{2\ln 2}{\ln 5} + 1`$$

(ii) **第一步**（合并对数）：

$$`\log_2[(x+1)(x-1)] = 3 \Rightarrow \log_2(x^2 - 1) = 3`$$

**第二步**（去对数）：两边取以 `$2$` 为底的指数：

$$`x^2 - 1 = 2^3 = 8`$$

**第三步**（解二次方程）：

$$`x^2 = 9 \Rightarrow x = \pm 3`$$

**第四步**（检查定义域）：对数的真数必须为正。

`$x+1 > 0 \Rightarrow x > -1$`，`$x-1 > 0 \Rightarrow x > 1$`。取交集得 `$x > 1$`。

`$x = 3 > 1$`，有效。`$x = -3$` 不满足 `$x > 1$`，舍去。

因此 `$x = 3$`。

---

**例 3**（解指数方程与指数—对数复合）：

(i) 解 `$3^{2x-1} = 7$`，结果用自然对数表示；
(ii) 已知 `$f(x) = \ln(e^{2x} + 1)$`，求 `$f(0)$`。

**解**：

(i) 两边取自然对数：

$$`\ln(3^{2x-1}) = \ln 7`$$

用幂法则将对数中的指数"降"下来：

$$`(2x - 1)\ln 3 = \ln 7`$$

解出 `$x$`：

$$`2x - 1 = \frac{\ln 7}{\ln 3} \Rightarrow 2x = 1 + \frac{\ln 7}{\ln 3} \Rightarrow x = \frac{1}{2}\left(1 + \frac{\ln 7}{\ln 3}\right)`$$

(ii) 代入 `$x = 0$`：

$$`f(0) = \ln(e^{0} + 1) = \ln(1 + 1) = \ln 2`$$

---

**例 4**（指数方程——代换法）：解方程 `$4^x - 5 \cdot 2^x + 4 = 0$`。

**解**：

**第一步**（识别结构）：注意到 `$4^x = (2^2)^x = 2^{2x} = (2^x)^2$`。

**第二步**（换元）：令 `$u = 2^x$`，注意 `$u > 0$`（因为指数函数的值域为正）。

方程化为：

$$`u^2 - 5u + 4 = 0`$$

**第三步**（解二次方程）：

$$`(u-1)(u-4) = 0 \Rightarrow u = 1 \text{ 或 } u = 4`$$

**第四步**（回代）：

- `$2^x = 1 \Rightarrow x = \log_2 1 = 0$`
- `$2^x = 4 \Rightarrow x = \log_2 4 = 2$`

两个解都满足定义域，所以 `$x = 0$` 或 `$x = 2$`。

---

**例 5**（对数方程——换底后化为一元二次）：解方程 `$\log_3 x + \log_x 3 = 2$`。

**解**：

**第一步**（换底）：用换底公式将 `$\log_x 3$` 转换为以 `$3$` 为底：

$$`\log_x 3 = \frac{\log_3 3}{\log_3 x} = \frac{1}{\log_3 x}`$$

**第二步**（换元）：设 `$u = \log_3 x$`（注意 `$x > 0$` 且 `$x \neq 1$`，否则 `$\log_x 3$` 无定义）。

方程化为：

$$`u + \frac{1}{u} = 2`$$

**第三步**（去分母）：两边乘以 `$u$`（`$u \neq 0$`）：

$$`u^2 + 1 = 2u \Rightarrow u^2 - 2u + 1 = 0 \Rightarrow (u-1)^2 = 0`$$

**第四步**（回代）：`$u = 1 \Rightarrow \log_3 x = 1 \Rightarrow x = 3^1 = 3$`。

**验证**：`$\log_3 3 = 1$`，`$\log_3 3 = 1$`，`$1 + 1 = 2$`。✓

---

**例 6**（指数不等式——两边取对数）：解不等式 `$2^x < 5$`。

**解**：

两边取自然对数。因为 `$\ln$` 是递增函数，不等号方向**不变**：

$$`\ln(2^x) < \ln 5`$$

用幂法则：

$$`x \ln 2 < \ln 5`$$

因为 `$\ln 2 > 0$`，两边除以 `$\ln 2$` 不等号方向不变：

$$`x < \frac{\ln 5}{\ln 2}`$$

所以解为 `$x < (\ln 5)/(\ln 2)$`（约为 `$x < 2.322$`）。

> ⚠️ **注意**：如果底数 `$a$` 满足 `$0 < a < 1$`，取对数时不等号会反转（因为 `$\ln a < 0$`）。例如解 `$(1/2)^x < 3$` 时：
>
> `$x\ln(1/2) < \ln 3$`，因为 `$\ln(1/2) < 0$`，两边除以它得到 `$x > (\ln 3)/(\ln(1/2))$`。

---

**例 7**（用已知对数表示未知对数）：已知 `$\log_a 2 = p$`，`$\log_a 5 = q$`。用 `$p$` 和 `$q$` 表示：

(i) `$\log_a 20$`；
(ii) `$\log_a 0.4$`。

**解**：

(i) `$20 = 2^2 \times 5$`，所以：

$$`\log_a 20 = \log_a (2^2 \times 5) = \log_a 2^2 + \log_a 5 = 2\log_a 2 + \log_a 5 = 2p + q`$$

(ii) `$0.4 = 2/5$`，所以：

$$`\log_a 0.4 = \log_a \frac{2}{5} = \log_a 2 - \log_a 5 = p - q`$$

---

**例 8**（指数函数图形的渐近线与变换——考纲 6.1）：函数 `$f(x) = 3e^{2x} - 4$`。

(i) 求 `$f(x)$` 的水平渐近线；
(ii) 求 `$f(x)$` 与坐标轴的交点；
(iii) 描述 `$f$` 的定义域和值域。

**解**：

(i) 当 `$x \to -\infty$` 时，`$e^{2x} \to 0$`，所以 `$3e^{2x} - 4 \to -4$`。

因此水平渐近线为 `$y = -4$`。

(ii) **`$y$` 截距**：令 `$x = 0$`，`$f(0) = 3e^{0} - 4 = 3 - 4 = -1$`，即 `$(0, -1)$`。

**`$x$` 截距**：令 `$f(x) = 0$`：

$$`3e^{2x} - 4 = 0 \Rightarrow e^{2x} = \frac{4}{3}`$$

两边取自然对数：`$2x = \ln(4/3) \Rightarrow x = (1/2)\ln(4/3)$`。

`$x$` 截距为 `$((1/2)\ln(4/3), 0)$`。

(iii) **定义域**：`$\mathbb{R}$`（全体实数）。

**值域**：因为 `$e^{2x} > 0$` 对所有实数 `$x$` 成立，所以 `$3e^{2x} > 0$`，`$3e^{2x} - 4 > -4$`。值域为 `$(-4, \infty)$`。

---

**例 9**（对数函数图形的渐近线——考纲 6.1）：函数 `$g(x) = 2\ln(x-1) + 3$`。

(i) 求 `$g(x)$` 的定义域和垂直渐近线；
(ii) 求 `$g(x)$` 与坐标轴的交点。

**解**：

(i) **定义域**：真数必须为正，`$x-1 > 0 \Rightarrow x > 1$`。定义域为 `$(1, \infty)$`。

**垂直渐近线**：真数为零处 `$x-1 = 0 \Rightarrow x = 1$`。当 `$x \to 1^+$` 时，`$\ln(x-1) \to -\infty$`，`$g(x) \to -\infty$`。

因此垂直渐近线方程为 `$x = 1$`。

(ii) **`$y$` 截距**：`$x = 0$` 不在定义域内，所以没有 `$y$` 截距。

**`$x$` 截距**：令 `$g(x) = 0$`：

$$`2\ln(x-1) + 3 = 0 \Rightarrow \ln(x-1) = -\frac{3}{2}`$$

两边取指数：`$x - 1 = e^{-3/2} \Rightarrow x = 1 + e^{-3/2}$`。

`$x$` 截距为 `$(1 + e^{-3/2}, 0)$`（约为 `$(1.223, 0)$`）。

---

## 4.4 反函数与复合函数

### 4.4.1 反函数

**定义**：若函数 `$f$` 是单射（一一对应），则存在反函数 `$f^{-1}$`，满足：

$$`f^{-1}(y) = x \iff f(x) = y`$$

**核心性质**：

$$`f^{-1}(f(x)) = x, \qquad f(f^{-1}(x)) = x`$$

**几何意义**：`$y = f(x)$` 与 `$y = f^{-1}(x)$` 的图形关于直线 `$y = x$` 对称。

**对称性的详细推导**：

设点 `$(a, b)$` 在 `$y = f(x)$` 上，即 `$b = f(a)$`。
由反函数定义，`$a = f^{-1}(b)$`，所以点 `$(b, a)$` 在 `$y = f^{-1}(x)$` 上。
点 `$(a, b)$` 关于直线 `$y = x$` 的对称点正是 `$(b, a)$`（因为交换坐标）。
因此 `$f$` 和 `$f^{-1}$` 的图形互为关于 `$y = x$` 的镜像。

**举例**：对于 `$f(x) = 2^x$` 和 `$f^{-1}(x) = \log_2 x$`：
- `$f$` 过 `$(0, 1)$`，`$(1, 2)$`，`$(2, 4)$`
- `$f^{-1}$` 过 `$(1, 0)$`，`$(2, 1)$`，`$(4, 2)$`
- 每一对点关于 `$y = x$` 对称

**为什么只有单射函数才有反函数？**

如果 `$f$` 不是单射（即存在 `$x_1 \neq x_2$` 使得 `$f(x_1) = f(x_2) = y_0$`），那么反函数 `$f^{-1}$` 在 `$x = y_0$` 处需要返回两个不同的值——`$x_1$` 和 `$x_2$`——这违反了函数的定义。因此非单射函数没有反函数。

**如何用文字解释函数没有反函数**（考纲 1.5 标准表述）：

"函数 `$f$` 不是单射（one–one），因为存在不同的 `$x$` 值对应同一个 `$y$` 值。例如 `$f(a) = f(b)$` 但 `$a \neq b$`。水平检验线 `$y = f(a)$` 与 `$y = f(x)$` 的图形相交于多于一个点。因此 `$f$` 没有反函数。"

**求反函数的步骤**：
1. 将 `$y = f(x)$` 写成等式。
2. 解出 `$x$`，用 `$y$` 表示。
3. 交换 `$x$` 和 `$y$` 的角色，得到 `$y = f^{-1}(x)$` 的表达式。
4. 注明反函数的定义域（即原函数的值域）。

**原函数与反函数定义域和值域的关系**：
- `$f$` 的定义域 = `$f^{-1}$` 的值域
- `$f$` 的值域 = `$f^{-1}$` 的定义域

> ⚠️ **关键**：有时原函数的定义域需要被限制（restrict）才能使 `$f$` 成为单射，从而存在反函数。例如 `$f(x) = x^2$` 在整个 `$\mathbb{R}$` 上不是单射，但限制为 `$x \geq 0$` 后，变为单射，反函数为 `$\sqrt{x}$`。

### 4.4.2 复合函数

**定义**：`$(f \circ g)(x) = f(g(x))$`，读作"`$f$` 复合 `$g$`"，也写作 `$fg(x)$`。

**复合顺序**：先应用 `$g$`，再将 `$f$` 应用于结果。顺序通常不可交换——一般 `$f(g(x)) \neq g(f(x))$`。

**复合函数的定义域**：需要满足两个条件：
1. `$x$` 在 `$g$` 的定义域内
2. `$g(x)$` 在 `$f$` 的定义域内

**重要关系**（考纲 1.2）：

$$`\text{Domain}(gf) \subseteq \text{Domain}(f), \qquad \text{Range}(gf) \subseteq \text{Range}(g)`$$

> 解释：要计算 `$gf(x) = g(f(x))$`，首先 `$x$` 必须在 `$f$` 的定义域内，且 `$f(x)$` 必须在 `$g$` 的定义域内。因此 `$gf$` 的定义域是 `$f$` 的定义域的子集。`$gf$` 的输出来自 `$g$`，所以其值域是 `$g$` 的值域的子集。

**定义域限制使复合函数存在**的进一步说明：

有时 `$f$` 的定义域需要被限制，才能使 `$gf$` 存在。这是因为 `$f(x)$` 必须落在 `$g$` 的定义域内。

*示例*：已知 `$f(x) = x^2$`（定义域 `$\mathbb{R}$`），`$g(x) = \sqrt{1 - x^2}$`（定义域 `$|x| \leq 1$`）。

`$gf(x) = g(f(x)) = \sqrt{1 - (x^2)^2} = \sqrt{1 - x^4}$`。

`$gf$` 的定义域要求 `$1 - x^4 \geq 0 \Rightarrow |x| \leq 1$`。

所以 `$\text{Domain}(gf) = [-1, 1] \subset \mathbb{R} = \text{Domain}(f)$`。

---

### 例题 4.4（反函数与复合函数）

**例 1**（求反函数——线性函数）：求 `$f(x) = (2x - 3)/5$` 的反函数。

**解**：

**第一步**（写等式）：`$y = (2x - 3)/5$`

**第二步**（解出 `$x$`）：

$$`5y = 2x - 3 \Rightarrow 2x = 5y + 3 \Rightarrow x = \frac{5y + 3}{2}`$$

**第三步**（交换 `$x$` 和 `$y$`）：

$$`f^{-1}(x) = \frac{5x + 3}{2}`$$

**第四步**（定义域）：反函数的定义域为 `$\mathbb{R}$`（原函数的值域为 `$\mathbb{R}$`）。

**验证**：

$$`f(f^{-1}(x)) = f\left(\frac{5x+3}{2}\right) = \frac{2\cdot\frac{5x+3}{2} - 3}{5} = \frac{5x+3-3}{5} = x`$$

---

**例 2**（求反函数——含定义域限制）：已知 `$f(x) = x^2 - 4x + 3$`，`$x \geq 2$`。求 `$f^{-1}(x)$` 及其定义域。

**解**：

**第一步**（配方法找顶点和值域）：

$$`f(x) = x^2 - 4x + 3 = (x^2 - 4x + 4) - 4 + 3 = (x-2)^2 - 1`$$

顶点在 `$(2, -1)$`。因为 `$a = 1 > 0$`，且定义域为 `$x \geq 2$`（在顶点右侧），函数单调递增。值域为 `$[-1, \infty)$`。

**第二步**（解出 `$x$`）：令 `$y = (x-2)^2 - 1$`。

$$`y + 1 = (x-2)^2 \Rightarrow x - 2 = \pm \sqrt{y+1}`$$

因为 `$x \geq 2$`，取正号：`$x = 2 + \sqrt{y+1}$`。

**第三步**（交换 `$x$` 和 `$y$`）：

$$`f^{-1}(x) = 2 + \sqrt{x+1}`$$

**第四步**（定义域）：反函数的定义域是原函数的值域，即 `$[-1, \infty)$`。

**验证**：

$$`f(f^{-1}(x)) = f(2 + \sqrt{x+1}) = (2+\sqrt{x+1} - 2)^2 - 1 = (x+1) - 1 = x`$$

---

**例 3**（复合函数及其定义域——详细推导）：已知 `$f(x) = \sqrt{x-1}$`，`$g(x) = 1/(x-2)$`。

(i) 求 `$fg(x)$` 及其定义域；
(ii) 求 `$gf(x)$` 及其定义域。

**解**：

(i) `$fg(x) = f(g(x)) = \sqrt{1/(x-2) - 1}$`

**定义域分析**：

**条件 1**：`$g(x)$` 有定义。分母不为零：`$x \neq 2$`。

**条件 2**：`$g(x)$` 必须落在 `$f$` 的定义域内。`$f$` 的定义域为 `$\sqrt{\text{内}} \geq 0$`，即：

$$`\frac{1}{x-2} - 1 \geq 0`$$

解这个不等式。这是关键步骤，分情况讨论：

*情况 A*：`$x > 2$`。两边乘以正数 `$(x-2)$`，不等号不变：

$$`1 \geq x - 2 \Rightarrow x \leq 3`$$

结合 `$x > 2$`，得 `$2 < x \leq 3$`。

*情况 B*：`$x < 2$`。两边乘以负数 `$(x-2)$`，不等号反转：

$$`1 \leq x - 2 \Rightarrow x \geq 3`$$

结合 `$x < 2$`，无解。

因此条件 2 给出 `$2 < x \leq 3$`。

**取交集**：`$x \neq 2$` 且 `$2 < x \leq 3$`，定义域为 `$(2, 3]$`。

**化简表达式**：

$$`\frac{1}{x-2} - 1 = \frac{1 - (x-2)}{x-2} = \frac{3 - x}{x-2}`$$

因此：

$$`fg(x) = \sqrt{\frac{3 - x}{x - 2}}, \quad x \in (2, 3]`$$

(ii) `$gf(x) = g(f(x)) = 1/(\sqrt{x-1} - 2)$`

**定义域分析**：

**条件 1**：`$f(x)$` 有定义。根号内非负：`$x - 1 \geq 0 \Rightarrow x \geq 1$`。

**条件 2**：`$f(x)$` 必须落在 `$g$` 的定义域内。`$g$` 的分母不能为零：`$\sqrt{x-1} - 2 \neq 0$`。

$$`\sqrt{x-1} \neq 2 \Rightarrow x-1 \neq 4 \Rightarrow x \neq 5`$$

**取交集**：`$x \geq 1$` 且 `$x \neq 5$`，定义域为 `$[1, 5) \cup (5, \infty)$`。

---

**例 4**（复合函数的值域与 `$fg \neq gf$`）：`$f(x) = 2^x$`，`$g(x) = x^2$`。求 `$fg(x)$` 和 `$gf(x)$` 的表达式，并求 `$fg(x)$` 的值域。

**解**：

**`$fg(x)$`**：`$fg(x) = f(g(x)) = 2^{x^2}$`

**`$gf(x)$`**：`$gf(x) = g(f(x)) = (2^x)^2 = 2^{2x} = 4^x$`

注意 `$fg(x) = 2^{x^2} \neq 4^x = gf(x)$`，说明复合顺序重要。

**求 `$fg(x)$` 的值域**：
- `$g(x) = x^2$` 的值域为 `$[0, \infty)$`
- `$f(u) = 2^u$` 在 `$[0, \infty)$` 上单调递增
- 最小值：`$u = 0$` 时 `$2^0 = 1$`
- 上界：`$u \to \infty$` 时 `$2^u \to \infty$`

因此 `$fg(x)$` 的值域为 `$[1, \infty)$`。

---

**例 5**（用文字解释函数为何没有反函数——考纲 1.5）：

(i) `$f(x) = x^2$`，定义域 `$\mathbb{R}$`。
(ii) `$f(x) = \cos x$`，定义域 `$[0, 2\pi]$`。

**解**：

(i) `$f(x) = x^2$` **不是单射**，因为存在不同的 `$x$` 值对应同一个 `$y$` 值。例如 `$f(2) = 4$` 且 `$f(-2) = 4$`，即 `$2 \neq -2$` 但 `$f(2) = f(-2)$`。水平检验线 `$y = 4$` 与 `$y = x^2$` 的图形相交于 `$(2, 4)$` 和 `$(-2, 4)$` 两点。因此 `$f$` 没有反函数。

> 但若将定义域限制为 `$x \geq 0$`，则 `$f$` 变为单射，反函数为 `$f^{-1}(x) = \sqrt{x}$`。

(ii) `$f(x) = \cos x$` 在 `$[0, 2\pi]$` 上**不是单射**。例如 `$\cos(\pi/3) = 1/2$` 且 `$\cos(5\pi/3) = 1/2$`，但 `$\pi/3 \neq 5\pi/3$`。水平检验线 `$y = 1/2$` 与图形相交于两点。因此 `$f$` 没有反函数。

> 但若将定义域限制为 `$[0, \pi]$`，`$\cos x$` 严格递减，此时反函数为 `$\arccos x$`。

---

**例 6**（草图表示函数与其反函数关于 `$y=x$` 对称——考纲 1.8）：已知 `$f(x) = e^x$`。

(i) 求 `$f^{-1}(x)$`；
(ii) 描述 `$y = f(x)$`，`$y = f^{-1}(x)$` 和 `$y = x$` 在同一坐标系中的关系。

**解**：

(i) `$y = e^x \Rightarrow x = \ln y \Rightarrow f^{-1}(x) = \ln x$`（定义域 `$x > 0$`）。

(ii) **对称关系**：`$y = e^x$` 和 `$y = \ln x$` 的图形关于直线 `$y = x$` 对称。

具体对应点：
- `$e^x$` 过 `$(0, 1)$` → `$\ln x$` 过 `$(1, 0)$`
- `$e^x$` 过 `$(1, e)$` → `$\ln x$` 过 `$(e, 1)$`
- `$e^x$` 过 `$(-1, e^{-1})$` → `$\ln x$` 过 `$(e^{-1}, -1)$`

每条曲线都是另一条在 `$y = x$` 中的镜像。

**渐近线关系**：
- `$y = e^x$` 有水平渐近线 `$y = 0$`（`$x$` 轴）
- `$y = \ln x$` 有垂直渐近线 `$x = 0$`（`$y$` 轴）
- 这一对渐近线也关于 `$y = x$` 对称

---

**例 7**（复合函数的顺序重要性 `$fg \neq gf$`——考纲 1.7）：`$f(x) = 3x + 1$`，`$g(x) = x^2$`。求 `$fg(x)$` 和 `$gf(x)$`，并比较。

**解**：

`$fg(x) = f(g(x)) = f(x^2) = 3x^2 + 1$`

`$gf(x) = g(f(x)) = g(3x+1) = (3x+1)^2 = 9x^2 + 6x + 1$`

显然 `$fg(x) \neq gf(x)$`，因此复合函数的顺序至关重要。

---

**例 8**（反函数与复合的综合应用）：已知 `$f(x) = 2x + 3$`，`$g(x) = 1/(x-1)$`，`$x \neq 1$`。

(i) 求 `$f^{-1}(x)$`；
(ii) 求 `$gf^{-1}(x)$` 及其定义域。

**解**：

(i) `$y = 2x + 3 \Rightarrow 2x = y - 3 \Rightarrow x = (y-3)/2$`
交换得 `$f^{-1}(x) = (x-3)/2$`，定义域 `$\mathbb{R}$`。

(ii) `$gf^{-1}(x) = g\left(\frac{x-3}{2}\right) = \frac{1}{\frac{x-3}{2} - 1}$`

化简分母：`$(x-3)/2 - 1 = (x-3 - 2)/2 = (x-5)/2$`

因此：

$$`gf^{-1}(x) = \frac{1}{\frac{x-5}{2}} = \frac{2}{x-5}`$$

**定义域**：
- `$f^{-1}(x)$` 的定义域为 `$\mathbb{R}$`（无限制）
- `$g$` 的定义域要求分母不为零：`$(x-3)/2 - 1 \neq 0 \Rightarrow (x-5)/2 \neq 0 \Rightarrow x \neq 5$`

因此定义域为 `$\mathbb{R} \setminus \{5\}$`。

---

**例 9**（考纲 1.6 指定示例——指数函数的反函数）：已知 `$f(x) = e^{2x}$`，求 `$f^{-1}(x)$`。

**解**：

令 `$y = e^{2x}$`。两边取自然对数：

$$`\ln y = \ln(e^{2x}) = 2x \cdot \ln e = 2x`$$

因为 `$\ln e = 1$`，所以 `$2x = \ln y \Rightarrow x = (1/2)\ln y$`。

交换 `$x$` 和 `$y$`：

$$`f^{-1}(x) = \frac{1}{2}\ln x`$$

**定义域**：原函数 `$f(x) = e^{2x}$` 的值域为 `$(0, \infty)$`，所以 `$f^{-1}$` 的定义域为 `$(0, \infty)$`。

**验证**：

$$`f(f^{-1}(x)) = e^{2 \cdot \frac12\ln x} = e^{\ln x} = x \quad (x > 0)`$$

---

**例 10**（验证 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$`——考纲 1.2）：`$f(x) = \sqrt{x}$`，`$g(x) = 1/(x-3)$`。求 `$gf(x)$` 的定义域，并验证 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$`。

**解**：

`$gf(x) = g(f(x)) = 1/(\sqrt{x} - 3)$`

**条件 1**：`$f$` 的定义域：`$x \geq 0$`。
**条件 2**：`$f(x)$` 在 `$g$` 的定义域内：`$\sqrt{x} - 3 \neq 0 \Rightarrow \sqrt{x} \neq 3 \Rightarrow x \neq 9$`。

取交集：`$x \geq 0$` 且 `$x \neq 9$`，即 `$[0, 9) \cup (9, \infty)$`。

验证 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$`：
`$\text{Domain}(f) = [0, \infty)$`，`$\text{Domain}(gf) = [0, 9) \cup (9, \infty) \subset [0, \infty)$`。✓

---

## 4.5 绝对值函数 `$y = |f(x)|$` 的图像

### 4.5.1 基本原理

`$y = |f(x)|$` 的定义为：

$$`|f(x)| = \begin{cases}
f(x), & f(x) \geq 0 \\
-f(x), & f(x) < 0
\end{cases}`$$

**图形变换规则**：取 `$y = f(x)$` 的图形，将 `$x$` 轴下方的部分沿 `$x$` 轴向上**反射**（即取 `$y$` 坐标的绝对值），`$x$` 轴上方和轴上的部分保持不变。

**关键观察**：
- 反射后，图形的所有 `$y$` 值都非负（`$|f(x)| \geq 0$`）。
- 在 `$f(x) = 0$` 的点（即 `$x$` 截距处），绝对值图形会形成**尖点（cusp）**——这是因为左右两侧的斜率突然改变了符号。
- `$|f(x)|$` 的值域是 `$[0, \infty)$`（如果原函数能取到任意大的正值）或 `$[0, \text{有限最大值}]$`。

### 4.5.2 各类函数的绝对值图形特征

| 原函数类型 | `$|f(x)|$` 的特征 |
|-----------|----------------|
| **线性** `$|mx + c|$` | V 形，尖点在 `$x = -c/m$` |
| **二次** `$|ax^2+bx+c|$` | 在 `$x$` 截距处有尖点，抛物线在 `$x$` 轴下方的部分翻折到上方 |
| **三次** `$|(x-p)(x-q)(x-r)|$` | 在三个 `$x$` 截距处都有尖点 |
| **三角** `$|a\sin bx + c|$` | 周期减半（`$\sin$` 从 `$2\pi$` 变为 `$\pi$`），波谷翻为波峰 |
| **三角** `$|a\cos bx + c|$` | 周期减半（`$\cos$` 从 `$2\pi$` 变为 `$\pi$`），波谷翻为波峰 |
| **三角** `$|a\tan bx + c|$` | 垂直渐近线位置不变，图像翻折使 `$y \geq 0$` |

### 4.5.3 绝对值方程与不等式的解法

**方程 `$|f(x)| = k$`（`$k > 0$`）**：等价于 `$f(x) = k$` 或 `$f(x) = -k$`。

**不等式 `$|f(x)| < k$`（`$k > 0$`）**：等价于 `$-k < f(x) < k$`。

**不等式 `$|f(x)| > k$`（`$k > 0$`）**：等价于 `$f(x) > k$` 或 `$f(x) < -k$`。

---

### 例题 4.5（绝对值函数）

**例 1**（线性函数的绝对值——V 形）：画出 `$y = |2x - 3|$` 的草图，并求 `$x$` 为何值时 `$y = 5$`。

**解**：

**画图**：

`$f(x) = 2x - 3$`，`$f(x) = 0$` 时 `$x = 1.5$`。

当 `$x \geq 1.5$` 时，`$2x - 3 \geq 0$`，所以 `$|2x-3| = 2x-3$`（直线，斜率 `$2$`）。
当 `$x < 1.5$` 时，`$2x - 3 < 0$`，所以 `$|2x-3| = -(2x-3) = -2x+3$`（直线，斜率 `$-2$`）。

图形是 V 形，尖点在 `$(1.5, 0)$`。

**解方程 `$|2x-3| = 5$`**：

情况 1：`$2x-3 = 5 \Rightarrow 2x = 8 \Rightarrow x = 4$`
情况 2：`$2x-3 = -5 \Rightarrow 2x = -2 \Rightarrow x = -1$`

解为 `$x = 4$` 或 `$x = -1$`。

---

**例 2**（二次函数的绝对值）：画出 `$y = |x^2 - 4x + 3|$` 的草图。

**解**：

**第一步**（画原函数）：`$y = x^2 - 4x + 3 = (x-1)(x-3)$`。

- `$x$` 截距：`$x = 1$` 和 `$x = 3$`
- 顶点：`$x = 2$`，`$y = 4 - 8 + 3 = -1$`，即 `$(2, -1)$`
- 开口向上（`$a = 1 > 0$`）

**第二步**（取绝对值）：`$[1, 3]$` 区间内原函数 `$f(x) \leq 0$`，这部分翻折到上方。
`$x < 1$` 和 `$x > 3$` 部分 `$f(x) > 0$`，保持不变。

**第三步**（标出关键点）：尖点在 `$x = 1$` 和 `$x = 3$` 处。翻折后原顶点 `$(2, -1)$` 变为 `$(2, 1)$`。

值域为 `$[0, \infty)$`。

---

**例 3**（三次函数的绝对值图像与符号分析）：已知 `$f(x) = (x+1)(x-1)(x-2)$`。

(i) 画出 `$y = |f(x)|$` 的草图；
(ii) 讨论 `$|f(x)|$` 与 `$y = 2$` 的交点情况。

**解**：

(i) **符号分析**：

| 区间 | 测试点 | `$(x+1)$` | `$(x-1)$` | `$(x-2)$` | `$f(x)$` 符号 | `$|f(x)|$` |
|------|--------|---------|---------|---------|------------|----------|
| `$x < -1$` | `$x=-2$` | 负 | 负 | 负 | **负** | 翻折到正 |
| `$-1 < x < 1$` | `$x=0$` | 正 | 负 | 负 | **正** | 保持不变 |
| `$1 < x < 2$` | `$x=1.5$` | 正 | 正 | 负 | **负** | 翻折到正 |
| `$x > 2$` | `$x=3$` | 正 | 正 | 正 | **正** | 保持不变 |

`$x$` 截距：`$x = -1, 1, 2$`（均为尖点）。
`$y$` 截距：`$f(0) = (1)(-1)(-2) = 2$`。

(ii) `$y = 2$` 与 `$|f(x)|$` 的交点：
- 在 `$(-1, 1)$` 区间上，`$f(0) = 2$`，有一个交点。
- 在 `$x < -1$` 翻折区域，`$|f(x)| = -f(x)$`，`$f(-2) = (-1)(-3)(-4) = -12$`，`$|f(-2)| = 12$`，`$f(-1) = 0$`。由连续性，`$y = 2$` 在此区间上相交一次。
- 在 `$(1, 2)$` 翻折区域，同理相交一次。

因此共有 **3 个交点**。

---

**例 4**（`$|a\sin bx + c|$` 形式——考纲 1.4 指定）：画出 `$y = |\sin x|$` 在 `$[0, 2\pi]$` 上的草图。

**解**：

先画 `$y = \sin x$`：
- `$[0, \pi]$` 上 `$\sin x \geq 0$`（`$x$` 轴及上方）
- `$[\pi, 2\pi]$` 上 `$\sin x \leq 0$`（`$x$` 轴下方）

取绝对值：
- `$[0, \pi]$`：保持不变
- `$[\pi, 2\pi]$`：向上反射（`$|\sin x| = -\sin x$`）

图形特征：
- 两个相同的"拱形"：`$(0,0) \to (\pi/2,1) \to (\pi,0)$` 和 `$(\pi,0) \to (3\pi/2,1) \to (2\pi,0)$`
- **周期**：`$\pi$`（原 `$\sin x$` 周期为 `$2\pi$`，反射后每 `$\pi$` 重复一次）
- **值域**：`$[0, 1]$`
- **尖点**：`$x = 0, \pi, 2\pi$` 等处

---

**例 5**（`$|a\cos bx + c|$` 形式——考纲 1.4 指定）：画出 `$y = |3\cos 2x - 1|$` 在 `$[0, \pi]$` 上的草图。

**解**：

**第一步**（画原函数 `$y = 3\cos 2x - 1$`）：
- 振幅为 `$3$`，周期为 `$2\pi/2 = \pi$`，垂直下移 `$1$` 个单位
- 最大值：`$3(1) - 1 = 2$`，最小值：`$3(-1) - 1 = -4$`
- 零点：`$3\cos 2x - 1 = 0 \Rightarrow \cos 2x = 1/3$`
  `$2x = \arccos(1/3)$` 或 `$2x = 2\pi - \arccos(1/3)$`
  即 `$x = (1/2)\arccos(1/3)$` 或 `$x = \pi - (1/2)\arccos(1/3)$`
  记 `$\alpha = (1/2)\arccos(1/3) \approx 0.615$` 弧度

**第二步**（取绝对值）：
- 在两个零点之间的区间（`$\alpha < x < \pi - \alpha$`），`$3\cos 2x - 1 \leq 0$`，这部分翻折到上方
- 其余部分保持不变

**第三步**（标出关键点）：
- 尖点：`$x = \alpha$` 和 `$x = \pi - \alpha$` 处
- 翻折后的"波峰"：原最小值 `$x = \pi/2$` 处 `$3\cos\pi - 1 = -4$` 翻折为 `$4$`
- 原最大值 `$x = 0$` 处 `$2$` 和 `$x = \pi$` 处 `$2$` 保持不变

**最终图形特征**：
- 值域：`$[0, 4]$`
- 尖点数量（一个周期内）：2 个
- 渐近线：无

---

**例 6**（`$|a\tan bx + c|$` 形式——考纲 1.4 指定）：画出 `$y = |\tan x - 1|$` 在 `$(-\pi/2, \pi/2)$` 上的草图。

**解**：

**第一步**（画原函数 `$y = \tan x - 1$`）：
- `$\tan x$` 的周期为 `$\pi$`，垂直下移 `$1$` 个单位
- 垂直渐近线：`$x = -\pi/2$` 和 `$x = \pi/2$`
- 零点：`$\tan x - 1 = 0 \Rightarrow \tan x = 1 \Rightarrow x = \pi/4$`（在区间 `$(-\pi/2, \pi/2)$` 内）
- 当 `$x \to \pi/2^-$` 时，`$\tan x \to \infty$`，`$\tan x - 1 \to \infty$`
- 当 `$x \to -\pi/2^+$` 时，`$\tan x \to -\infty$`，`$\tan x - 1 \to -\infty$`

**第二步**（取绝对值）：
- 当 `$x < \pi/4$` 时，`$\tan x - 1 < 0$`，翻折到上方
- 当 `$x > \pi/4$` 时，`$\tan x - 1 > 0$`，保持不变
- 当 `$x = \pi/4$` 时，`$\tan(\pi/4) - 1 = 0$`，为尖点

**第三步**（分析翻折后的行为）：
- 在 `$x < \pi/4$` 区域，`$|\tan x - 1| = -(\tan x - 1) = 1 - \tan x$`
  当 `$x \to -\pi/2^+$` 时，`$\tan x \to -\infty$`，`$1 - \tan x \to \infty$`
- 在 `$x > \pi/4$` 区域，`$|\tan x - 1| = \tan x - 1$`
  当 `$x \to \pi/2^-$` 时，`$\tan x \to \infty$`，`$\tan x - 1 \to \infty$`

**最终图形特征**：
- 垂直渐近线仍然在 `$x = -\pi/2$` 和 `$x = \pi/2$` 处（绝对值不改变渐近线的位置）
- 尖点在 `$x = \pi/4$` 处
- 图形在尖点两侧都趋向无穷大
- 值域：`$[0, \infty)$`（最小值 `$0$` 在 `$x = \pi/4$` 处）

---

**例 7**（绝对值方程——二次型）：解方程 `$|x^2 - 3x + 2| = 2$`。

**解**：

原二次式 `$x^2 - 3x + 2 = (x-1)(x-2)$`，根为 `$x = 1, 2$`。

**情况 1**：`$x^2 - 3x + 2 = 2$`

$$`x^2 - 3x = 0 \Rightarrow x(x-3) = 0 \Rightarrow x = 0 \text{ 或 } x = 3`$$

检查：`$x = 0$` 时 `$0 - 0 + 2 = 2 > 0$`（在非负区域），有效。`$x = 3$` 时 `$9 - 9 + 2 = 2 > 0$`，有效。

**情况 2**：`$x^2 - 3x + 2 = -2$`

$$`x^2 - 3x + 4 = 0`$$

判别式 `$\Delta = 9 - 16 = -7 < 0$`，无实数解。

因此解为 `$x = 0$` 或 `$x = 3$`。

---

**例 8**（绝对值不等式——两边平方技巧）：解 `$|x-1| > |2x+3|$`。

**解**：

两边都非负，可以平方，不等号方向不变：

$$`(x-1)^2 > (2x+3)^2`$$

展开：

$$`x^2 - 2x + 1 > 4x^2 + 12x + 9`$$

移项：

$$`0 > 3x^2 + 14x + 8 \Rightarrow 3x^2 + 14x + 8 < 0`$$

求根：

$$`x = \frac{-14 \pm \sqrt{196 - 96}}{6} = \frac{-14 \pm 10}{6}`$$

`$x = (-14 + 10)/6 = -4/6 = -2/3$`，`$x = (-14 - 10)/6 = -24/6 = -4$`。

因为二次项系数 `$3 > 0$`，抛物线开口向上，不等式在两根之间成立：

$$`-4 < x < -\frac{2}{3}`$$

---

**例 9**（绝对值函数的最值——几何意义法）：函数 `$f(x) = |x-1| + |x-3|$`，求 `$f(x)$` 的最小值。

**解**：

**几何解释**：`$|x-1|$` 是数轴上 `$x$` 到 `$1$` 的距离，`$|x-3|$` 是 `$x$` 到 `$3$` 的距离。`$f(x)$` 是这两个距离之和。

当 `$x$` 在 `$[1, 3]$` 之间时，两距离之和恒等于线段长度 `$2$`。
当 `$x < 1$` 或 `$x > 3$` 时，和大于 `$2$`。

因此 `$f(x)$` 的最小值为 `$2$`，在 `$x \in [1, 3]$` 时取到。

**代数验证**——分段讨论：

$$`f(x) = \begin{cases}
-(x-1) - (x-3) = -2x + 4, & x < 1 \\
(x-1) - (x-3) = 2, & 1 \leq x \leq 3 \\
(x-1) + (x-3) = 2x - 4, & x > 3
\end{cases}`$$

当 `$x < 1$` 时，`$f(x) = -2x + 4 > 2$`（因为 `$-2x > -2$`）。
当 `$1 \leq x \leq 3$` 时，`$f(x) = 2$`。
当 `$x > 3$` 时，`$f(x) = 2x - 4 > 2$`。

最小值为 `$2$`。

---

## 本章综合练习题

### 一、定义域与值域（6 题）

1. 求 `$f(x) = \sqrt{x+3}/(x-5)$` 的定义域。

2. 已知 `$f(x) = x^2 - 6x + 10$`，`$x \in \mathbb{R}$`。求 `$f(x)$` 的最小值及值域。

3. 求 `$f(x) = \ln(4 - x^2)$` 的定义域和值域。

4. 求 `$f(x) = 1/\sqrt{2x-1} + \ln(5-x)$` 的定义域。

5. 判断 `$y = \pm \sqrt{9 - x^2}$`（`$|x| \leq 3$`）是否为函数，并解释原因。

6. 已知 `$f(x) = (3x-1)/(x+2)$`，求 `$f$` 的值域，并判断 `$f$` 是否为单射。

### 二、线性与三次函数（5 题）

7. 直线 `$L$` 经过点 `$(2, -1)$` 且与直线 `$y = 3x + 2$` 平行，求 `$L$` 的方程。

8. 已知 `$f(x) = x^3 - 2x^2 - 5x + 6$`。
   (i) 验证 `$x = 1$` 是 `$f(x)$` 的一个根，并完全因式分解 `$f(x)$`；
   (ii) 求 `$f(x) = 0$` 的所有解。

9. 已知三次函数 `$f(x) = x^3 - 3x^2 - 9x + 11$`，求 `$f'(x)$` 并解 `$f'(x) = 0$`，判断驻点的类型。

10. 解三次不等式 `$(x+1)(x-2)(x-4) > 0$`。

11. 已知 `$f(x) = x^3 + kx^2 - 5x - 6$` 能被 `$x+2$` 整除，求 `$k$` 的值，并完全因式分解 `$f(x)$`。

### 三、指数与对数（9 题）

12. 化简：`$\log_3 54 - \log_3 2 + \log_3 (1/3)$`。

13. 解方程 `$2^{x+1} = 5$`，结果用自然对数表示。

14. 解方程 `$\log_3 (x+2) + \log_3 (x-4) = 2$`。

15. 解方程 `$3^{2x} - 10 \cdot 3^x + 9 = 0$`。

16. 计算：`$\log_2 3 \cdot \log_3 4 \cdot \log_4 5 \cdot \log_5 2$`。

17. 函数 `$f(x) = 2e^{x} - 3$`。求 `$f(x)$` 的水平渐近线、`$y$` 截距和 `$x$` 截距。

18. 解不等式 `$3^x < 7$`。

19. 已知 `$\log_a 3 = p$`，`$\log_a 7 = q$`。用 `$p, q$` 表示 `$\log_a 21$` 和 `$\log_a (7/3)$`。

20. 解方程 `$\log_2 x + \log_4 x = 3$`。（提示：用换底公式将 `$\log_4 x$` 换为以 `$2$` 为底）

### 四、反函数与复合函数（8 题）

21. 求 `$f(x) = (3x - 2)/4$` 的反函数 `$f^{-1}(x)$`。

22. 已知 `$f(x) = x^2 + 2x$`，`$x \geq -1$`。求 `$f^{-1}(x)$` 及其定义域。

23. 已知 `$f(x) = 2x + 1$`，`$g(x) = 1/x$`。求 `$fg(x)$` 和 `$gf(x)$`，并求 `$gf(x)$` 的定义域。

24. 已知 `$f(x) = e^{3x}$`，求 `$f^{-1}(x)$`。

25. 已知 `$f(x) = \ln(x+2)$`，`$x > -2$`。求 `$f^{-1}(x)$`。

26. 用文字解释为什么 `$f(x) = x^4$`（定义域 `$\mathbb{R}$`）没有反函数。

27. `$f(x) = \sqrt{x+1}$`，`$g(x) = x^2 - 4$`。求 `$fg(x)$` 及其定义域。

28. 已知 `$f(x) = x^2$`，`$x \geq 0$`，`$g(x) = 2x + 1$`。验证 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$`。

### 五、绝对值函数（6 题）

29. 画出 `$y = |x-2|$` 的草图，并解 `$|x-2| = 3$`。

30. 解不等式 `$|2x+1| < 5$`。

31. 画出 `$y = |\cos x|$` 在 `$[0, 2\pi]$` 上的草图，并指出其周期。

32. 解方程 `$|x^2 - 5x + 6| = 2$`。

33. 解不等式 `$|x+2| \geq 3$`。

34. 函数 `$f(x) = |x-2| + |x+1|$`，求 `$f(x)$` 的最小值。

### 六、三角函数绝对值（新增 3 题——考纲 1.4 指定）

35. 画出 `$y = |2\cos x + 1|$` 在 `$[0, 2\pi]$` 上的草图要点，标出关键点和尖点位置。

36. 画出 `$y = |\tan x|$` 在 `$(-\pi/2, \pi/2)$` 上的草图，说明渐近线位置。

37. 已知 `$f(x) = |3\sin 2x - 2|$`，求 `$f(x)$` 在 `$[0, \pi]$` 上的最大值和最小值。

### 七、定义域限制与复合函数存在（新增 1 题——考纲 1.2）

38. 已知 `$f(x) = \sqrt{x-2}$`，`$g(x) = 1/\sqrt{5-x}$`。求 `$gf(x)$` 及其定义域，并验证 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$`。

---

## 综合练习题答案与详细解析

### 一、定义域与值域

**1.**
条件 1（根号）：`$x+3 \geq 0 \Rightarrow x \geq -3$`。
条件 2（分母不为零）：`$x-5 \neq 0 \Rightarrow x \neq 5$`。
取交集：`$[-3, 5) \cup (5, \infty)$`。

---

**2.**
配方法：

$$`f(x) = (x^2 - 6x + 9) + 1 = (x-3)^2 + 1`$$

因为 `$(x-3)^2 \geq 0$`，所以 `$f(x) \geq 1$`。
最小值在 `$x = 3$` 处，`$f(3) = 1$`。
值域为 `$[1, \infty)$`。

---

**3.**
定义域：`$4 - x^2 > 0 \Rightarrow x^2 < 4 \Rightarrow -2 < x < 2$`，即 `$(-2, 2)$`。

值域：令 `$u = 4 - x^2$`，`$x \in (-2, 2)$`，则 `$u \in (0, 4]$`。
`$f(x) = \ln u$`，`$u \in (0, 4]$`。
`$\ln u$` 在 `$(0, 4]$` 上递增，最小趋近于 `$-\infty$`（`$u \to 0^+$`），最大为 `$\ln 4$`（`$u = 4$`）。
值域为 `$(-\infty, \ln 4]$`。

---

**4.**
条件 1（分母根号内）：`$2x-1 > 0$`（严格大于，因在分母中）`$\Rightarrow x > 1/2$`。
条件 2（对数真数）：`$5 - x > 0 \Rightarrow x < 5$`。
取交集：`$(1/2, 5)$`。

---

**5.**
**不是函数**。例如 `$x = 0$` 时，`$y = \pm \sqrt{9 - 0} = \pm 3$`，一个 `$x$` 对应两个 `$y$` 值。垂直检验线 `$x = 0$` 与图形相交于 `$(0, 3)$` 和 `$(0, -3)$` 两点。

---

**6.**
分离常数：

$$`f(x) = \frac{3x-1}{x+2} = \frac{3(x+2) - 7}{x+2} = 3 - \frac{7}{x+2}`$$

因为 `$7/(x+2) \neq 0$`，所以 `$f(x) \neq 3$`。值域为 `$\mathbb{R} \setminus \{3\}$`。

判断单射：设 `$f(a) = f(b)$`：

$$`3 - \frac{7}{a+2} = 3 - \frac{7}{b+2} \Rightarrow \frac{7}{a+2} = \frac{7}{b+2} \Rightarrow a+2 = b+2 \Rightarrow a = b`$$

因此 `$f$` 是单射。

---

### 二、线性与三次函数

**7.**
平行直线斜率相同，`$m = 3$`。
点斜式：`$y - (-1) = 3(x - 2) \Rightarrow y + 1 = 3x - 6 \Rightarrow y = 3x - 7$`。

---

**8.**
(i) `$f(1) = 1 - 2 - 5 + 6 = 0$`，所以 `$x-1$` 是因子。

综合除法：

$$`\begin{array}{c|ccc}
1 & 1 & -2 & -5 & 6 \\
  &   & 1  & -1 & -6 \\
\hline
  & 1 & -1 & -6 & 0
\end{array}`$$

商为 `$x^2 - x - 6 = (x-3)(x+2)$`。

因此 `$f(x) = (x-1)(x-3)(x+2)$`。

(ii) `$f(x) = 0$` 的解为 `$x = 1, 3, -2$`。

---

**9.**
`$f'(x) = 3x^2 - 6x - 9 = 3(x^2 - 2x - 3) = 3(x-3)(x+1)$`。

令 `$f'(x) = 0$`，得 `$x = -1$` 或 `$x = 3$`。

符号检验：
- `$x < -1$`（取 `$x = -2$`）：`$f'(-2) = 3(-5)(-1) = 15 > 0$`，递增
- `$-1 < x < 3$`（取 `$x = 0$`）：`$f'(0) = 3(-3)(1) = -9 < 0$`，递减
- `$x > 3$`（取 `$x = 4$`）：`$f'(4) = 3(1)(5) = 15 > 0$`，递增

`$x = -1$`：递增→递减 → **局部最大值**，`$f(-1) = -1 - 3 + 9 + 11 = 16$`。
`$x = 3$`：递减→递增 → **局部最小值**，`$f(3) = 27 - 27 - 27 + 11 = -16$`。

---

**10.**
三个根：`$x = -1, 2, 4$`。最高次项系数 `$a = 1 > 0$`。

符号分析：

| 区间 | `$x+1$` | `$x-2$` | `$x-4$` | 乘积 |
|------|-------|-------|-------|------|
| `$x < -1$` | 负 | 负 | 负 | **负** |
| `$-1 < x < 2$` | 正 | 负 | 负 | **正** |
| `$2 < x < 4$` | 正 | 正 | 负 | **负** |
| `$x > 4$` | 正 | 正 | 正 | **正** |

`$f(x) > 0$` 的解为 `$-1 < x < 2$` 或 `$x > 4$`。

---

**11.**
由因式定理，`$f(-2) = 0$`：

$$`f(-2) = (-2)^3 + k(4) - 5(-2) - 6 = -8 + 4k + 10 - 6 = 4k - 4 = 0`$$

`$k = 1$`。

综合除法除以 `$(x+2)$`：

$$`\begin{array}{c|ccc}
-2 & 1 & 1 & -5 & -6 \\
   &   & -2 & 2 & 6 \\
\hline
   & 1 & -1 & -3 & 0
\end{array}`$$

商为 `$x^2 - x - 3$`。

因此 `$f(x) = (x+2)(x^2 - x - 3)$`。二次式不可进一步有理分解。

---

### 三、指数与对数

**12.**

$$`\log_3 54 - \log_3 2 = \log_3 \frac{54}{2} = \log_3 27 = 3`$$

$$`3 + \log_3 \frac13 = \log_3 \left(27 \times \frac13\right) = \log_3 9 = 2`$$

值为 `$2$`。

---

**13.**

$$`2^{x+1} = 5 \Rightarrow \ln(2^{x+1}) = \ln 5 \Rightarrow (x+1)\ln 2 = \ln 5`$$

$$`x+1 = \frac{\ln 5}{\ln 2} \Rightarrow x = \frac{\ln 5}{\ln 2} - 1`$$

---

**14.**

$$`\log_3[(x+2)(x-4)] = 2 \Rightarrow (x+2)(x-4) = 3^2 = 9`$$

$$`x^2 - 2x - 8 = 9 \Rightarrow x^2 - 2x - 17 = 0`$$

$$`x = \frac{2 \pm \sqrt{4 + 68}}{2} = \frac{2 \pm \sqrt{72}}{2} = \frac{2 \pm 6\sqrt{2}}{2} = 1 \pm 3\sqrt{2}`$$

检查定义域：`$x+2 > 0$` 且 `$x-4 > 0 \Rightarrow x > 4$`。

`$1 + 3\sqrt{2} \approx 5.24 > 4$`，有效。
`$1 - 3\sqrt{2} \approx -3.24 < 4$`，舍去。

因此 `$x = 1 + 3\sqrt{2}$`。

---

**15.**
令 `$u = 3^x > 0$`，则 `$3^{2x} = (3^x)^2 = u^2$`。

$$`u^2 - 10u + 9 = 0 \Rightarrow (u-1)(u-9) = 0 \Rightarrow u = 1 \text{ 或 } u = 9`$$

`$3^x = 1 \Rightarrow x = 0$`。
`$3^x = 9 \Rightarrow x = 2$`。

解为 `$x = 0$` 或 `$x = 2$`。

---

**16.**
使用换底公式，全部换为自然对数：

$$`\log_2 3 = \frac{\ln 3}{\ln 2}, \quad \log_3 4 = \frac{\ln 4}{\ln 3}, \quad \log_4 5 = \frac{\ln 5}{\ln 4}, \quad \log_5 2 = \frac{\ln 2}{\ln 5}`$$

乘积为：

$$`\frac{\ln 3}{\ln 2} \cdot \frac{\ln 4}{\ln 3} \cdot \frac{\ln 5}{\ln 4} \cdot \frac{\ln 2}{\ln 5} = 1`$$

值为 `$1$`（所有项抵消）。

---

**17.**
水平渐近线：`$x \to -\infty$` 时 `$e^x \to 0$`，`$2e^x - 3 \to -3$`，渐近线为 `$y = -3$`。

`$y$` 截距：`$f(0) = 2e^0 - 3 = 2 - 3 = -1$`，即 `$(0, -1)$`。

`$x$` 截距：`$2e^x - 3 = 0 \Rightarrow e^x = 3/2 \Rightarrow x = \ln(3/2)$`，即 `$(\ln(3/2), 0)$`。

---

**18.**
`$3^x < 7 \Rightarrow \ln(3^x) < \ln 7 \Rightarrow x\ln 3 < \ln 7 \Rightarrow x < (\ln 7)/(\ln 3)$`。

---

**19.**
`$\log_a 21 = \log_a (3 \times 7) = \log_a 3 + \log_a 7 = p + q$`。

`$\log_a (7/3) = \log_a 7 - \log_a 3 = q - p$`。

---

**20.**
用换底公式将 `$\log_4 x$` 换为以 `$2$` 为底：

$$`\log_4 x = \frac{\log_2 x}{\log_2 4} = \frac{\log_2 x}{2}`$$

设 `$u = \log_2 x$`，则原方程化为：

$$`u + \frac{u}{2} = 3 \Rightarrow \frac{3u}{2} = 3 \Rightarrow u = 2`$$

`$\log_2 x = 2 \Rightarrow x = 2^2 = 4$`。

---

### 四、反函数与复合函数

**21.**
`$y = (3x-2)/4 \Rightarrow 4y = 3x - 2 \Rightarrow 3x = 4y + 2 \Rightarrow x = (4y+2)/3$`。

交换得 `$f^{-1}(x) = (4x+2)/3$`。

---

**22.**
配方法：`$f(x) = (x+1)^2 - 1$`，`$x \geq -1$`。值域为 `$[-1, \infty)$`。

令 `$y = (x+1)^2 - 1 \Rightarrow y+1 = (x+1)^2 \Rightarrow x+1 = \sqrt{y+1}$`（因 `$x \geq -1$`，取正根）。

`$x = \sqrt{y+1} - 1$`。

交换得 `$f^{-1}(x) = \sqrt{x+1} - 1$`，定义域为 `$[-1, \infty)$`。

---

**23.**
`$fg(x) = f(g(x)) = f(1/x) = 2(1/x) + 1 = 2/x + 1$`，`$x \neq 0$`。

`$gf(x) = g(f(x)) = g(2x+1) = 1/(2x+1)$`。

`$gf(x)$` 的定义域：`$2x+1 \neq 0 \Rightarrow x \neq -1/2$`，即 `$\mathbb{R} \setminus \{-1/2\}$`。

---

**24.**
`$y = e^{3x} \Rightarrow \ln y = 3x \Rightarrow x = (1/3)\ln y$`。

交换得 `$f^{-1}(x) = (1/3)\ln x$`，定义域 `$x > 0$`。

---

`$f(x) = \ln(x+2)$`，`$x > -2$`。求 `$f^{-1}(x)$`。

**解**：

`$y = \ln(x+2) \Rightarrow e^y = x+2 \Rightarrow x = e^y - 2$`。

交换得 `$f^{-1}(x) = e^x - 2$`。

原函数值域为 `$\mathbb{R}$`，所以 `$f^{-1}$` 的定义域为 `$\mathbb{R}$`。

验证：`$f(f^{-1}(x)) = \ln(e^x - 2 + 2) = \ln(e^x) = x$`。✓

---

**26.**
`$f(x) = x^4$` **不是单射**，因为存在不同的 `$x$` 值对应同一个 `$y$` 值。例如 `$f(2) = 16$` 且 `$f(-2) = 16$`，即 `$2 \neq -2$` 但 `$f(2) = f(-2)$`。水平检验线 `$y = 16$` 与 `$y = x^4$` 的图形相交于 `$(2, 16)$` 和 `$(-2, 16)$` 两点。因此 `$f$` 没有反函数。

> 若将定义域限制为 `$x \geq 0$`，则反函数为 `$f^{-1}(x) = \sqrt[4]{x}$`。

---

**27.**
`$fg(x) = f(g(x)) = \sqrt{(x^2 - 4) + 1} = \sqrt{x^2 - 3}$`。

定义域：`$f(x) = \sqrt{x+1}$` 的定义域为 `$x+1 \geq 0 \Rightarrow x \geq -1$`。

所以 `$g(x) \geq -1 \Rightarrow x^2 - 4 \geq -1 \Rightarrow x^2 \geq 3 \Rightarrow |x| \geq \sqrt{3}$`。

定义域为 `$(-\infty, -\sqrt{3}] \cup [\sqrt{3}, \infty)$`。

---

**28.**
`$f(x) = x^2$`，`$x \geq 0$`，定义域 `$[0, \infty)$`。
`$g(x) = 2x + 1$`，定义域 `$\mathbb{R}$`。

`$gf(x) = g(f(x)) = 2x^2 + 1$`。

`$gf$` 的定义域：`$x$` 必须在 `$f$` 的定义域内（`$x \geq 0$`），且 `$f(x)$` 必须在 `$g$` 的定义域内（`$\mathbb{R}$`，无限制）。

因此 `$\text{Domain}(gf) = [0, \infty)$`。

`$\text{Domain}(f) = [0, \infty)$`，所以 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$` 成立（实际上两者相等）。✓

---

### 五、绝对值函数

**29.**
V 形，尖点在 `$(2, 0)$`。
`$x \geq 2$` 时 `$y = x-2$`；`$x < 2$` 时 `$y = 2-x$`。

解 `$|x-2| = 3$`：
情况 1：`$x-2 = 3 \Rightarrow x = 5$`
情况 2：`$x-2 = -3 \Rightarrow x = -1$`

解为 `$x = 5$` 或 `$x = -1$`。

---

**30.**
`$|2x+1| < 5 \Rightarrow -5 < 2x+1 < 5$`。

左边：`$-5 < 2x+1 \Rightarrow -6 < 2x \Rightarrow -3 < x$`。
右边：`$2x+1 < 5 \Rightarrow 2x < 4 \Rightarrow x < 2$`。

取交集：`$-3 < x < 2$`。

---

**31.**
`$y = |\cos x|$` 在 `$[0, 2\pi]$` 上：
- `$[0, \pi/2]$`：`$\cos x \geq 0$`，从 `$(0,1)$` 下降到 `$(\pi/2, 0)$`
- `$[\pi/2, 3\pi/2]$`：`$\cos x \leq 0$`，翻折到上方：从 `$(\pi/2, 0)$` 上升到 `$(\pi, 1)$` 再下降到 `$(3\pi/2, 0)$`
- `$[3\pi/2, 2\pi]$`：`$\cos x \geq 0$`，从 `$(3\pi/2, 0)$` 上升到 `$(2\pi, 1)$`

周期为 `$\pi$`。尖点在 `$x = \pi/2, 3\pi/2$` 等处。

---

**32.**
`$x^2 - 5x + 6 = (x-2)(x-3)$`。

情况 1：`$x^2 - 5x + 6 = 2 \Rightarrow x^2 - 5x + 4 = 0 \Rightarrow (x-1)(x-4) = 0 \Rightarrow x = 1$` 或 `$x = 4$`。
检查：`$x=1$` 时 `$1-5+6=2>0$`，有效。`$x=4$` 时 `$16-20+6=2>0$`，有效。

情况 2：`$x^2 - 5x + 6 = -2 \Rightarrow x^2 - 5x + 8 = 0$`。
`$\Delta = 25 - 32 = -7 < 0$`，无实数解。

解为 `$x = 1$` 或 `$x = 4$`。

---

**33.**
`$|x+2| \geq 3 \Rightarrow x+2 \leq -3$` 或 `$x+2 \geq 3$`。

`$x+2 \leq -3 \Rightarrow x \leq -5$`。
`$x+2 \geq 3 \Rightarrow x \geq 1$`。

解为 `$x \leq -5$` 或 `$x \geq 1$`。

---

**34.**
几何解释：`$|x-2|$` 是 `$x$` 到 `$2$` 的距离，`$|x+1| = |x-(-1)|$` 是 `$x$` 到 `$-1$` 的距离。`$f(x)$` 是这两段距离之和。

数轴上点 `$-1$` 到 `$2$` 的距离为 `$3$`。当 `$x$` 在 `$[-1, 2]$` 之间时，两距离之和恒等于 `$3$`。当 `$x < -1$` 或 `$x > 2$` 时，和大于 `$3$`。

因此最小值为 `$3$`，在 `$x \in [-1, 2]$` 时取到。

代数验证：

$$`f(x) = \begin{cases}
-(x-2) - (x+1) = -2x + 1, & x < -1 \\
-(x-2) + (x+1) = 3, & -1 \leq x \leq 2 \\
(x-2) + (x+1) = 2x - 1, & x > 2
\end{cases}`$$

当 `$x < -1$` 时，`$f(x) = -2x + 1 > 3$`。
当 `$-1 \leq x \leq 2$` 时，`$f(x) = 3$`。
当 `$x > 2$` 时，`$f(x) = 2x - 1 > 3$`。

最小值为 `$3$`。

---

### 六、三角函数绝对值

**35.**
`$y = 2\cos x + 1$` 在 `$[0, 2\pi]$` 上：
- 最大值：`$2(1) + 1 = 3$`（`$x = 0, 2\pi$`）
- 最小值：`$2(-1) + 1 = -1$`（`$x = \pi$`）
- 零点：`$2\cos x + 1 = 0 \Rightarrow \cos x = -1/2 \Rightarrow x = 2\pi/3, 4\pi/3$`

取绝对值后：
- `$[2\pi/3, 4\pi/3]$` 区间内 `$2\cos x + 1 \leq 0$`，翻折到上方
- 其余部分保持不变
- 尖点：`$x = 2\pi/3$` 和 `$x = 4\pi/3$`
- 翻折后最大值出现在 `$x = \pi$` 处，`$|2\cos\pi + 1| = |-2+1| = 1$`
- 原最大值 `$x = 0$` 处 `$3$` 和 `$x = 2\pi$` 处 `$3$` 保持不变

值域：`$[0, 3]$`。

---

**36.**
`$y = |\tan x|$` 在 `$(-\pi/2, \pi/2)$` 上：

- `$(-\pi/2, 0)$`：`$\tan x < 0$`，绝对值后翻折到上方，`$|\tan x| = -\tan x$`
- `$(0, \pi/2)$`：`$\tan x > 0$`，保持不变，`$|\tan x| = \tan x$`
- `$x = 0$`：`$\tan 0 = 0$`，为尖点

**渐近线**：`$x = -\pi/2$` 和 `$x = \pi/2$` 仍然是垂直渐近线（绝对值不改变渐近线位置）。

图形特征：
- 在 `$x = 0$` 处有尖点
- 在 `$x \to \pm\pi/2$` 时，`$|\tan x| \to \infty$`
- 值域：`$[0, \infty)$`
- 图形关于 `$x = 0$` 对称（偶函数），因为 `$|\tan(-x)| = |-\tan x| = |\tan x|$`

---

**37.**
`$f(x) = |3\sin 2x - 2|$`，`$x \in [0, \pi]$`。

先分析 `$y = 3\sin 2x - 2$`：
- 振幅 `$3$`，周期 `$\pi$`，垂直下移 `$2$`
- 最大值：`$3(1) - 2 = 1$`，最小值：`$3(-1) - 2 = -5$`
- 零点：`$3\sin 2x - 2 = 0 \Rightarrow \sin 2x = 2/3$`
  `$2x = \arcsin(2/3)$` 或 `$2x = \pi - \arcsin(2/3)$`
  记 `$\alpha = (1/2)\arcsin(2/3) \approx 0.364$` 弧度
  则 `$x = \alpha$` 或 `$x = \pi/2 - \alpha$`

在 `$[0, \pi]$` 上，`$3\sin 2x - 2$` 在 `$[\alpha, \pi/2 - \alpha]$` 区间为负，翻折到上方。

最大值：翻折后，原最小值 `$-5$`（`$x = 3\pi/4$`）变为 `$5$`。所以 `$f(x)$` 的最大值为 `$5$`。
最小值：`$0$`（在零点 `$x = \alpha$` 和 `$x = \pi/2 - \alpha$` 处）。

因此 `$f(x)$` 在 `$[0, \pi]$` 上的最大值为 `$5$`，最小值为 `$0$`。

---

### 七、定义域限制与复合函数存在

**38.**
`$f(x) = \sqrt{x-2}$`，定义域 `$[2, \infty)$`。
`$g(x) = 1/\sqrt{5-x}$`，定义域 `$x < 5$`。

`$gf(x) = g(f(x)) = 1/\sqrt{5 - \sqrt{x-2}}$`。

定义域条件：
1. `$f$` 的定义域：`$x \geq 2$`
2. `$f(x)$` 在 `$g$` 的定义域内：
   - `$5 - \sqrt{x-2} > 0$`（分母根号内为正）
   - `$\sqrt{x-2} < 5 \Rightarrow x-2 < 25 \Rightarrow x < 27$`
   - `$5 - \sqrt{x-2} \neq 0$` 已由 `$>0$` 保证

取交集：`$2 \leq x < 27$`。

验证 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$`：
`$\text{Domain}(f) = [2, \infty)$`，`$\text{Domain}(gf) = [2, 27) \subset [2, \infty)$`。✓

---

> **复习建议**：函数是 IGCSE 0606 附加数学的基石——微分、积分、曲线草图、运动学都建立在函数概念之上。本章的重点是：
>
> 1. 熟练掌握定义域和值域的计算（尤其是根号、分母、对数同时出现时）
> 2. 深刻理解指数与对数的互逆关系（`$a^{\log_a x} = x$`，`$\log_a(a^x) = x$`）
> 3. 掌握对数运算法则的灵活运用（特别是换底公式 `$\log_a M = (\log_b M)/(\log_b a)$`）
> 4. 熟练求反函数和复合函数，注意定义域的变化
> 5. 能够画出 `$|f(x)|$` 的变换图形（线性、二次、三次、三角 `$a\sin bx + c$`、`$a\cos bx + c$`、`$a\tan bx + c$`）
> 6. 能用文字准确解释函数没有反函数的原因（考纲 1.5）
> 7. 理解 `$\text{Domain}(gf) \subseteq \text{Domain}(f)$` 和 `$\text{Range}(gf) \subseteq \text{Range}(g)$`（考纲 1.2）

---
---
